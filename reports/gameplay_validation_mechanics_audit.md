# Gameplay Validation Mechanics Audit

## Round 1 - 2026-06-03

- Preflight: git clean at `d306cd8 Unify combat ending player HP reads`.
- Baseline: `D:\anaconda\envs\stsai\python.exe -m pytest -q --basetemp C:\Users\20571\Documents\Codex\2026-06-03\d-pycharmprojects-slay-the-spire-ai\.pytest-basetemp` -> 1453 passed before changes.
- Validation: CommunicationMod ran `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance` with Windows Python.
- Outcome: no Ironclad `victory=true`; AI markers `1780450800` through `1780452924`; latest visible run `1780452921.run` died at floor 16 to Hexaghost.
- Failure type: lethal-sequence execution drift during RL energy-guard fallback takeover. A proven sequence could be planned, but `OptimizedAgent` invalidated the cached sequence after the first card left hand, replanned mid-turn, and could abandon the remaining lethal line.
- Fix: keep executing cached combat sequences when the next planned action is still available, and advance `current_action_index` after returning a newly planned first action.
- Regression: `test_optimized_agent_continues_cached_sequence_after_played_card_leaves_hand`.
- Verification after fix: focused related tests `56 passed`; full pytest `1454 passed`.
- Next candidate: inspect post-fix bounded validation for remaining Act 1 boss deaths and late Act 2 boss deaths, especially low-HP turns where fallback selects zero-damage setup cards or Havoc before lethal/defense.

## Round 2 - 2026-06-03

- Preflight: git clean at `c35d6df Penalize lethal current combat lines`.
- Baseline: full pytest with an isolated `STS_AI_LOG_FILE`, disabled pytest cache, and unique basetemp -> 1455 passed before changes.
- Validation: controlled CommunicationMod restart with Windows Python ran `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance`.
- Outcome before interruption: no Ironclad `victory=true`; new completed runs were `1780455201.run` (floor 19, killed by Shell Parasite) and `1780455267.run` (floor 16, killed by Slime Boss).
- Failure type: HAND_SELECT selection-count mismatch during a floor 16 boss state. The game already had one selected card with `max_cards=3`, but `SimpleAgent.handle_screen()` still requested three more cards. `CardSelectAction` rejected the action with `Too many cards selected (provided 3, max 2)`, and the batch loop repeatedly retried the same in-game screen without producing further `.run` records.
- Fix: HAND_SELECT now computes remaining required cards as `num_cards - len(selected_cards)`, avoids reselecting already-selected card objects, and confirms when the required count is already satisfied.
- Regression: `test_hand_select_only_selects_remaining_required_cards`.
- Verification after fix: focused regression `1 passed`; related card-select/agent/RL context tests `27 passed`; full pytest `1456 passed`.
- Next candidate: restart bounded validation after this commit and inspect remaining floor 16 boss deaths, especially RL EndTurnAction choices that force fallback takeover at low HP.

## Round 3 - 2026-06-03

- Preflight: git clean at `94d9c9e Respect remaining hand select count`.
- Baseline: full pytest with isolated `STS_AI_LOG_FILE`, disabled pytest cache, and unique basetemp -> 1456 passed.
- Validation: controlled CommunicationMod fresh run with Windows Python; log reached `Max games reached (20); exiting.`
- Outcome: no Ironclad `victory=true`. Visible new run files after the fresh start were 19 deaths: Act 1 boss deaths to The Guardian/Hexaghost dominated, with later deaths to Collector, Champ, Cultist and Chosen, Sentry and Sphere, and Gremlin Gang.
- Failure type: HAND_SELECT no longer over-selected, but the queued optional confirm skipped HAND_SELECT confirmations when `confirm` was already available and `confirm_up` was false. Logs showed repeated HAND_SELECT callbacks and repeated `KEY action: key=CARD_*` selections followed by `Skipping optional card-select confirm`.
- Fix: allow stale optional card-select confirm on HAND_SELECT only when the `confirm` command is actually available, while preserving the existing guard that avoids confirming HAND_SELECT screens without confirm.
- Regression: `test_stale_card_select_confirm_fires_for_hand_select_with_confirm_available`.
- Verification after fix: focused regression `1 passed`; related card-select/agent tests `23 passed`; full pytest `1457 passed`.
- Next candidate: rerun bounded validation and inspect repeated Act 1 boss deaths, especially low-HP Hexaghost/Guardian turns where RL ends turn and fallback takeover chooses defensive or setup lines that still leave lethal current-turn damage.

## Round 4 - 2026-06-03

- Preflight: `ce442d7 Confirm stale hand select screens`; same completed CommunicationMod batch was used for follow-up attribution before launching the next bounded validation.
- Baseline evidence: latest visible run from that batch, `1780458068.run`, died at floor 16 to Hexaghost with no `victory=true` in the batch.
- Failure type: healing-potion conservation risk. Logs showed `[POTION_GUARD] Using Blood Potion: incoming=16 hp=73/80 room=MonsterRoom monsters=3`, spending the run's only percent-heal potion in a high-HP normal fight. The later Hexaghost lethal turn ended at 9 HP before 24 incoming; keeping Blood Potion would have enabled survival with the already-selected 10 block line. Run potion usage fields were not treated as authoritative because logs showed the actual potion usage.
- Fix: healing/regen/fairy potion scoring now requires immediate lethal pressure, low-HP pressure, or moderate boss pressure. Damage, block, energy, and other potion classes keep the existing dangerous-combat thresholds.
- Regression: `test_potion_guard_saves_healing_potion_when_hp_is_high`; positive guard `test_potion_guard_uses_healing_potion_to_survive_lethal_turn`.
- Verification after fix: focused healing-potion regressions `2 passed`; `tests/test_combat_rl_guards.py` `36 passed`; full pytest `1459 passed`.
- Next candidate: rerun bounded validation from `ce442d7` plus this potion fix and inspect remaining Act 1 boss deaths, especially low-HP boss turns where saved potion use, fallback takeover, and current-turn lethal scoring interact.

## Round 5 - 2026-06-03

- Preflight: git clean at `0888c65 Conserve healing potions for emergencies`; CommunicationMod still used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance`.
- Baseline: full pytest with isolated `STS_AI_LOG_FILE`, disabled pytest cache, and unique basetemp -> 1459 passed.
- Validation: controlled CommunicationMod fresh run started at 11:55 CST. It completed 8 visible runs with no `victory=true`: deaths to Automaton twice, Hexaghost three times, Slime Boss once, 3 Sentries once, and Centurion and Healer once. The 9th game marker was `1780459838`, but no matching `.run` was written.
- Interruption: the batch was intentionally stopped with `restart_sts_modded.ps1 -SkipLaunch` after the repeated potion-action evidence was selected as the focused fix target, so the 9th game marker without a completed `.run` is a manual stop, not an organic no-log blocker.
- Failure type: potion command pacing/state-stability risk. Runtime logs selected `PotionAction` repeatedly, including guard-selected Block/Elixir/Energy/Blessing/Smoke Bomb/Strength/Dexterity potions; one combat selected Smoke Bomb and then Blessing of the Forge on consecutive callbacks before any explicit state-stabilizing wait. All completed run files still reported `potions_floor_usage=[]`, so policy changes would be premature until the action execution boundary is stabilized.
- Fix: `PotionAction.execute()` now queues `WaitAction(timeout=1)` after potion use/discard commands, matching the stabilization pattern already used for shop potion purchases and forcing a state update opportunity before the next decision.
- Regression: `test_potion_action_execute_uses_get_real_potions_without_raw_potions` now asserts both the `potion use 0` command and the queued `WaitAction`.
- Verification after fix: focused regression red `1 failed` for missing queued wait, then green `1 passed`; `tests/test_combat_rl_guards.py` `36 passed`; full pytest `1459 passed`.
- Next candidate: rerun bounded validation from `50ce48e Stabilize potion action updates` and verify whether completed run files now show non-empty potion usage when logs select PotionAction; if potion records remain empty, inspect sent command/effect confirmation at the CommunicationMod boundary before changing potion selection policy.

## Round 6 - 2026-06-03

- Preflight: git clean at `3d7164b Consolidate round five audit note`; full pytest baseline with disabled pytest cache and unique basetemp -> `1459 passed`.
- Validation: controlled CommunicationMod fresh run from `3d7164b`; `restart_sts_modded.ps1 -FreshRun` moved the leftover `IRONCLAD.autosave` from the manually stopped Round 5 batch before launch.
- Outcome before stopping for a focused fix: no Ironclad `victory=true`; completed new run `1780460697.run` died at floor 16 to Hexaghost and still reported `potions_floor_usage=[]`. Runtime logs from the same run selected `PotionAction` for Block Potion/Fear Potion; the following in-progress run also selected Dexterity Potion and Skill Potion before the batch was manually stopped for the focused fix.
- Failure type: potion command wait sequencing. The first potion-action stabilization fix queued `WaitAction(timeout=1)`, but `WaitAction` defaults to `requires_game_ready=False`, so the main loop could send `potion use ...` and `wait 1` back-to-back in the same action drain before the potion command response restored `game_is_ready`. That did not actually prove a post-potion state refresh.
- Fix: the wait queued by `PotionAction.execute()` is now ready-gated by setting `requires_game_ready=True`, so the coordinator waits for the potion command response before sending the stabilizing `wait 1`.
- Regression: `test_potion_action_execute_uses_get_real_potions_without_raw_potions` now asserts the queued `WaitAction` is `requires_game_ready=True`.
- Verification after fix: focused regression red `1 failed` on the non-ready-gated wait, then green `1 passed`; `tests/test_combat_rl_guards.py` `36 passed`; full pytest `1459 passed`.
- Next candidate: rerun bounded validation from the ready-gated potion wait commit. If completed run files still show zero potion usage after logged PotionAction decisions, add direct command/effect instrumentation at `PotionAction.execute()` and CommunicationMod response handling before changing gameplay potion policy.

## Round 7 - 2026-06-03

- Preflight: git clean at `ef583bf Gate potion wait on command readiness`; the ready-gated potion wait fix had full pytest verification -> `1459 passed`.
- Validation: controlled CommunicationMod fresh run with Windows Python after clearing a stale `Continue` autosave state; `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance` reached `Max games reached (20); exiting.`
- Outcome: no Ironclad `victory=true`; 20 AI markers were written and 19 completed `.run` files were visible from this fresh batch. The completed deaths were Act 1 boss-heavy, including six The Guardian deaths, two Slime Boss deaths, one Hexaghost death, plus several Act 2/3 deaths. Marker `1780462402` had no visible completed `.run`, but the batch continued writing later run files and did not show current Python tracebacks, so it was recorded as a side observation rather than the selected blocker.
- Failure type: energy-potion conservation risk. In latest completed run `1780464272.run`, the AI died to The Guardian on floor 16. Runtime logs showed `[POTION_GUARD] Using Energy Potion: incoming=0 hp=68/80 room=MonsterRoomBoss` on boss turn 1, then a later lethal Guardian turn with `8 HP`, `incoming=32`, and a hand where saving Energy Potion would have enabled the defensive `Shockwave+` plus three Defends line instead of dying without enough energy.
- Fix: energy/draw/randomize-cost potions no longer get a positive danger-guard score merely because the room is elite or boss; the guard now uses them only when there is meaningful current-turn incoming damage. Other potion categories keep their existing boss/elite danger behavior.
- Regression: `test_potion_guard_saves_energy_potion_on_safe_boss_turn` reproduced the unsafe boss-turn Energy Potion use and failed red; `test_potion_guard_uses_energy_potion_under_current_turn_pressure` keeps the lethal-pressure positive path.
- Verification after fix: focused Energy Potion tests `2 passed`; `tests/test_combat_rl_guards.py` `38 passed`; full pytest `1461 passed`.
- Next candidate: rerun bounded validation from this energy-potion conservation commit and inspect repeated Guardian/Slime Boss deaths, especially whether saved Energy Potion appears in late lethal boss turns and whether the missing marker-without-run observation recurs.

## Round 8 - 2026-06-03

- Preflight: git clean at `ff577d4 Conserve energy potions for pressure turns`; post-commit full pytest baseline -> `1461 passed`.
- Validation: controlled CommunicationMod fresh run with Windows Python. The batch was stopped manually after the selected crash was attributed, so it intentionally did not reach the 20-game bound. Before stopping, it wrote 3 AI markers and 2 completed `.run` files: `1780465389.run` died at floor 16 to Slime Boss and `1780465520.run` died at floor 25 to Snake Plant.
- Failure type: stale queued card action crash. During game #1 on floor 10, Elixir opened HAND_SELECT, stale card-select confirms produced invalid confirm errors, RL then played True Grit, and fallback planned Burning Pact from the latest hand. Before the queued `PlayCardAction` executed, the bound card UUID was no longer in `last_game_state.hand`, so `PlayCardAction.execute()` raised `Specified card for CardAction is not in hand`, aborting `play_one_game` and causing the batch loop to resume mid-run as game #2.
- Fix: when a UUID-bound `PlayCardAction` no longer finds that UUID in the latest hand, execution now logs a warning, sends `state`, and returns without sending a stale `play` command or raising. This lets the next state callback replan from the actual game state instead of failing the run.
- Regression: `test_play_card_action_requests_state_when_uuid_card_left_hand` reproduced the exception path red and now asserts the safe `state` request.
- Verification after fix: focused stale-card regression `1 passed`; related action/RL guard tests `40 passed`; full pytest `1462 passed`.
- Next candidate: rerun bounded validation from this stale-card guard commit and check whether HAND_SELECT stale confirm errors still occur without aborting runs. If they recur but no longer crash, inspect the Elixir/HAND_SELECT selection loop as a separate focused target.

## Round 9 - 2026-06-03

- Preflight: git clean at `0208f87 Guard stale queued card actions`; post-commit full pytest baseline -> `1462 passed`.
- Validation: controlled CommunicationMod fresh run with Windows Python. The batch was stopped manually after the selected repeated-error issue was attributed. Before stopping, it completed 3 visible `.run` files with no `victory=true`: `1780466186.run` died at floor 16 to Slime Boss, `1780466237.run` died at floor 16 to The Guardian, and `1780466425.run` died at floor 33 to Automaton.
- Failure type: repeated command-error propagation. Stale `confirm` commands still occurred after HAND_SELECT or screen transitions, but the immediate damage was that the same `Invalid command: confirm` error was passed through `Coordinator.receive_game_state_update()` to `CombatRLAgent.handle_error()` on consecutive state updates. That incremented `rl_failure_count` three times for one stale-confirm episode, disabled RL for the rest of the run, and polluted the validation signal.
- Fix: coordinator command-error handling now deduplicates consecutive identical errors until a clean state arrives. It clears the queue, processes the first distinct error through `error_callback`, suppresses repeated identical errors, clears `last_error`, and requests `state` when no recovery action is queued.
- Regression: `test_repeated_command_error_is_handled_once_and_resyncs_state` reproduced two consecutive `Invalid command: confirm` messages red and now asserts one callback plus two state resyncs.
- Verification after fix: focused repeated-error regression `1 passed`; related coordinator/card/action tests `10 passed`; full pytest `1463 passed`.
- Next candidate: rerun bounded validation from this coordinator error-dedupe commit. If stale confirms still occur but no longer disable RL, inspect the source of repeated HAND_SELECT confirms; if no protocol errors recur, return to Act 1 boss failure attribution.

## Round 10 - 2026-06-03

- Preflight: git clean at `5300275 Deduplicate repeated command errors`; post-commit full pytest baseline -> `1463 passed`.
- Validation: controlled CommunicationMod fresh run with Windows Python. The batch was stopped manually after the remaining HAND_SELECT stale-confirm source was attributed. Before stopping, it completed 7 visible `.run` files with no `victory=true`; all seven died at floor 16 to Act 1 bosses, dominated by Slime Boss.
- Failure type: HAND_SELECT command pacing. The coordinator dedupe fix worked: repeated `Invalid command: confirm` responses were suppressed instead of three-striking RL. The source still remained: `CardSelectAction` queued multiple HAND_SELECT `KeyAction`s and an optional stale confirm, all with `requires_game_ready=False`, so a single callback could send several card keys and `confirm` before any state response reflected the selections. That produced repeated stale confirm errors in game #8.
- Fix: `KeyAction` now supports explicit ready gating and response waiting. `CardSelectAction` uses ready-gated, response-waiting key commands for HAND_SELECT only, and the final optional HAND_SELECT confirm is also ready-gated. GRID selection keeps its existing immediate stale-confirm behavior.
- Regression: `test_hand_select_card_select_waits_between_keys_and_confirm` reproduced the old immediate HAND_SELECT queue red and now asserts key actions plus final confirm require game readiness, with HAND_SELECT keys waiting for responses.
- Verification after fix: focused HAND_SELECT queue regression `1 passed`; related card/coordinator/action tests `11 passed`; full pytest `1464 passed`.
- Next candidate: rerun bounded validation from this HAND_SELECT pacing commit. If protocol errors stop, attribute the repeated Act 1 boss deaths; if HAND_SELECT confirm errors persist, inspect whether `SimpleAgent.handle_screen()` is reselecting too many cards before selected-card state updates arrive.

## Round 11 - 2026-06-03

- Preflight: git clean at `18189dd Gate hand select key confirms`; post-commit full pytest baseline -> `1464 passed`.
- Validation: controlled CommunicationMod fresh run with Windows Python reached `Max games reached (20); exiting.` The batch produced 20 AI markers and 19 visible completed `.run` files; none had `victory=true`.
- Outcome: visible deaths included four Hexaghost, four The Guardian, two Slime Boss, one 3 Sentries, and several Act 2 boss/event deaths. The highest-floor visible failures were floor 33 Champ/Automaton/Collector and floor 31 Sentry and Sphere.
- Failure type: unsafe automatic Elixir use. With protocol errors quiet, logs showed `[POTION_GUARD] Using Elixir` twice in ordinary dangerous combats. The latest selected failure was `1780470582.run`, which died on floor 27 to Colosseum Slavers after guard-used Elixir opened HAND_SELECT on turn 1, exhausted three selected cards, and left only `Bloodletting` plus `Blood for Blood`; the turn ended with `energy_remaining=4` and an empty hand before the run died four turns later.
- Fix: potion guard scoring now refuses `exhaust_hand_select` / `Elixir` in automatic danger-guard use, so Elixir no longer falls through to the generic unknown-potion positive score for `incoming >= 18`, elite, or boss states.
- Regression: `test_potion_guard_does_not_auto_use_elixir_hand_select_potion` reproduced the Colosseum-style high-incoming state red, then passed after the scoring guard.
- Verification after fix: focused regression `1 passed`; `tests/test_combat_rl_guards.py` `39 passed`; default full pytest first hit a Windows temp permission error (`C:\Users\20571\AppData\Local\Temp\pytest-of-20571`), then isolated full pytest with repo-local basetemp and disabled cache provider passed `1465 passed`.
- Next candidate: rerun bounded validation from this Elixir guard commit. If potion policy remains the clearest signal, inspect boss-opening debuff use such as `Weak Potion` on Hexaghost turn 1 with only `incoming=5`; otherwise return to the dominant Act 1 boss deaths and late Act 2/3 boss failures.

## Round 12 - 2026-06-03

- Preflight: git clean at `23ef5de Skip automatic Elixir guard use`; post-commit full pytest baseline with isolated repo-local basetemp -> `1465 passed`.
- Validation: CommunicationMod stayed on Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance`. The first fresh launch reached the Slay the Spire main menu but did not start a run; process/log checks and offline model-load probes indicated a one-off CommunicationMod/pipe startup deadlock, not a code model-load blocker. A controlled restart then ran the bounded batch to `Max games reached (20); exiting.`
- Outcome: no Ironclad `victory=true`; the restarted batch wrote 20 AI markers and 18 visible completed `.run` files. The best visible run was `1780475627.run`, floor 50, died to Donu and Deca. Other visible deaths included repeated Act 1 bosses, Act 2 hallway/elites, and Act 2 bosses.
- Failure type: unsafe automatic utility/choice potion use on safe boss turns. Runtime logs showed repeated `POTION_GUARD` uses of `Attack Potion`, `Gambler's Brew`, `Blessing of the Forge`, and `Distilled Chaos` on `MonsterRoomBoss` turns with `incoming=0` or only `incoming=5`. These potions can open card-choice or hand-select-like decision screens or consume a one-time burst resource, so boss-room presence alone is not a sufficient danger signal. Matching logs also showed the same potion family being useful under `incoming>=18` normal combat pressure.
- Fix: potion guard scoring now treats utility/choice effects (`card_choice_*`, `discard_draw`, `play_top_cards`, `upgrade_hand`, `duplicate_next_card`, `return_discard_card`, and similar generated-card utility effects) as current-pressure tools. They no longer get a positive automatic score merely because the room is elite or boss, but they remain eligible when current incoming damage is at least 18.
- Regression: `test_potion_guard_saves_utility_choice_potion_on_safe_boss_turn` reproduced the boss-opening `Attack Potion` use red; `test_potion_guard_uses_utility_choice_potion_under_current_turn_pressure` preserves the high-pressure positive path.
- Verification after fix: focused utility-choice tests `2 passed`; `tests/test_combat_rl_guards.py` `41 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1467 passed`.
- Next candidate: rerun bounded validation from this commit. If potion noise drops, inspect dominant remaining boss failures, especially floor 50 Donu/Deca and repeated Act 1 boss deaths where RL opens with `EndTurnAction` and fallback takeover must carry the whole turn.

## Round 13 - 2026-06-03

- Preflight: git clean at `f50c3c5 Conserve utility potions for pressure turns`; CommunicationMod config still used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance`.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1467 passed`.
- Validation: controlled CommunicationMod fresh run started at 17:17 CST. The batch reached a successful Ironclad validation game before the 20-game bound, so the automation was stopped with `restart_sts_modded.ps1 -SkipLaunch` after confirming the run record.
- Outcome: goal stop condition reached. `1780479519.run` recorded `floor_reached=51`, `victory=true`, score `664`, ascension `0`, character `IRONCLAD`, playtime `203`, with no `killed_by`.
- Failure/fix selection: no new fix was selected because the audit objective was satisfied by the victory record. Earlier non-victory games in the same batch still showed deaths to Act 1 bosses and Act 2 fights, and logs contained a few low-pressure boss potion uses for setup/status potions, but those remain candidates rather than blockers after the successful run.
- Verification after outcome: the victory `.run` was parsed directly from `D:\SteamLibrary\steamapps\common\SlayTheSpire\runs\IRONCLAD\1780479519.run`; the validation processes were stopped cleanly and no new gameplay batch was launched.
- Next candidate if continuing beyond the first-win goal: inspect low-pressure boss use of setup/status potions such as `Flex Potion`, `Fear Potion`, `Liquid Bronze`, and `Ancient Potion`, separating burst/status tools from permanent scaling before writing any policy regression.

## Round 14 - 2026-06-04

- Preflight: git tracked tree clean at `7607393 Guard Act 1 pre-boss potion use`; `git status --short` initially warned on three pytest basetemp ACL leftovers, which were later removed. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1481 passed`.
- Validation: controlled CommunicationMod fresh run reached `Max games reached (20); exiting.` It wrote 20 AI markers and 19 visible completed `.run` files; marker `1780549210` had no matching visible `.run`.
- Outcome: no Ironclad `victory=true`. Visible deaths included four Slime Boss, four Hexaghost, one Guardian, two 3 Byrds, two Shelled Parasite and Fungi, and several Act 2 fights. Compared with the previous Act 1 boss replay snapshot, Slime/Hex deaths appeared lower (`8/19` visible records versus `10/20`), but the missing run record makes this only a directional signal. `POTION_SAVE_GUARD` fired frequently before Act 1 bosses, while some actual pre-boss `PotionAction` callbacks still occurred.
- Failure type: stale queued EndTurnAction crossed a combat turn boundary. In `1780549029.run` on floor 20, killed by 3 Byrds, logs showed RL selected `EndTurnAction` on turn 1 with `energy=0`, then that stale action executed after the game had advanced to fresh turns with `energy=3` and large hands, directly ending turns 2 through 5.
- Fix: `EndTurnAction` now carries optional `expected_floor` and `expected_turn`; if execution sees a different latest combat context, it requests `state` instead of sending `end`. `CombatRLAgent` stamps EndTurn actions returned from RL, fallback takeover, fallback replacement, and final OptimizedAgent fallback with the current combat context.
- Regression: `test_end_turn_action_requests_state_when_turn_snapshot_is_stale` and `test_rl_end_turn_action_is_stamped_with_combat_turn_context` reproduced the bug red (`end` sent instead of `state`; missing context stamp) before the fix.
- Verification after fix: new focused regressions `2 passed`; related action/RL guard files `54 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1483 passed`.
- Next candidate: rerun bounded validation from this stale-EndTurn guard commit and check whether 3 Byrds no longer shows multi-turn direct-end behavior. Continue tracking Slime/Hex pre-boss potion burn separately from boss-window potion use, because the current batch suggests the pre-boss guard is active but not yet enough to prove boss survival improvement.

## Round 15 - 2026-06-05

- Preflight: git tracked tree clean at `b52365d Add validation workflow diagnostics`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1512 passed`.
- Validation: controlled CommunicationMod fresh run from `b52365d` was stopped after a focused failure target was attributed. It completed 3 visible `.run` files after cutoff `1780590624`: `1780619220.run` died at floor 22 to Snake Plant, `1780619304.run` died at floor 14 to Exordium Thugs, and `1780619373.run` died at floor 16 to Slime Boss. No `victory=true` appeared, and `communication_mod_errors.log` had no active Python traceback.
- Failure type: unsafe fallback Elixir auto-use. In `1780619304.run`, live decision trace showed the AI at 13 HP versus 10 incoming using Elixir from `OptimizedAgent`/fallback potion logic after playing two attacks. Elixir opened HAND_SELECT and exhausted the remaining Defends/Bash, leaving 0 block before the Thug hit and causing a lethal next turn. The existing `CombatRLAgent` potion guard already skipped Elixir, so the selected risk was the fallback/optimized automatic potion path.
- Fix: automatic potion helpers now identify `exhaust_hand_select` / `ElixirPotion` as unsafe for blind use. `OptimizedAgent.use_next_potion()` skips those potions in high-danger generic scoring, and `SimpleAgent.use_next_potion()` skips them as well so boss fallback does not reintroduce the same hand-select risk.
- Regression: `test_optimized_agent_does_not_auto_use_elixir_hand_select_potion_in_danger` and `test_optimized_agent_boss_fallback_does_not_auto_use_elixir_hand_select_potion` reproduced the two automatic-use paths red before the fix.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; related agent/CombatRL potion tests `107 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1514 passed`.
- Next candidate: rerun bounded validation from this commit and confirm that fallback no longer burns Elixir into HAND_SELECT. If that signal is quiet, inspect the remaining Slime Boss death and the Act 2 low-HP Snake Plant/Snecko decisions rather than broad potion policy tuning.

## Round 16 - 2026-06-05

- Preflight: git tracked tree clean at `3195618 Skip fallback Elixir auto-use`; CommunicationMod config still used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`.
- Baseline: the previous commit had just passed full pytest `1514 passed`; post-fix verification for this round passed full pytest `1515 passed`.
- Validation: controlled CommunicationMod fresh run from `3195618` was stopped after a focused second Elixir entry point was attributed. It completed 6 visible `.run` files after cutoff `1780619680` with no `victory=true`: deaths to Champ, Collector, Exordium Wildlife, The Guardian twice, and Slime Boss. No CommunicationMod Python traceback was active.
- Failure type: RL action-space Elixir auto-use bypassed the fallback guard. Decision trace for the Slime Boss run showed `CombatRLAgent` returning `PotionAction` for `ElixirPotion` on floor 16 turn 2, even though fallback and potion-guard scoring no longer auto-use Elixir. That means the learned action decoder path could still open HAND_SELECT and exhaust playable cards.
- Fix: `CombatRLAgent._should_override_low_value_potion()` now treats `exhaust_hand_select` / `ElixirPotion` as an always-replace unsafe blind potion action before boss/elite exceptions. `CombatRLAgent._score_potion_for_guard()` also uses the shared `potion_is_exhaust_hand_select()` helper.
- Regression: `test_rl_elixir_action_is_replaced_even_in_boss_combat` reproduced the Slime Boss RL-action path red, then passed after the guard.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; related agent/CombatRL potion tests `108 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1515 passed`.
- Next candidate: rerun bounded validation from this commit and confirm that no `PotionAction` uses `ElixirPotion` from either guard, fallback, or RL action-space paths. If Elixir stays quiet, inspect repeated Act 1 boss deaths and the Act 2 boss deaths separately.

## Round 17 - 2026-06-05

- Preflight: git tracked tree clean at `36787bb Block RL Elixir auto-use`; CommunicationMod config still used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`.
- Baseline: the previous commit had just passed full pytest `1515 passed`; post-fix verification for this round passed full pytest `1516 passed`.
- Validation: controlled CommunicationMod fresh run from `36787bb` was stopped after a focused Act 1 boss survival failure was attributed. It completed 4 visible `.run` files after cutoff `1780621037` with no `victory=true`: three Hexaghost deaths and one Guardian death. The trace scan after cutoff found `elixir_potion_actions_after_cutoff=0`, so the selected failure moved away from Elixir.
- Failure type: current-turn survival fallback ignored player end-turn Burn damage. In `1780621787.run`, Hexaghost floor 16 turn 8 had player HP 9, block 0, incoming 8, and an unplayed `Burn` in hand. RL initially ended turn, energy guard handed off to fallback, and fallback selected attacks (`Thunderclap`, `Bash`) instead of `Defend`. Incoming 8 plus Burn 2 killed from 9 HP; playing `Defend` would have survived the turn.
- Fix: `CombatRLAgent` survival fallback now counts unplayed hand Burn damage together with monster incoming damage before asking fallback for an action. When current HP would not survive incoming plus Burn after current block, energy guard/takeover chooses the highest-confidence playable block card instead of blindly accepting fallback's attack or first playable card.
- Regression: `test_energy_guard_counts_burn_damage_when_selecting_survival_fallback` reproduced the Hexaghost hand red by making fallback choose `Thunderclap`; after the fix, the replacement chooses `Defend`.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; related CombatRL/agent guard tests `109 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1516 passed`.
- Next candidate: rerun bounded validation from this commit and inspect whether Hexaghost deaths still contain same-turn Burn lethal lines. If this signal quiets, compare remaining Act 1 boss deaths by boss and turn phase before touching broader fallback/RL policy.

## Round 18 - 2026-06-05

- Preflight: git tracked tree clean at `a48a238 Count Burn damage in survival fallback`; CommunicationMod config still used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1516 passed`.
- Validation: controlled CommunicationMod fresh run from `a48a238` was stopped after the selected potion-action bypass was attributed. It completed 5 visible `.run` files after cutoff `1780621790` with no `victory=true`: Slime Boss, Guardian, Hexaghost, Writhing Mass at floor 46, and another Hexaghost. The batch was then stopped with `restart_sts_modded.ps1 -SkipLaunch` to preserve the current trace.
- Failure type: RL action-space Elixir guard only handled `PotionAction` objects carrying a bound `potion`. The v2 decoder emits index-only `PotionAction(use=True, potion_index=slot)`, and decision trace showed that shape using `ElixirPotion` on Hexaghost floor 16 turn 2 (`1780623209.run`), opening HAND_SELECT despite the previous Elixir guard.
- Fix: `CombatRLAgent` now resolves the potion for a `PotionAction` from either the bound `action.potion` or `action.potion_index` against the current game potions before deciding whether the action is an unsafe exhaust-hand-select potion. Combat action logging also includes the resolved potion name and index for index-only potion actions.
- Regression: `test_rl_elixir_index_action_is_replaced_even_in_boss_combat` reproduced the real v2 decoder path red with `PotionAction(True, potion_index=0)` and a current Elixir slot, then passed after the guard resolved the indexed potion.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; related CombatRL/agent/potion tests `116 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1517 passed`.
- Next candidate: rerun bounded validation from this commit and confirm trace contains no `ElixirPotion` `PotionAction` from bound or index-only RL paths. If Elixir stays quiet, inspect the remaining Act 1 boss deaths and the floor 46 Writhing Mass death separately.

## Round 19 - 2026-06-05

- Preflight: git tracked tree clean at `2152394 Resolve indexed RL Elixir actions`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1517 passed`.
- Validation: controlled CommunicationMod fresh run initially reached the main menu but did not enter a run because the Slay the Spire window was opened at `1920x1080` under Windows scaling and the launch UI was partially clipped. `D:\SteamLibrary\steamapps\common\SlayTheSpire\info.displayconfig` was backed up and temporarily set to `1280x720`, then the restarted bounded batch reached `Max games reached (20); exiting.`
- Outcome: no Ironclad `victory=true`. The batch wrote 20 visible `.run` files from `1780638179.run` through `1780640095.run`. Visible deaths included five Hexaghost, four The Guardian, two Slime Boss, three Act 2 hallway deaths, two Act 2 bosses, and early Act 1 hallway deaths. The decision trace search found zero `ElixirPotion`, `PotionAction`, or `potion_index` hits, so the indexed Elixir fix stayed quiet.
- Failure type: fallback-turn takeover could still end a turn when fallback also returned `EndTurnAction`. The decision trace showed boss states where `EndTurnAction` was emitted with energy and playable cards still available, including Guardian floor 16 turn 5 at 5 HP with 3 energy and playable attacks while Guardian intended `DEFEND`. This means the energy guard could correctly start takeover after RL ended early, but the takeover branch then trusted fallback's own EndTurn instead of applying the same non-end replacement policy.
- Fix: during active fallback-turn takeover, `CombatRLAgent` now treats a fallback `EndTurnAction` like a wasteful end-turn candidate: it asks `_get_non_end_turn_fallback()` for a playable replacement and only allows EndTurn when no replacement exists.
- Regression: `test_energy_guard_takeover_replaces_fallback_end_turn_with_playable_card` reproduced the Guardian-style active-takeover state red before the fix, asserting that playable attacks should be used instead of ending.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_combat_rl_guards.py` passed `60 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1518 passed`.
- Next candidate: rerun bounded validation from this commit and check whether Act 1 boss deaths still contain `EndTurnAction` with energy plus playable cards during active fallback takeover. If that signal quiets but Act 1 boss deaths remain dominant, inspect boss-specific high-pressure defense/offense selection rather than broader cleanup or training.

## Round 20 - 2026-06-05

- Preflight: git tracked tree clean at `6464e0b Guard fallback takeover end turns`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`; Slay the Spire display config remained at the temporary `1280x720` validation size from Round 19.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1518 passed`.
- Validation: controlled CommunicationMod fresh run from `6464e0b` was stopped after the selected trace failure was attributed. It completed 10 visible `.run` files after cutoff `1780640095` with no `victory=true`: six Act 1 boss deaths (`Slime Boss` twice and `The Guardian` four times), two `Collector` deaths, one `Automaton` death, and one `Snake Plant` death. No active Python traceback appeared in `communication_mod_errors.log`.
- Failure type: decision trace recorded raw RL potion candidates before the combat guards had validated or replaced them. Live trace after the cutoff showed post-fix `ElixirPotion` / `PotionAction` entries, including Elixir in Act 2 (`Collector` floor 33 and `Snecko` floor 25), even though `POTION_SAVE_GUARD` / Elixir replacement logic was supposed to prevent those actions from being sent. Root cause: `CombatRLAgent` called `_with_combat_action_context()` immediately after `rl_agent.get_next_action_in_game()`, so the trace wrote the raw RL action before `POTION_SAVE_GUARD`, Elixir replacement, energy guard, and other override branches selected the final action. Bound potion actions also leaked `potion_index=-1` into trace because slot resolution was deferred until `PotionAction.execute()`.
- Fix: decision trace now resolves bound potion objects to their current potion slot, including `get_real_potions()` fallback, before writing `action.potion_index`. `CombatRLAgent` now traces only the final action returned to CommunicationMod: raw RL candidates are logged for debugging but not written to `ai_decision_trace.jsonl` until they survive guard validation or are replaced.
- Regression: `test_decision_trace_resolves_bound_potion_index` reproduced the `potion_index=-1` trace leak red; `test_rl_elixir_replacement_trace_records_final_action_only` reproduced the raw Elixir trace pollution red by asserting that a guarded Elixir candidate writes only the final `PlayCardAction`.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; related trace/combat/potion/action-space tests passed `105 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1520 passed`.
- Next candidate: rerun bounded validation from this trace-final-action commit and confirm the post-fix trace no longer contains guarded raw `ElixirPotion` `PotionAction` entries or bound-potion `potion_index=-1` leaks. If that signal quiets, return to the dominant Act 1 boss deaths and inspect boss-specific high-pressure defense/offense selection with the now-trustworthy final-action trace.

## Round 21 - 2026-06-05

- Preflight: git tracked tree clean at `43d1bfe Trace final combat potion actions`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`; Slay the Spire display config remained at the temporary `1280x720` validation size from Round 19.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1520 passed`.
- Validation: controlled CommunicationMod fresh run from `43d1bfe` reached `Max games reached (20); exiting.` It completed 20 visible `.run` files after cutoff `1780644220`, with no `victory=true`. Visible deaths were dominated by Act 1 bosses: four `The Guardian`, four `Hexaghost`, and two `Slime Boss`; the remaining deaths included one Act 1 `Large Slime`, Act 2 hallway/boss fights (`Shelled Parasite and Fungi`, `Chosen and Byrds`, `3 Cultists`, `Centurion and Healer`, two `Collector`, `Automaton`, `Champ`), and one Act 3 `Sphere and 2 Shapes`.
- Trace check: final-action trace after the batch cutoff had no `action` entries for `ElixirPotion` and no final-action `potion_index=-1` leaks. Some trace rows still contained `ElixirPotion` in the current potion inventory snapshot, which is expected state context rather than a sent `PotionAction`.
- Failure type: direct RL combat actions could bypass `SURVIVAL_GUARD`. In `1780647773.run`, Slime Boss split left the player at 3/80 HP on floor 16 turn 7 against a `Spike Slime (L)` `ATTACK_DEBUFF` with live trace `move_damage=16`. The hand contained `Slimed`, `Slimed`, `Defend`, `Strike`, and `Uppercut`; RL selected `Uppercut`, then only one `Defend`, ending at 3 block and dying. The existing survival fallback only applied to EndTurn, potion, and active-takeover paths, so a direct RL `PlayCardAction` attack did not first ask whether a higher-block replacement was needed under lethal incoming.
- Fix: `CombatRLAgent` now checks direct RL `PlayCardAction` results with `_get_survival_action_replacement()` before other action-specific guards. When current incoming plus end-turn Burn would be lethal and a playable block card has higher survival block value than the RL-selected card, it replaces the RL action, sets fallback-turn takeover for the current combat turn, and logs the replacement under `SURVIVAL_GUARD`.
- Regression: `test_survival_guard_overrides_rl_attack_when_lethal_block_available` reproduced the Slime Boss split shape red by making RL choose `Uppercut` at 3 HP with `Defend` available; after the fix, the agent returns the `Defend` action and preserves same-turn takeover.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_combat_rl_guards.py` passed `62 passed`; related incoming-damage, decision-context, Ironclad combat, and fallback damage tests passed `779 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1521 passed`.
- Next candidate: rerun bounded validation from this survival-direct-action commit and check whether Act 1 boss deaths still show direct RL attacks or status consumption before available block under lethal incoming. If that signal quiets, inspect the next repeated boss-specific high-pressure pattern rather than training or broad policy cleanup.

## Round 22 - 2026-06-05

- Preflight: git tracked tree clean at `bcbc364 Guard lethal RL attacks with block`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`; Slay the Spire display config remained at the temporary `1280x720` validation size from Round 19.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1521 passed`.
- Validation: controlled CommunicationMod fresh run from `bcbc364` reached `Max games reached (20); exiting.` It completed 20 visible `.run` files after cutoff `1780647879`, with no `victory=true`. The repeated failure signal was six `The Guardian` deaths; the remaining deaths included three `Hexaghost`, Act 2 hallway/boss fights, two floor-48 Act 3 deaths (`Spire Growth`, `Transient`), and one low-HP event death after `Forgotten Altar`.
- Trace check: final-action trace after the batch cutoff had no `ElixirPotion` action entries and no final-action `potion_index=-1` leaks. `communication_mod_errors.log` had no active Python traceback.
- Failure type: Guardian high-pressure nonlethal attacks were treated too much like ordinary safe turns. The prior `SURVIVAL_GUARD` only replaced RL attacks when incoming plus end-turn damage would be immediately lethal. Round 22 Guardian traces repeatedly showed large 24/32-damage attack windows leading to a low-HP spiral, including turns that attacked or set up before adding little or no block. This selected risk is boss-specific high-pressure defense selection, not training or route tuning.
- Fix: `CombatRLAgent` now has a narrow Guardian pressure block guard. Against `The Guardian`, when incoming damage is at least `24`, current block does not cover it, the damage is nonlethal, and a playable block card beats the RL-selected card's block value, the agent replaces the RL `PlayCardAction` with the best block card and hands the rest of the turn to fallback takeover. The same pressure block helper is used inside active fallback-turn takeover and takeover potion replacement paths. Weak-applying attacks such as `Clothesline`, `Uppercut`, and `Shockwave` are not replaced by this guard.
- Regression: `test_guardian_pressure_guard_overrides_rl_attack_when_big_nonlethal_block_available` reproduced the focused Guardian shape red: RL chose `Strike` into a 32-damage Guardian attack at 80 HP while `Defend` was playable. After the fix, the agent returns `Defend` and preserves same-turn takeover.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_combat_rl_guards.py` passed `63 passed`; related incoming-damage, decision-context, Ironclad combat, damage fallback, and potion action tests passed `780 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1522 passed`.
- Next candidate: rerun bounded validation from this Guardian pressure commit and check whether Guardian deaths still show the same nonlethal high-pressure attack-before-block pattern. If that signal quiets, inspect the low-HP `Forgotten Altar` event death and the floor-48 Act 3 combat deaths separately rather than broad cleanup or training.

## Round 23 - 2026-06-05

- Preflight: git tracked tree clean at `8a0bd7c Guard Guardian pressure attacks with block`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`; Slay the Spire display config remained at the temporary `1280x720` validation size from Round 19.
- Baseline: the previous commit had just passed full pytest `1522 passed`; post-fix verification for this round passed full pytest `1523 passed`.
- Validation: controlled CommunicationMod fresh run from `8a0bd7c` reached `Max games reached (20); exiting.` It completed 20 visible `.run` files after cutoff `1780652239`, with no `victory=true`. Visible deaths were dominated by Act 1 bosses again: seven `The Guardian` and five `Hexaghost`, with the rest spread across Act 2 hallway/boss fights and later `3 Cultists`.
- Trace check: final-action trace after the batch cutoff had no final-action `ElixirPotion` entries and no final-action `potion_index=-1` leaks. `communication_mod_errors.log` had no active Python traceback. The new `GUARDIAN_PRESSURE_GUARD` did fire in Guardian fights that were later survived, so the selected failure moved to a different Guardian mechanism rather than reverting the pressure hypothesis.
- Failure type: Guardian `Sharp Hide` reflection could convert a survivable incoming turn into a lethal one after the AI spent existing block by attacking in defensive mode. In the selected Guardian traces, the player could survive the visible incoming damage before attacking, but attacks such as `Strike`, `Thunderclap`, or `Headbutt` consumed block through `Sharp Hide` before end turn damage landed. Direct RL `PlayCardAction` and fallback takeover screening did not account for this boss-specific reflected damage.
- Fix: `CombatRLAgent` now detects alive Guardian `SharpHide` / `Thorns` powers before accepting an attack action. If incoming plus end-turn Burn is survivable with current block, but adding the Guardian's Sharp Hide damage would make the turn lethal, the guard replaces the attack with the best playable block card when available, otherwise it ends the turn to avoid the lethal reflected damage. The same guard also screens active fallback-turn takeover actions before Guardian pressure handling.
- Regression: `test_guardian_sharp_hide_guard_blocks_attack_that_would_make_incoming_lethal` reproduced the selected shape red: RL chose `Thunderclap` at 9 HP with 2 current block against Guardian attacking for 9 and `SharpHide(3)`, while `Defend` was playable. After the fix, the agent returns `Defend` and preserves same-turn takeover.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_combat_rl_guards.py` passed `64 passed`; related incoming-damage, decision-context, Ironclad combat, damage fallback, and potion action tests passed `780 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1523 passed`.
- Next candidate: rerun bounded validation from this Guardian Sharp Hide commit and check whether Guardian deaths decrease or move away from reflected-damage block erosion. If Guardian remains dominant, inspect Guardian mode-shift/trigger-damage planning before broadening to Hexaghost or Act 2 `3 Cultists`.

## Round 24 - 2026-06-05

- Preflight: git tracked tree clean at `2020d68 Guard Guardian sharp hide attacks`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`; Slay the Spire display config remained at the temporary `1280x720` validation size from Round 19.
- Baseline: full pytest with disabled cache provider and repo-local basetemp passed `1523 passed`; post-fix verification for this round passed full pytest `1524 passed`.
- Validation: controlled CommunicationMod fresh run from `2020d68` reached `Max games reached (20); exiting.` It completed 20 visible `.run` files after cutoff `1780655858`, with no `victory=true`. Visible deaths were five `Hexaghost`, four `The Guardian`, two `Slime Boss`, two `Centurion and Healer`, two `Collector`, and one each to `Cultist and Chosen`, `Spheric Guardian`, `Blue Slaver`, `Chosen and Byrds`, and `Sentry and Sphere`.
- Trace check: final-action trace after the batch cutoff had 6482 rows, 69 `PotionAction` rows, zero final-action `ElixirPotion` rows, and zero final-action negative `potion_index` rows. `communication_mod_errors.log` had no active Python traceback.
- Failure type: Guardian defensive-mode Sharp Hide inference was still too narrow. Round 24 Guardian deaths no longer showed a simple missing power-based regression, but live traces still showed repeated attacks into Guardian defensive attack turns where player HP/block eroded by reflected damage. The new `GUARDIAN_SHARP_HIDE_GUARD` did not appear in the live log, so the selected risk was that the guard only detected explicit `SharpHide` / `Thorns` powers and missed CommunicationMod states where Guardian is in defensive attack moves by `move_id`.
- Fix: `_guardian_sharp_hide_damage()` now keeps explicit power detection but also infers Sharp Hide for alive Guardian defensive attack moves `move_id` 5 (`Roll Attack`) and 6 (`Twin Slam`). The inferred amount is 3 at normal ascensions and 4 at ascension 19+, matching the enhanced monster data.
- Regression: `test_guardian_sharp_hide_guard_infers_roll_attack_reflection_without_power` reproduced the live-shaped blind spot red: Guardian had no powers but current `move_id=5`, incoming 9, player HP 12, and RL chose `Thunderclap`; without inferred Sharp Hide the attack survived the guard. After the fix, the agent chooses `Defend` and preserves same-turn takeover.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_combat_rl_guards.py` passed `65 passed`; related incoming-damage, decision-context, Ironclad combat, damage fallback, and potion action tests passed `780 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1524 passed`.
- Next candidate: rerun bounded validation from this Guardian defensive-mode Sharp Hide commit. If Guardian deaths remain, inspect mode-shift timing around moves 4/5/6 and whether trace should include `move_id`/powers for attribution before broadening to Hexaghost Burn/status cleanup or Act 2 hallway defense.

## Round 25 - 2026-06-05

- Preflight: git tracked tree clean at `361ad37 Infer Guardian sharp hide from defensive moves`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`; Slay the Spire display config remained at the temporary `1280x720` validation size from Round 19.
- Validation: controlled CommunicationMod fresh run from `361ad37` reached `Max games reached (20); exiting.` It completed 20 visible `.run` files after cutoff `1780659334`, with no `victory=true`. Visible deaths were `Slime Boss` six times, `The Guardian` four times, `Hexaghost` three times, `3 Cultists` twice, and one each to `Sentry and Sphere`, `2 Fungi Beasts`, `Gremlin Leader`, `Automaton`, and `Gremlin Gang`.
- Trace check: final-action trace after the batch cutoff had 53 `PotionAction` rows, zero final-action `ElixirPotion` rows, and zero final-action negative `potion_index` rows when filtered by `unix_time`. The new Guardian Sharp Hide inference fired in live logs, so the selected failure moved away from Elixir and away from the exact Round 24 Guardian blind spot.
- Failure type: Slime Boss split-phase survival targeting could leave a killable attacking slime alive on a lethal turn. In `1780661235.run`, floor 16 turn 9 reached 17 HP, 3 block, and 2 energy after playing `Defend`; the split monsters were attacking for 8, 8, and 11. The trace then played `Carnage` into the 16-HP Spike Slime, leaving it at 1 HP, and ended with 27 incoming against 3 block. The same 15-damage `Carnage` into the 10-HP attacking Spike Slime would have removed 8 incoming and converted lethal damage into a one-HP survival turn.
- Fix: `CombatRLAgent` now checks direct RL `PlayCardAction` results with a narrow Slime Boss split survival attack guard before the generic survival block guard. On floor 16 boss split states, when current incoming after block is lethal and the selected attack can kill an attacking slime whose removed incoming makes the turn survivable, the guard retargets that same card to the killable attacker and hands the turn to fallback takeover. The helper is limited to targetable monsters and uses the same incoming-damage shape as the existing combat guards.
- Regression: `test_slime_split_survival_guard_retargets_killable_attacker` reproduced the focused post-Defend state red: RL chose `Carnage` into the 16-HP Spike while a 10-HP attacking Spike was killable. Before the fix, the generic survival block guard returned `Defend`; after the fix, the agent preserves `Carnage` and retargets it to monster slot 0.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_combat_rl_guards.py` passed `66 passed`; related incoming-damage, decision-context, Ironclad combat, damage fallback, and potion action tests passed `780 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1525 passed`; `git diff --check` reported no whitespace errors beyond line-ending warnings.
- Next candidate: rerun bounded validation from this Slime split survival commit and check whether Slime Boss deaths decrease or show `SLIME_SPLIT_SURVIVAL_GUARD` in live logs. If Slime Boss quiets but Act 1 bosses remain dominant, inspect Hexaghost late-turn Burn/status cleanup and Guardian mode-shift timing separately rather than training or broad policy cleanup.

## Round 26 - 2026-06-05

- Preflight: git tracked tree clean at `0563f76 Retarget Slime split survival attacks`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`; pytest baseline for this commit passed `1525 passed`.
- Validation: controlled CommunicationMod fresh run from `0563f76` reached `Max games reached (20); exiting.` It completed 20 visible `.run` files after cutoff `1780662235`, with no `victory=true`. Visible deaths were five `Hexaghost`, three `Slime Boss`, two `The Guardian`, two `Collector`, two `Chosen and Byrds`, and one each to `3 Cultists`, `Snake Plant`, `Automaton`, `Champ`, `3 Byrds`, and `Centurion and Healer`.
- Trace check: final-action trace after the batch cutoff had 89 `PotionAction` rows, zero final-action `ElixirPotion` rows, and zero final-action negative `potion_index` rows. `communication_mod_errors.log` had no active Python traceback. No `SLIME_SPLIT_SURVIVAL_GUARD` line appeared in the live log, so repeated Slime deaths need separate post-split attribution rather than assuming the Round 25 shape recurred.
- Failure type: end-turn Burn damage was modeled as if current block could absorb it. The strongest live evidence was `1780667052.run`: on Hexaghost floor 16 turn 12, the player had 8 HP and 16 block against 8 monster incoming, but ended with four `Burn+` in hand and died anyway. The previous survival/Sharp Hide formulas used `max(0, incoming + burn_damage - current_block)`, which can falsely treat high current block as covering Burn. The focused regression used the same shape at 12 HP to isolate the false-safe case.
- Fix: `CombatRLAgent` now calculates end-turn damage as `max(0, blockable_damage - current_block) + burn_damage`. Survival block selection and Guardian Sharp Hide screening use this helper, with monster incoming and Sharp Hide treated as blockable damage while Burn remains independent end-turn damage.
- Regression: `test_survival_guard_treats_burn_damage_as_unblocked_by_current_block` reproduced the false-safe formula red: with 12 HP, 16 block, 8 incoming, four `Burn+`, and RL choosing `Strike`, the old guard accepted the attack. After the fix, the survival guard replaces it with `Defend` and hands the turn to fallback takeover.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_combat_rl_guards.py` passed `67 passed`; related incoming-damage, decision-context, Ironclad combat, damage fallback, and potion action tests passed `780 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1526 passed`.
- Next candidate: rerun bounded validation from this Burn-damage mechanics commit and check whether Hexaghost deaths still cluster around low-HP Burn hands. If they do, the next focused risk is not more block math but explicit Burn/status mitigation or earlier Hexaghost HP preservation; if Hexaghost quiets, compare the remaining Slime Boss and Act 2 `Chosen and Byrds` failures separately.

## Round 27 - 2026-06-05

- Preflight: git tracked tree clean at `b0b1ba1 Treat Burn damage as unblockable in guards`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 20 --phase conservative --restart-guidance --truncate-log-after-backup`; pytest baseline passed `1526 passed`.
- Validation: controlled CommunicationMod fresh run from `b0b1ba1` reached `Max games reached (20); exiting.` It completed 20 visible `.run` files after cutoff `1780667174`, with no `victory=true`. Visible deaths were four `The Guardian`, four `Hexaghost`, two `Automaton`, two `3 Cultists`, two `Shelled Parasite and Fungi`, and one each to `Slime Boss`, `Spheric Guardian`, `2 Fungi Beasts`, `Collector`, `Champ`, and `Sentry and Sphere`.
- Trace check: final-action trace after the batch cutoff had 78 `PotionAction` rows, zero `ElixirPotion` actions, and zero negative potion indexes. `communication_mod_errors.log` had no active Python traceback. New Burn damage logging fired in Hexaghost and hallway combats, so the Round 26 formula was active.
- Failure type: Guardian defensive-mode Sharp Hide inference still missed live states where CommunicationMod omitted both `SharpHide` power and `move_id` but exposed `Intent.ATTACK_BUFF`. The strongest trace was `1780669705.run`: on Guardian floor 16 turn 5, the player had 13 HP, 5 block, and Guardian incoming `8x2`. Ending after `Defend` would survive at 2 HP, but the agent then attacked with `Twin Strike` and `Strike`; Sharp Hide consumed the saved block/HP and the turn became lethal. The existing guard only inferred Sharp Hide from explicit powers or defensive `move_id` 5/6, so it accepted the attack.
- Fix: `_guardian_sharp_hide_damage()` now keeps explicit power and `move_id` inference, and additionally infers Guardian Sharp Hide when the live Guardian intent serializes as `ATTACK_BUFF` / `Intent.ATTACK_BUFF`. This makes direct RL actions and fallback takeover screening end the turn or prefer block when reflected damage would convert a survivable incoming turn into a lethal one.
- Regression: `test_guardian_sharp_hide_guard_infers_attack_buff_reflection_without_power` reproduced the selected live shape red: Guardian had no powers and no move id, intent `ATTACK_BUFF`, incoming `8x2`, player HP 13 with 5 block, and RL chose `Twin Strike`. Before the fix, the agent returned the attack; after the fix, it returns `EndTurnAction`.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_combat_rl_guards.py` passed `68 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1527 passed`.
- Next candidate: rerun bounded validation from this Guardian `ATTACK_BUFF` Sharp Hide inference commit and check whether Guardian deaths move away from reflected-damage block erosion. If Guardian quiets, compare remaining Hexaghost low-HP Burn/status cleanup against Slime Boss post-split survival before touching training or broader policy.

## Round 28 - 2026-06-06

- Preflight: git tracked tree was at `15208ac Reduce sim divergence false positives`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance` and clean decision/sim-divergence trace paths. The latest `sim_divergence_trace_clean.jsonl` was written at `2026-06-06 01:02:52`, before `15208ac` was committed at `01:13:29`, so it was useful live evidence but not a post-`15208ac` validation. Process checks found no active `python.exe` batch or Slay the Spire/ModTheSpire process; a fresh controlled restart was not run because it required explicit approval after the reviewer flagged the risk of disrupting a running game session.
- Validation: latest AI-marked Ironclad runs around the clean batch still had no `victory=true`; examples included `1780678972.run` ending at floor 14 to `Gremlin Gang`, `1780678915.run` ending at floor 33 to `Automaton`, and several Act 1 boss deaths. `communication_mod_errors.log` had batch output and no active traceback in the inspected tail. The stale clean trace still showed high-volume oracle mismatches, but previously fixed `Headbutt`/`Armaments`/end-turn energy rows must be rechecked with a new post-fix batch before being counted.
- Failure type: `Hemokinesis` remained a high-confidence sim-oracle miss in the current code and the existing live trace. Live rows showed `Hemokinesis` / `Hemokinesis+` with `action.card.damage=0`, while the actual state dealt the card damage and reduced player HP by 2. At least one row had an explicit `target_index=0`, separating this from the separate `card_index=-1` / target inference boundary. Current `sim_divergence.py` had no base damage entry for `Hemokinesis` and no self-damage modeling, so expected monster HP and player HP stayed too high.
- Fix: `sim_divergence.py` now models `Hemokinesis` as 15 base attack damage, uses the existing +5 upgrade bonus helper for `Hemokinesis+`, and applies fixed 2 HP self-damage for that card effect. This is diagnostic oracle quality work only; it is not evidence that the RL policy made a bad gameplay decision.
- Regression: `test_hemokinesis_zero_live_damage_matches_live_effect` reproduced the live-shaped false divergence red with `damage=0`, explicit target slot, monster HP dropping by 15, and player HP dropping by 2. After the fix, the expectation matches the live effect and no divergence trace is written.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_sim_divergence.py tests/test_training_batch_runner.py` passed `17 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1539 passed`. `git diff --check` reported no whitespace errors beyond line-ending warnings.
- Next candidate: run a new post-`Hemokinesis` clean validation batch once restart is explicitly approved, then choose from the new trace distribution. Likely candidates to re-evaluate are `Heavy Blade` strength-scaled damage, `Blood for Blood` dynamic damage/cost, `Offering` self-damage/energy, and `Mind Blast` deck-size-driven damage; do not hardcode `Mind Blast` without confirming the snapshot has enough deck state.

## Round 29 - 2026-06-06

- Preflight: git tracked tree was clean at `71cb55e Model Hemokinesis in sim divergence`; CommunicationMod config still used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance` and clean trace paths. No active `python.exe` or `java.exe` batch/game process was visible from the safe process checks. The latest clean sim-divergence trace still had last write time `2026-06-06 01:02:52`, so there was no post-`71cb55e` live validation yet.
- Validation: the latest AI-marked Ironclad records remained the same clean-batch set and still had no `victory=true`; recent examples included `1780678972.run` ending at floor 14 to `Gremlin Gang`, `1780678915.run` ending at floor 33 to `Automaton`, and multiple Act 1 boss deaths. The inspected error-log tail showed prior batch output and no active traceback.
- Failure type: `Clothesline` was a high-confidence static attack damage oracle miss in the existing live trace and current code. A representative explicit-target row had `card_index=3`, `target_index=0`, `action.card.damage=0`, before monster HP `154`, expected HP `154`, and actual HP `142`. Current `sim_divergence.py` had no `BASE_ATTACK_DAMAGE` entry for `Clothesline`, while the upgrade helper already knew `Clothesline` gets +2 damage.
- Fix: `sim_divergence.py` now models `Clothesline` as 12 base attack damage. Upgraded `Clothesline+` uses the existing +2 damage bonus path, so no separate special case was added. Weak application is outside the current snapshot diff surface and was not modeled in this round.
- Regression: `test_clothesline_zero_live_damage_uses_base_damage` reproduced the selected live shape red with `damage=0`, explicit target slot, monster HP dropping by 12, and no player-state changes.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_sim_divergence.py tests/test_training_batch_runner.py` passed `18 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1540 passed`.
- Next candidate: the next goal wording should likely become "minimal sufficient fix" rather than broad "systematic fix": each round still starts from live evidence, but static card-value oracle misses can be fixed across the proven same class, while dynamic cards such as `Heavy Blade`, `Blood for Blood`, `Offering`, and `Mind Blast` need snapshot sufficiency checks before broad modeling.

## Round 30 - 2026-06-06

- Preflight: git tracked tree was clean at `a8d13e0 Model Clothesline in sim divergence`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance` and clean decision/sim-divergence trace paths.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced AI-marked Ironclad run files `1780681667.run`, `1780681790.run`, `1780681878.run`, `1780681976.run`, and `1780682070.run`; all had `victory=false`, with three `The Guardian`, one `Hexaghost`, and one Act 2 `Shell Parasite` death. `communication_mod_errors.log` had batch output and no active traceback.
- Trace distribution: post-batch sim-divergence rows after cutoff `1780680000` were `315 monster_state_mismatch`, `66 player_state_mismatch`, `13 guardian_sharp_hide_reflection`, and `5 end_turn_burn_damage`. The top buckets were `PlayCardAction|monster_state_mismatch` (`199`), `EndTurnAction|monster_state_mismatch` (`107`), and `PlayCardAction|player_state_mismatch` (`54`). `Iron Wave` appeared in 13 post-batch rows with `action.card.block=5`; representative rows showed expected monster HP already matched the 5 damage, while expected player block stayed unchanged and actual player block increased by 5.
- Failure type: `Iron Wave` exposed a same-class oracle miss for attack cards that also grant block. `_expected_after_action()` only applied `_card_block()` in the non-attack branch, so `CardType.ATTACK` hybrid block effects were skipped even though `BASE_SKILL_BLOCK` already knew `Iron Wave` blocks for 5.
- Fix: PlayCard expectation now applies card block after the attack/self-damage branch for all played cards. Normal attacks still add zero block, skills still add their block, and hybrid attacks such as `Iron Wave` now predict both damage and block.
- Regression: `test_iron_wave_attack_block_matches_live_effect` reproduced the live row red: playing `Iron Wave` reduced Acid Slime HP by 5, spent one energy, and added 5 player block, but the old oracle still wrote a `player.block` divergence.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_sim_divergence.py` passed `11 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1541 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Iron Wave` block rows quiet, choose the next class from the new distribution. `EndTurnAction` monster intent/block churn looks like likely trace-lifecycle noise, while dynamic-damage cards such as `Heavy Blade` and `Mind Blast` still need snapshot-sufficiency checks before modeling.

## Round 31 - 2026-06-06

- Preflight: git tracked tree was clean at `724a42d Model hybrid attack block in sim divergence`; CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance` and clean decision/sim-divergence trace paths. The prior clean trace cutoff was `1780683000`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced AI-marked Ironclad run files `1780683456.run`, `1780683581.run`, `1780683660.run`, `1780683837.run`, and `1780683902.run`; all had `victory=false`, with deaths to `Hexaghost`, `Slavers`, `The Guardian`, `Automaton`, and `The Guardian`. `communication_mod_errors.log` had batch output and no active traceback.
- Trace distribution: post-cutoff sim-divergence rows were `410` total: `352 monster_state_mismatch`, `24 player_state_mismatch`, `19 slime_split_state_mismatch`, `8 guardian_sharp_hide_reflection`, and `7 end_turn_burn_damage`. The top PlayCard card names included `Cleave` (`34`), `Whirlwind` (`20`), and `Thunderclap` (`18`). `Iron Wave` no longer appeared as a dominant block-only false-positive class.
- Failure type: static all-enemy attacks were a high-confidence oracle miss. `Cleave` rows had explicit `action.card.damage=8` and `target_index=null`, while actual monster HP dropped across all live monsters and expected HP stayed unchanged. `Thunderclap` rows had live `damage=0`, but actual state applied 4 damage through block or HP to all monsters. `Whirlwind` showed similar surface symptoms but was excluded because its X-cost damage and energy semantics need a separate snapshot-sufficiency check.
- Fix: `sim_divergence.py` now models static all-enemy attacks for `Cleave` and `Thunderclap`. It applies expected attack damage to each live, non-half-dead monster; `Thunderclap` also has a 4 base damage entry and uses the existing +3 upgrade bonus path when upgraded. Vulnerable application is outside the current snapshot diff surface and was not modeled in this round.
- Regression: `test_cleave_hits_all_live_monsters_without_target` reproduced the explicit-damage all-enemy miss red; `test_thunderclap_zero_live_damage_hits_all_monsters` reproduced the zero-live-damage base AoE miss red.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `13 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1543 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If static AoE rows quiet, choose the next class from the new distribution. Likely candidates are `Whirlwind` X-cost AoE damage/energy, `Blood for Blood` dynamic damage/cost, and remaining EndTurnAction monster intent/block lifecycle noise.

## Round 32 - 2026-06-06

- Preflight: git tracked files were clean at `fa9efdc Model static AOE attacks in sim divergence`; only a repo-local `.tmp/` pytest basetemp ACL residue remained untracked. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance` and clean decision/sim-divergence trace paths. The prior clean trace cutoff was `1780684900`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced AI-marked Ironclad run files `1780685115.run`, `1780685225.run`, `1780685302.run`, `1780685452.run`, and `1780685545.run`; all had `victory=false`, with deaths to `Champ`, `Hexaghost`, `Slime Boss`, `Cultist and Chosen`, and `Hexaghost`. `communication_mod_errors.log` had batch output and no active traceback.
- Trace distribution: post-cutoff sim-divergence rows were `513` total: `429 monster_state_mismatch`, `61 player_state_mismatch`, `12 slime_split_state_mismatch`, `8 end_turn_burn_damage`, and `3 guardian_sharp_hide_reflection`. `PlayCardAction|player_state_mismatch` accounted for `58` rows. The top clean block false-positive class was `Finesse`: live rows had `action.card.block=0`, expected player block unchanged, and actual block commonly increased by 2.
- Failure type: `Finesse` exposed another static card-value oracle miss. Current snapshots do not include player powers, so a few +1 block rows are likely Frail-modified and cannot be fully resolved this round, but normal Finesse rows have enough evidence to model the base 2 block and upgraded 4 block.
- Fix: `sim_divergence.py` now models `Finesse` as 2 base block. Upgraded `Finesse+` uses the existing +2 block upgrade helper. Draw behavior is outside the current snapshot diff surface and was not modeled.
- Regression: `test_finesse_zero_live_block_uses_base_block` reproduced the live `block=0` / actual +2 row red; `test_finesse_plus_zero_live_block_uses_upgrade_block` covers the upgraded +4 path through the shared upgrade helper.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `15 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1545 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If normal `Finesse` rows quiet, choose the next class from the new distribution. Likely candidates remain `Offering` self-damage/energy, `Heavy Blade` strength-scaled damage if snapshot evidence is sufficient, and EndTurnAction monster intent/block lifecycle noise.

## Round 33 - 2026-06-06

- Preflight: git tracked files were clean at `49b0ab4 Model Finesse block in sim divergence`; only the known repo-local `.tmp/` pytest basetemp ACL residue remained untracked. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance` and clean decision/sim-divergence trace paths. The prior AI marker cutoff was `1780685548`, and the clean sim-divergence trace last write time was `2026-06-06 02:52`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced Ironclad run files `1780686780.run`, `1780686859.run`, `1780686934.run`, `1780687018.run`, and `1780687117.run`; all had `victory=false`. The best run reached floor 46 and died to `2 Orb Walkers`; the remaining four died at floor 16 to `The Guardian`, `The Guardian`, `Hexaghost`, and `Hexaghost`. `communication_mod_errors.log` had batch output and no active traceback in the inspected tail.
- Trace distribution: post-cutoff sim-divergence rows were `600` total: `506 monster_state_mismatch`, `72 player_state_mismatch`, `15 guardian_sharp_hide_reflection`, and `7 end_turn_burn_damage`. Top action-card buckets included `Strike_R` (`116`), `Anger` (`95`), `Defend_R` (`30`), `Whirlwind` (`25`), `Thunderclap` (`24`), and `Clothesline` (`23`). Delta analysis showed many base attacks doing 1.5x expected damage, e.g. `Strike` 6 expected vs 9 actual and `Anger+` 8 expected vs 12 actual, while block cards often gained 25% less block, e.g. `Defend+` 8 expected vs 6 actual and `Shrug It Off` 8 expected vs 6 actual.
- Failure type: the sim-divergence snapshot did not record player or monster powers, so the oracle could not apply common live combat modifiers. This made target `Vulnerable` look like attack over-damage and player `Frail` look like block under-gain. This is a diagnostic oracle quality issue; it is not evidence that the RL policy selected wrong cards.
- Fix: `sim_divergence.py` now includes summarized powers in player and monster snapshots, applies target `Vulnerable` to expected attack damage with STS integer flooring, and applies player `Frail` to expected block with STS integer flooring. The fix deliberately does not add Strength/Weak/Dexterity yet because current live card damage/block fields may already include some source-side modifiers; those need fresh post-fix evidence before modeling.
- Regression: `test_vulnerable_target_damage_does_not_create_false_monster_diff` reproduced the live `Strike` 6-to-9 target-Vulnerable shape red. `test_frail_player_block_does_not_create_false_player_diff` reproduced the live `Defend+` 8-to-6 player-Frail shape red. Both tests failed before the fix and passed after the oracle applied the relevant power modifier.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `17 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1547 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Vulnerable/Frail rows quiet, choose the next fresh class by distribution. Likely candidates are `Offering` self-damage/energy, `Whirlwind` X-cost AoE/energy, `Blood for Blood` dynamic damage/cost, and EndTurnAction monster intent/block lifecycle noise; do not model Strength/Weak/Dexterity until fresh powers-in-snapshot rows show whether CommunicationMod card fields already include them.

## Round 34 - 2026-06-06

- Preflight: git tracked files were clean at `327676c Apply power modifiers in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance` and clean decision/sim-divergence trace paths. The prior AI marker cutoff was `1780687120`, and the clean sim-divergence trace last write time was `2026-06-06 03:18:37`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced Ironclad run files `1780688293.run`, `1780688474.run`, `1780688616.run`, `1780688801.run`, and `1780688873.run`; all had `victory=false`. The best runs reached floor 33 and died to `Champ` and `Automaton`; the others died at floor 16 to `Slime Boss`, ended at floor 20 with `killed_by=None`, and died at floor 16 to `Hexaghost`. `communication_mod_errors.log` had batch output and no active traceback in the inspected tail.
- Trace distribution: post-cutoff sim-divergence rows were `520` total: `406 monster_state_mismatch`, `63 player_state_mismatch`, `42 slime_split_state_mismatch`, and `9 guardian_sharp_hide_reflection`. Top action-card buckets included `Strike_R` (`111`), `Uppercut` (`29`), `Havoc` (`29`), `Anger` (`27`), `Bloodletting` (`14`), `Twin Strike` (`13`), `Bash` (`13`), `Thunderclap` (`13`), `Defend_R` (`13`), and `Blood for Blood` (`13`). The cleanest player-state signature was `Bloodletting`: `12` rows with `diffs=['player.current_hp','player.energy']`.
- Failure type: HP-for-energy skills were not modeled by the sim-divergence oracle. The representative live row had `Bloodletting` with `cost=0`, before player HP `74`, energy `0`, expected HP `74`, energy `0`, and actual HP `71`, energy `2`. Existing code only spent card cost and only applied self-damage inside the attack-card branch, so non-attack HP/energy card effects were invisible to the oracle.
- Fix: `sim_divergence.py` now models HP-for-energy card effects for `Bloodletting` and `Offering`. It applies fixed self-damage to all card types, adds fixed energy gain after cost spending, and uses `card_upgrade_count()` so `Bloodletting+` gains 3 energy while base `Bloodletting` gains 2. Offering draw/exhaust remains outside the current snapshot diff surface and was not modeled.
- Regression: `test_bloodletting_hp_energy_effect_does_not_create_false_player_diff` reproduced the fresh live `Bloodletting` row red. `test_upgraded_bloodletting_gains_three_energy` covers the upgraded path from the existing simulator reference. `test_offering_hp_energy_effect_does_not_create_false_player_diff` covers the same HP-for-energy class for `Offering`, which current snapshots can verify on HP and energy without modeling draw.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `20 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1550 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Bloodletting`/`Offering` rows quiet, choose from the new distribution. Likely candidates are `Whirlwind` X-cost AoE/energy if energy and current card damage are sufficient, `Blood for Blood` dynamic damage/cost if cost/damage state is sufficient, `Mind Blast` if deck-size state is available, and EndTurnAction monster intent/block lifecycle noise if it remains dominant.

## Round 35 - 2026-06-06

- Preflight: git tracked files were clean at `688f4f8 Model HP-for-energy cards in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance --truncate-log-after-backup` and clean decision/sim-divergence trace paths. The prior AI marker cutoff was `1780688876`, and the clean sim-divergence trace last write time was `2026-06-06 03:47:52`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced Ironclad run files `1780689759.run`, `1780689848.run`, `1780690033.run`, `1780690132.run`, and `1780690294.run`; all had `victory=false`. Deaths were `Slime Boss`, `The Guardian`, `Collector`, `Hexaghost`, and `Champ`, with the two best runs reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `442` total: `370 monster_state_mismatch`, `32 slime_split_state_mismatch`, `28 player_state_mismatch`, `6 guardian_sharp_hide_reflection`, and `6 end_turn_burn_damage`. Top action-card buckets included `Strike_R` (`86`), `Anger` (`70`), `Havoc` (`45`), `Uppercut` (`23`), `Bash` (`20`), `Heavy Blade` (`14`), `Dramatic Entrance` (`6`), `Whirlwind` (`6`), `Iron Wave` (`6`), and `Twin Strike` (`6`).
- Failure type: `Heavy Blade` was a high-confidence dynamic attack oracle miss now that snapshots include player powers. Live rows had `action.card.damage=0`; base rows dealt 14 damage, target-Vulnerable rows dealt 21, and player-Strength rows matched the Heavy Blade formula `14 + Strength * multiplier` before target-side modifiers. This is diagnostic oracle quality work only, not evidence that the RL policy selected the wrong card.
- Fix: `sim_divergence.py` now models `Heavy Blade` as 14 base damage and, only when live `card.damage` is zero, applies the existing Heavy Blade Strength multiplier helper to the player Strength power in the snapshot. Upgraded Heavy Blade uses the shared multiplier helper; generic Strength/Weak/Dexterity modeling remains intentionally out of scope because live card fields can already include source-side modifiers for other cards.
- Regression: `test_heavy_blade_zero_live_damage_uses_base_damage` reproduced the floor-16 boss-row shape red with `damage=0` and 14 actual damage. `test_heavy_blade_zero_live_damage_uses_strength_scaling` reproduced the player-Strength shape red with Strength 3 producing 23 damage.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `22 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1552 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Heavy Blade rows quiet, choose from the fresh distribution. Likely candidates are `Havoc` if draw-pile/exhaust snapshot sufficiency can be proven, `Dramatic Entrance` as static all-enemy colorless damage, `Whirlwind` X-cost AoE/energy, and EndTurnAction monster intent/block lifecycle noise.

## Round 36 - 2026-06-06

- Preflight: git tracked files were clean at `0946284 Model Heavy Blade in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance --truncate-log-after-backup` and clean decision/sim-divergence trace paths. The prior AI marker cutoff was `1780690297`, and the clean sim-divergence trace length was `10322389` bytes before the batch.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced Ironclad run files `1780691249.run`, `1780691386.run`, `1780691521.run`, `1780691630.run`, and `1780691712.run`; all had `victory=false`. Deaths were `The Guardian`, `Collector`, `3 Cultists`, `Centurion and Healer`, and `Slime Boss`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `463` total: `384 monster_state_mismatch`, `38 player_state_mismatch`, `31 slime_split_state_mismatch`, `9 guardian_sharp_hide_reflection`, and `1 end_turn_burn_damage`. Top action-card buckets included `Strike_R` (`140`), `Anger` (`21`), `Bash` (`16`), `Second Wind` (`15`), `Reckless Charge` (`15`), `Defend_R` (`15`), `Pommel Strike` (`11`), `Whirlwind` (`11`), `Clothesline` (`8`), and `Sever Soul` (`8`). Heavy Blade no longer appeared as a high-frequency bucket.
- Failure type: `Second Wind` was the cleanest player-state oracle miss. Live rows had `action.card.block=0`, expected player block unchanged, and actual block increased by 5 per non-Attack card exhausted, or 7 per card for `Second Wind+`. The existing combat snapshot already included hand card types, so this dynamic block amount was computable without new trace fields.
- Fix: `sim_divergence.py` now models `Second Wind` block from `before.hand`, excluding the played card and counting non-Attack cards. It applies the upgraded 7-block-per-card value through `card_upgrade_count()` and keeps Frail handling through the existing `_modified_block()` path. The round also constrained card-id aliases in `_known_card_name()` so small effect tables do not treat `Strike_R` as a match for unrelated tables.
- Regression: `test_second_wind_zero_live_block_counts_non_attack_cards` reproduced the base live shape with Skill/Status cards counting and Attack cards excluded. `test_second_wind_plus_uses_upgraded_block_per_non_attack` reproduced the upgraded live shape with Skill and Power cards counting for 14 total block.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; a targeted old-regression subset passed `7 passed`; `tests/test_sim_divergence.py` passed `24 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1554 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Second Wind rows quiet, choose from the fresh distribution. Likely candidates are `Seeing Red` as a small static energy-gain oracle miss, `Whirlwind` X-cost AoE/energy if energy and damage fields remain sufficient, and EndTurnAction monster intent/block lifecycle noise if it continues to dominate.

## Round 37 - 2026-06-06

- Preflight: git tracked files were clean at `1283faf Model Second Wind in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance --truncate-log-after-backup` and clean decision/sim-divergence trace paths. The prior AI marker cutoff was `1780691715`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced Ironclad run files `1780692685.run`, `1780692829.run`, `1780692912.run`, `1780693016.run`, and `1780693143.run`; all had `victory=false`. Deaths were `Snake Plant`, `Collector`, `Hexaghost`, `The Guardian`, and `Sentry and Sphere`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `396` total: `347 monster_state_mismatch`, `22 slime_split_state_mismatch`, `16 player_state_mismatch`, `10 guardian_sharp_hide_reflection`, and `1 end_turn_burn_damage`. Top action-card buckets included `Strike_R` (`60`), `Twin Strike` (`20`), `Anger` (`17`), `Uppercut` (`12`), `Clothesline` (`12`), `Whirlwind` (`12`), `Bash` (`12`), `Cleave` (`11`), `Pummel` (`9`), and `Defend_R` (`8`). Second Wind no longer appeared as a high-frequency bucket.
- Failure type: `Whirlwind` was a high-confidence X-cost all-enemy oracle miss. Live rows had `action.card.damage=0` and `cost=0`, while actual state spent all current energy and dealt 5 damage per energy to all enemies, or 8 per energy when upgraded. Existing snapshots already included player energy, card upgrade count, and all live monster states.
- Fix: `sim_divergence.py` now models `Whirlwind` as an all-enemy attack. It calculates total damage from pre-play energy, sets expected player energy to zero, and uses the shared upgrade helper after adding `Whirlwind`'s +3 damage upgrade bonus.
- Regression: `test_whirlwind_zero_live_damage_spends_energy_for_all_enemy_damage` reproduced the base live shape with 3 energy producing 15 AoE damage and energy 0. `test_whirlwind_plus_uses_upgraded_damage_per_energy` covers the upgraded 8-damage-per-energy path.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `26 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1556 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Whirlwind rows quiet, choose from the new distribution. Likely candidates are `Blood for Blood` dynamic cost/damage if target fields are sufficient, `Feed` static damage with kill-side HP caveats, or remaining multi-hit/Curl Up/Flight lifecycle effects for `Twin Strike` and `Pummel`.

## Round 38 - 2026-06-06

- Preflight: git tracked files were clean at `3c4a982 Model Whirlwind in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance --truncate-log-after-backup` and clean decision/sim-divergence trace paths. The prior AI marker cutoff was `1780693146`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced Ironclad run files `1780694052.run`, `1780694151.run`, `1780694251.run`, `1780694348.run`, and `1780694455.run`; all had `victory=false`. Deaths were `Automaton`, `The Guardian`, `3 Byrds`, `The Guardian`, and `The Guardian`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `323` total: `274 monster_state_mismatch`, `27 player_state_mismatch`, `15 slime_split_state_mismatch`, and `7 guardian_sharp_hide_reflection`. Top action-card buckets included `Strike_R` (`68`), `Bash` (`16`), `Havoc` (`15`), `Iron Wave` (`10`), `Whirlwind` (`10`), `Cleave` (`7`), `Hemokinesis` (`7`), `Blood for Blood` (`6`), `Anger` (`6`), and `Twin Strike` (`6`). The remaining Whirlwind rows were mostly Curl Up, Guardian Mode Shift, monster-removal, or Weak side effects rather than the X-cost energy/damage miss fixed in Round 37.
- Failure type: `Blood for Blood` was the cleanest no-new-fields attack miss. Live rows had `action.card.damage=0`, while actual state dealt 18 base damage, or 22 when upgraded before target-side Vulnerable. The dynamic cost was already visible in `action.card.cost` and the existing energy oracle matched it.
- Fix: `sim_divergence.py` now models `Blood for Blood` as 18 base attack damage. `Blood for Blood+` uses the existing +4 damage upgrade helper, so no special upgraded branch was needed.
- Regression: `test_blood_for_blood_zero_live_damage_uses_base_damage` reproduced the base live shape with 18 damage and dynamic cost spending. `test_blood_for_blood_plus_uses_upgrade_damage_bonus` covers the upgraded 22 damage path under target Vulnerable.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `28 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1558 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Blood for Blood rows quiet, choose from the fresh distribution. Likely candidates are adding draw-pile snapshot support before touching Havoc, or static attack entries such as `Feed` only if kill-side HP and Guardian reflection side effects are separated from base damage evidence.

## Round 39 - 2026-06-06

- Preflight: git tracked files were clean at `1f1ea4b Model Blood for Blood in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance --truncate-log-after-backup` and clean decision/sim-divergence trace paths. The prior AI marker cutoff was `1780694458`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced Ironclad run files `1780695535.run`, `1780695624.run`, `1780695730.run`, `1780695796.run`, and `1780695882.run`; all had `victory=false`. Deaths were `Champ`, `Slime Boss`, `Slime Boss`, `The Guardian`, and `The Guardian`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `359` total: `283 monster_state_mismatch`, `39 player_state_mismatch`, `28 slime_split_state_mismatch`, and `9 guardian_sharp_hide_reflection`. Top action-card buckets included `Strike_R` (`50`), `Twin Strike` (`33`), `Clothesline` (`20`), `Bash` (`17`), `Seeing Red` (`10`), `Carnage` (`10`), `Pommel Strike` (`10`), `Heavy Blade` (`9`), `Bandage Up` (`7`), and `Havoc` (`6`). Blood for Blood no longer appeared as a high-frequency bucket.
- Failure type: `Seeing Red` was the cleanest player-state oracle miss. Live rows had `action.card.damage=0`, `block=0`, and only `player.energy` diffs. Actual state paid the current card cost and then gained 2 energy; zero-cost rows, including upgraded/cost-reduced `Seeing Red`, gained the same +2 energy.
- Fix: `sim_divergence.py` now models `Seeing Red` as a fixed +2 energy gain through the existing card energy effect path. No draw/exhaust behavior was modeled because current snapshot diffs do not compare piles.
- Regression: `test_seeing_red_energy_gain_does_not_create_false_player_diff` reproduced the cost-1 live shape with energy 1 -> 2. `test_zero_cost_seeing_red_gains_two_energy` covers the zero-cost/upgraded path with energy 2 -> 4.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `30 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1560 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Seeing Red rows quiet, likely candidates are `Bandage Up` healing if HP/max-HP evidence remains clean, or adding draw-pile snapshot support before touching Havoc. Multi-hit, Flight, Curl Up, and Malleable rows should stay separated from static damage entries.

## Round 40 - 2026-06-06

- Preflight: git tracked files were clean at `6395b24 Model Seeing Red in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config still used Windows Python with `scripts/run_training_batch.py --eval --max-games 5 --phase conservative --restart-guidance --truncate-log-after-backup` and clean decision/sim-divergence trace paths. The prior AI marker cutoff was `1780695885`.
- Validation: controlled `-FreshRun` clean batch produced five new AI markers and then exited with no remaining `python.exe` batch process: `1780696952`, `1780697120`, `1780697254`, `1780697424`, and `1780697535`. The corresponding Ironclad run files were `1780696948.run`, `1780697117.run`, `1780697250.run`, `1780697421.run`, and `1780697532.run`; all had `victory=false`. Deaths were `Collector`, `Centurion and Healer`, `Sentry and Sphere`, `Champ`, and `Slime Boss`, with the best runs reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `494` total: `420 monster_state_mismatch`, `44 player_state_mismatch`, `17 slime_split_state_mismatch`, `10 guardian_sharp_hide_reflection`, and `3 end_turn_burn_damage`. Top action-card buckets included `Strike` (`64`), `Bash` (`23`), `Reckless Charge` (`23`), `Anger` (`22`), `Havoc+` (`22`), `Blood for Blood+` (`17`), `Good Instincts` (`14`), `Anger+` (`12`), `Uppercut` (`12`), and `Headbutt` (`8`). `Seeing Red` no longer appeared in the fresh high-frequency distribution.
- Failure type: `Good Instincts` was the cleanest player-state oracle miss. Fresh rows had `action.card.block=0`, `cost=0`, no HP/energy changes, and only `player.block` diffs. Base rows gained 6 block and upgraded rows gained 9 block, with snapshot fields sufficient to predict the effect.
- Fix: `sim_divergence.py` now models `Good Instincts` as 6 base block through the existing skill-block table. `Good Instincts+` uses the shared `known_block_upgrade_bonus()` +3 path; no new special branch was added.
- Regression: `test_good_instincts_zero_live_block_uses_base_block` reproduced the base live shape with block 0 -> 6. `test_good_instincts_plus_zero_live_block_uses_upgrade_block` covers the upgraded block 0 -> 9 path.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `32 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1562 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Good Instincts` rows quiet, likely candidates are dynamic draw/exhaust behavior for `Havoc` only after adding or proving draw-pile snapshot support, or monster-side lifecycle effects such as Flight, Malleable, Curl Up, and split handling. Remaining static damage rows are often mixed with those monster powers and should not be flattened into generic base-damage fixes without clean samples.

## Round 41 - 2026-06-06

- Preflight: git tracked files were clean at `6d3f4a1 Model Good Instincts in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config still used Windows Python with the 5-game clean eval batch command. The prior AI marker cutoff was `1780697535`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780698309`, `1780698367`, `1780698451`, `1780698533`, and `1780698609`. The corresponding Ironclad run files were `1780698305.run`, `1780698364.run`, `1780698447.run`, `1780698530.run`, and `1780698606.run`; all had `victory=false`. Deaths were `Hexaghost`, `None` at floor 13, `3 Byrds`, `Hexaghost`, and `The Guardian`.
- Trace distribution: post-cutoff sim-divergence rows were `191` total: `173 monster_state_mismatch`, `10 player_state_mismatch`, `5 end_turn_burn_damage`, and `3 guardian_sharp_hide_reflection`. Top action-card buckets included `Strike` (`46`), `Reckless Charge+` (`12`), `Bash` (`11`), `Thunderclap` (`10`), `Uppercut` (`5`), and `Heavy Blade` (`4`). `Good Instincts` no longer appeared in the fresh distribution.
- Failure type: `Reckless Charge+` was the cleanest static attack oracle miss. Fresh rows had `action.card.damage=0`, `cost=0`, explicit targets, unchanged player HP/energy, and monster HP dropping by 10, or by 15 under target `Vulnerable`. The Dazed side effect is outside the current snapshot diff surface and was not modeled.
- Fix: `sim_divergence.py` now models `Reckless Charge` as 7 base attack damage. `Reckless Charge+` uses the shared `known_damage_upgrade_bonus()` +3 path, so no new upgraded branch was added.
- Regression: `test_reckless_charge_zero_live_damage_uses_base_damage` reproduced the base 7-damage zero-live-damage shape. `test_reckless_charge_plus_zero_live_damage_uses_upgrade_damage` covers the upgraded 10-damage path from the fresh live trace.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `34 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1564 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Reckless Charge` quiets, likely candidates are `Uppercut` as another zero-live-damage static attack, or a separate monster-power modeling round for Malleable, Flight, Curl Up, and split rows. Do not fold those monster lifecycle effects into generic attack entries without clean target samples.

## Round 42 - 2026-06-06

- Preflight: git tracked files were clean at `c36bf5f Model Reckless Charge in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config still used Windows Python with the 5-game clean eval batch command. The prior AI marker cutoff was `1780698609`.
- Validation: controlled `-FreshRun` clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780699338`, `1780699437`, `1780699600`, `1780699799`, and `1780699862`. The corresponding Ironclad run files were `1780699334.run`, `1780699434.run`, `1780699597.run`, `1780699796.run`, and `1780699858.run`; all had `victory=false`. Deaths were `Collector`, `The Guardian`, `Champ`, `2 Orb Walkers`, and `The Guardian`, with the best run reaching floor 37.
- Trace distribution: post-cutoff sim-divergence rows were `467` total: `418 monster_state_mismatch`, `29 player_state_mismatch`, `8 guardian_sharp_hide_reflection`, `7 slime_split_state_mismatch`, and `5 end_turn_burn_damage`. Top action-card buckets included `Strike` (`69`), `Anger` (`27`), `Pommel Strike` (`21`), `Immolate+` (`19`), `Bash` (`17`), `Pommel Strike+` (`14`), `Armaments+` (`14`), `Defend` (`13`), and `Uppercut` (`11`). `Reckless Charge` no longer appeared in the fresh distribution.
- Failure type: `Uppercut` was the cleanest remaining static attack oracle miss. Fresh rows had `action.card.damage=0` and monster HP dropping by 13, or by target-side modified amounts when existing powers applied. Several rows also had `target_index=None` from guard-generated actions; the safe subset had exactly one live monster and therefore enough state to infer the target. Multi-monster `target_index=None` rows remain intentionally unresolved.
- Fix: `sim_divergence.py` now models `Uppercut` as 13 base attack damage and infers a missing target only when exactly one live, non-half-dead monster is present. `Uppercut+` does not add damage because the shared upgrade table already marks its damage bonus as 0. Weak/Vulnerable application remains outside the current diff surface.
- Regression: `test_uppercut_zero_live_damage_uses_base_damage` reproduced an explicit-target 13-damage row. `test_missing_target_attack_hits_only_live_monster` covers the one-live-monster target inference path without guessing in multi-target combats.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `36 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1566 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Uppercut` quiets, the next likely work is no longer simple static damage; fresh rows show many Rage player-block diffs and monster lifecycle/power effects such as Curl Up, Flight, Malleable, split, and combat-reward boundary churn. Choose one clean class only after the next distribution confirms it.

## Round 43 - 2026-06-06

- Preflight: git tracked files were clean at `110d37b Model Uppercut in sim divergence`; only known untracked pytest/ACL temp directories remained. CommunicationMod config still used Windows Python with the 5-game clean eval batch command. The prior AI marker cutoff was `1780699862`. One controlled restart attempt sat at the main menu and was captured at `debug_screenshots/sts_screen_20260606_070407_439.png`; a second controlled restart produced the clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780700827`, `1780701022`, `1780701103`, `1780701249`, and `1780701323`. The corresponding Ironclad run files were `1780700823.run`, `1780701019.run`, `1780701099.run`, `1780701246.run`, and `1780701319.run`; all had `victory=false`. Deaths were `Hexaghost`, `3 Byrds`, `The Guardian`, `Automaton`, and `Slime Boss`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `319` total: `268 monster_state_mismatch`, `25 slime_split_state_mismatch`, `18 player_state_mismatch`, `4 end_turn_burn_damage`, and `4 guardian_sharp_hide_reflection`. `Uppercut`, `Reckless Charge`, `Good Instincts`, and `Seeing Red` no longer appeared in the fresh high-frequency buckets. The cleanest same-class monster-side signature was `Curl Up`: `13` PlayCard rows across `Strike`, `Bash`, `Pommel Strike`, `Clothesline`, `Whirlwind`, `Carnage+`, and `Havoc+`. Representative rows had damage and HP reduction correct, but expected monster block stayed `0` while actual block increased by the Louse `Curl Up` amount.
- Failure type: Louse `Curl Up` was not modeled in the sim-divergence oracle. The live shape was an attack dealing unblocked HP damage to a surviving Louse with `Curl Up`; actual state then gained block equal to the power amount. This is diagnostic oracle quality work and does not imply a policy mistake.
- Fix: `_apply_expected_attack()` now applies `Curl Up` block only when the target actually loses HP and survives the hit. It does not apply Curl Up on fully blocked attacks or lethal attacks, and it reuses the existing snapshot power summary rather than adding card-specific special cases.
- Regression: `test_curl_up_target_gains_block_after_surviving_attack` reproduced the fresh live shape with Strike hitting a 12 HP Louse with `Curl Up` 5, leaving it at 6 HP and 5 block.
- Verification after fix: focused regression red `1 failed`, then green `1 passed`; `tests/test_sim_divergence.py` passed `37 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1567 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Curl Up rows quiet, the next clean candidate appears to be player `Dexterity` on block cards such as `Shrug It Off`, but it should still be selected from the fresh post-fix distribution. Keep `Havoc` blocked until draw-pile/top-card state is available, and keep slime split and combat-boundary churn separated from card mechanics fixes.

## Round 44 - 2026-06-06

- Preflight: git tracked files were clean at `26ba576 Model Curl Up in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780701323`. A controlled `-FreshRun` launched the next clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780702249`, `1780702319`, `1780702400`, `1780702522`, and `1780702631`. The corresponding Ironclad run files were `1780702245.run`, `1780702315.run`, `1780702396.run`, `1780702519.run`, and `1780702628.run`; all had `victory=false`. Deaths were `The Guardian`, `Hexaghost`, `Slime Boss`, `Sentry and Sphere`, and `3 Cultists`, with the best run reaching floor 27. `communication_mod_errors.log` showed the batch runner output and no traceback in the inspected tail. `ai_debug.log` had one recoverable `Invalid command: choose` on a map screen, then continued to complete the batch.
- Trace distribution: post-cutoff sim-divergence rows were `275` total: `221 monster_state_mismatch`, `36 slime_split_state_mismatch`, `15 player_state_mismatch`, `2 guardian_sharp_hide_reflection`, and `1 end_turn_burn_damage`. Curl Up fell to `4` mixed PlayCard rows and was no longer the dominant class. Fresh samples showed `action.card.damage` remained unmodified by source-side player powers: `Strike` still reported 6 while player `Strength` 2 made actual damage 8, and `Bash` still reported 8 while player `Weakened` made actual damage 6.
- Failure type: player-side attack damage modifiers were not modeled by the sim-divergence oracle. This created broad false monster HP diffs across attacks under player `Strength` and `Weakened`, while existing target-side `Vulnerable` modeling already handled the receiving side. Heavy Blade required its existing special strength multiplier, not the normal +Strength path.
- Fix: `_card_damage_for_snapshot()` now applies source-side attack modifiers before target-side modifiers: player `Strength` increases attack damage, player `Weakened` / `Weak` applies STS 75% flooring, `Whirlwind` applies these modifiers per hit before multiplying by spent energy, and `Heavy Blade` uses the existing `heavy_blade_strength_multiplier()` path even when live `card.damage` is explicit.
- Regression: `test_player_strength_increases_attack_damage` reproduced a Strength 2 Strike row; `test_player_weak_reduces_attack_damage` reproduced a Weakened Bash row; `test_explicit_heavy_blade_damage_uses_strength_multiplier` covers the explicit-damage Heavy Blade path.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `40 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1570 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If source modifier rows quiet, likely candidates are static/dynamic zero-damage entries such as `Searing Blow` and `Rampage`, attack-triggered `Rage` block, or monster powers such as `Flight` and `Malleable`. Keep slime split and combat-boundary row churn separated from card mechanics fixes.

## Round 45 - 2026-06-06

- Preflight: git tracked files were clean at `f53d51b Apply source attack modifiers in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780702631`. The first controlled restart started Python but produced no callbacks or AI markers; screenshot capture found no visible STS window and then `CopyFromScreen` failed with an invalid handle. A second controlled restart cleared the stalled Java/Python processes and produced the clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780703730`, `1780703881`, `1780703972`, `1780704169`, and `1780704233`. The corresponding Ironclad run files were `1780703726.run`, `1780703878.run`, `1780703969.run`, `1780704166.run`, and `1780704229.run`; all had `victory=false`. Deaths were `Snecko`, `Centurion and Healer`, `Hexaghost`, `Champ`, and `Hexaghost`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `307` total: `267 monster_state_mismatch`, `19 slime_split_state_mismatch`, `15 player_state_mismatch`, and `6 end_turn_burn_damage`. Source Strength/Weak rows were no longer the clean dominant simple-attack class; `Curl Up` fell to zero. The highest fresh zero-damage card bucket was `Dropkick` with `14` rows. Representative rows had live `action.card.damage=0`, actual monster HP dropping by 5, and rows against target `Vulnerable` also refunded 1 energy after the card cost.
- Failure type: `Dropkick` was a high-confidence oracle miss with enough snapshot state. Its base 5 damage was absent from `BASE_ATTACK_DAMAGE`, and its conditional energy refund can be determined from the target monster's existing `Vulnerable` power in the pre-action snapshot. Draw behavior remains outside the current diff surface.
- Fix: `sim_divergence.py` now models `Dropkick` as 5 base attack damage and adds a conditional +1 energy only when the resolved target had `Vulnerable` before the action. Non-vulnerable targets still only pay the card cost.
- Regression: `test_dropkick_zero_live_damage_uses_base_damage` reproduced the non-vulnerable 5-damage row. `test_dropkick_vulnerable_target_refunds_energy` reproduced the target-Vulnerable row with 7 expected damage after target-side Vulnerable and net +1 energy refund.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `42 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1572 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Dropkick` rows quiet, likely candidates are zero-damage exhaust attacks such as `Sever Soul` or `Fiend Fire`, or monster-side `Malleable` rows. Keep `Havoc` blocked until draw-pile/top-card state is available, and keep EndTurnAction monster lifecycle churn separate from card-effect fixes.

## Round 46 - 2026-06-06

- Preflight: git tracked files were clean at `28c1046 Model Dropkick in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780704233`; a controlled `-FreshRun` produced a clean batch without restart recovery.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780704929`, `1780705008`, `1780705043`, `1780705116`, and `1780705211`. The corresponding Ironclad run files were `1780704925.run`, `1780705005.run`, `1780705040.run`, `1780705113.run`, and `1780705207.run`; all had `victory=false`. Deaths were `Automaton`, `Hexaghost`, `Lagavulin`, `Hexaghost`, and `Sentry and Sphere`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `202` total: `177 monster_state_mismatch`, `16 player_state_mismatch`, `6 guardian_sharp_hide_reflection`, and `3 end_turn_burn_damage`. `Dropkick` fell to zero. The fresh zero-damage attack bucket was smaller and led by `Fiend Fire` and `Sever Soul+` at 4 rows each; `Fiend Fire` is hand-count dynamic, while `Sever Soul` has static damage and enough current snapshot state.
- Failure type: `Sever Soul` was a high-confidence static attack oracle miss. Live rows had `action.card.damage=0`, but actual state dealt 16 base damage or 22 when upgraded. The exhaust side effect is not part of the current snapshot diff comparison, so the damage can be modeled independently.
- Fix: `sim_divergence.py` now models `Sever Soul` as 16 base attack damage. `Sever Soul+` uses the existing +6 damage upgrade helper.
- Regression: `test_sever_soul_zero_live_damage_uses_base_damage` reproduced the base 16-damage row. `test_sever_soul_plus_zero_live_damage_uses_upgrade_damage` reproduced the upgraded 22-damage row.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `44 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1574 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Sever Soul` rows quiet, likely candidates are dynamic attacks with currently available hand state such as `Fiend Fire`, or monster-side `Malleable` rows. Keep `Perfected Strike` separate unless deck/strike-source state is sufficient, and keep EndTurnAction lifecycle churn out of card-effect fixes.

## Round 47 - 2026-06-06

- Preflight: git tracked files were clean at `780dd40 Model Sever Soul in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780705211`; the already-started controlled clean batch continued from that cutoff.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780705943`, `1780706015`, `1780706217`, `1780706291`, and `1780706489`. The corresponding Ironclad run files were `1780705940.run`, `1780706012.run`, `1780706214.run`, `1780706287.run`, and `1780706486.run`; all had `victory=false`. Deaths were `Collector`, `Hexaghost`, `Champ`, `Hexaghost`, and `Champ`, with the best runs reaching floor 33. `communication_mod_errors.log` showed batch runner output and no active traceback in the inspected tail.
- Trace distribution: post-cutoff sim-divergence rows were `353` total: `315 monster_state_mismatch`, `21 player_state_mismatch`, `8 end_turn_burn_damage`, `7 slime_split_state_mismatch`, and `2 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`202`), `Strike` (`25`), `Anger` (`24`), `Twin Strike+` (`16`), `Havoc+` (`9`), `Bash` (`7`), `Hemokinesis+` (`7`), `Fiend Fire` (`7`), and `Immolate` (`7`). `Sever Soul` fell to low-frequency mixed rows.
- Failure type: deterministic multi-hit attacks were not modeled correctly. Fresh `Twin Strike+` rows had live `action.card.damage=12`, but actual upgraded damage was `7x2=14` before source modifiers. Under player `Strength`, the source modifier applied to each hit, e.g. Strength 6 dealt `13x2=26` while the old oracle predicted `12+6=18`. A Spheric Guardian row showed the same issue through block: Strength 3 should reduce block by `10x2=20`, but the old single-hit oracle only reduced it by `15`.
- Fix: `sim_divergence.py` now models deterministic multi-hit attacks through a hit-count path. `Twin Strike` uses two hits of base 5 damage, with the existing +2 upgrade bonus applied per hit; source-side Strength/Weak and target-side block/Vulnerable are applied to each hit through the shared attack application logic. Whirlwind was deliberately left at the prior total-damage behavior pending separate live evidence.
- Regression: `test_twin_strike_plus_uses_upgraded_damage_per_hit` reproduced the upgraded `7x2` HP row. `test_twin_strike_strength_applies_to_each_hit` reproduced the Strength-per-hit HP row. `test_twin_strike_strength_hits_block_per_hit` reproduced the Spheric Guardian block row.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `47 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1577 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Twin Strike+` rows quiet, likely candidates are static all-enemy zero-damage `Immolate`, dynamic hand-count `Fiend Fire`, or monster-side `Malleable`. Keep Whirlwind, Havoc, Perfected Strike, and EndTurnAction lifecycle churn separate until fresh distribution and snapshot sufficiency justify them.

## Round 48 - 2026-06-06

- Preflight: git tracked files were clean at `571abe2 Model Twin Strike multi-hit divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780706489`; a controlled `-FreshRun` launched the next clean batch.
- Validation: the clean batch produced AI markers `1780707464`, `1780707620`, `1780707696`, `1780707818`, and `1780708027`, after which the Python batch process exited. The corresponding Ironclad run files were `1780707460.run`, `1780707616.run`, `1780707692.run`, `1780707814.run`, and `1780708023.run`; all had `victory=false`. Deaths were `Sentry and Sphere`, `Champ`, `The Guardian`, `Snecko`, and `Centurion and Healer`, with the best run reaching floor 33. `communication_mod_errors.log` showed batch runner output and no active traceback in the inspected tail.
- Trace distribution: post-cutoff sim-divergence rows were `340` total: `259 monster_state_mismatch`, `58 player_state_mismatch`, `13 guardian_sharp_hide_reflection`, `7 slime_split_state_mismatch`, and `3 end_turn_burn_damage`. Top action-card buckets included `EndTurnAction` (`155`), `Strike` (`28`), `Swift Strike+` (`17`), `Havoc+` (`16`), `Defend` (`12`), `Bash` (`11`), `Thunderclap` (`9`), `Bludgeon+` (`9`), `Heavy Blade` (`8`), `Cleave` (`7`), `Defend+` (`7`), and `Immolate` (`7`). `Twin Strike+` no longer appeared in the fresh high-frequency distribution.
- Failure type: static zero-live-damage attacks were the cleanest same-class oracle miss. `Swift Strike` dealt 7 base damage and 10 upgraded damage while live `action.card.damage=0`; `Bludgeon+` dealt 42 plus source Strength; `Immolate` dealt 21 all-enemy damage, or 28 upgraded. The current snapshot already had target, energy, powers, and monster lists needed to predict these HP/block effects.
- Fix: `sim_divergence.py` now models `Swift Strike` as 7 base attack damage, `Bludgeon` as 32, and `Immolate` as 21. Existing upgrade helpers provide `Swift Strike+` +3, `Bludgeon+` +10, and `Immolate+` +7. `Immolate` was also added to the static all-enemy attack table so it hits each live, non-half-dead monster through the shared attack application path.
- Regression: `test_swift_strike_zero_live_damage_uses_base_damage` reproduced the base Swift Strike row. `test_swift_strike_plus_zero_live_damage_uses_upgrade_damage` reproduced the upgraded Swift Strike row. `test_bludgeon_plus_zero_live_damage_uses_upgrade_damage` reproduced the Bludgeon+ plus Strength row. `test_immolate_zero_live_damage_hits_all_monsters` reproduced the all-enemy base Immolate row. `test_immolate_plus_zero_live_damage_uses_upgrade_damage` reproduced the upgraded Immolate row.
- Verification after fix: focused regressions red `5 failed`, then green `5 passed`; `tests/test_sim_divergence.py` passed `52 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1582 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If these static attack rows quiet, likely candidates are player block modifiers such as `Dexterity` on `Defend`/`Sentinel`, dynamic `Fiend Fire` hand-count damage, or monster-side `Malleable`. Keep `Havoc`, `Reaper`, `Rampage`, and EndTurnAction lifecycle churn separate unless the fresh distribution makes their snapshot sufficiency clear.

## Round 49 - 2026-06-06

- Preflight: git tracked files were clean at `7305e85 Model static zero-damage attacks in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780708027`; a controlled `-FreshRun` launched the next clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780708717`, `1780708940`, `1780709068`, `1780709203`, and `1780709392`. The corresponding Ironclad run files were `1780708713.run`, `1780708937.run`, `1780709065.run`, `1780709199.run`, and `1780709388.run`; all had `victory=false`. Deaths were `The Guardian`, `Centurion and Healer`, `Champ`, `Hexaghost`, and `Champ`, with the best runs reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `365` total: `290 monster_state_mismatch`, `49 player_state_mismatch`, `18 slime_split_state_mismatch`, `6 end_turn_burn_damage`, and `2 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`204`), `Strike` (`29`), `Sword Boomerang` (`17`), `CardRewardAction` (`16`), `Bash` (`13`), `Clothesline` (`11`), `Whirlwind+` (`10`), `Thunderclap` (`8`), and `Cleave` (`6`). The prior static zero-damage buckets for `Swift Strike`, `Bludgeon`, and `Immolate` no longer appeared in the fresh top distribution.
- Failure type: `Sword Boomerang` exposed a bounded random multi-hit oracle miss. It is not safely predictable in multi-monster combats, but `11` of `17` fresh rows had exactly one live monster, making the random target deterministic. Representative rows showed base `Sword Boomerang` dealing `3x3=9`, target `Vulnerable` applying to each 3-damage hit, and block being consumed per hit.
- Fix: `sim_divergence.py` now models `Sword Boomerang` as a deterministic multi-hit attack only when the existing target inference can resolve a target, which currently includes the one-live-monster case. Base `Sword Boomerang` uses three 3-damage hits; upgraded `Sword Boomerang+` uses four 3-damage hits. Multi-monster no-target rows remain intentionally unpredicted rather than guessing random hit distribution.
- Regression: `test_sword_boomerang_single_live_monster_uses_three_hits` reproduced the one-live-monster 9-damage row. `test_sword_boomerang_vulnerable_block_is_applied_per_hit` reproduced per-hit Vulnerable/block behavior. `test_sword_boomerang_plus_uses_four_hits` covers the upgraded four-hit mechanics.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `55 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1585 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If one-live `Sword Boomerang` rows quiet, likely candidates are `Whirlwind+` X-cost damage if per-hit/block evidence is clean, player block modifier rows such as `Dexterity`, or isolated dynamic cards such as `Reaper`. Keep multi-target random Sword Boomerang, `Havoc`, and EndTurnAction lifecycle churn separate.

## Round 50 - 2026-06-06

- Preflight: git tracked files were clean at `8b2b8f1 Model deterministic Sword Boomerang divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780709392`; a controlled `-FreshRun` launched the next clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780710129`, `1780710305`, `1780710503`, `1780710703`, and `1780710796`. The corresponding Ironclad run files were `1780710126.run`, `1780710302.run`, `1780710499.run`, `1780710700.run`, and `1780710793.run`; all had `victory=false`. Deaths were `Champ`, `Colosseum Slavers`, `Snake Plant`, `Centurion and Healer`, and `Slime Boss`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `450` total: `324 monster_state_mismatch`, `99 player_state_mismatch`, `19 slime_split_state_mismatch`, `5 guardian_sharp_hide_reflection`, and `3 end_turn_burn_damage`. Top action-card buckets included `EndTurnAction` (`250`), `Strike` (`40`), `Defend` (`25`), `Havoc+` (`18`), `PotionAction` (`12`), `Anger+` (`10`), `Bash` (`10`), `Heavy Blade` (`8`), `Pommel Strike+` (`7`), `Hemokinesis+` (`7`), and `Cleave` (`7`). The zero-live-damage attack bucket was empty, so the prior static attack fixes were quiet.
- Failure type: attack-triggered `Rage` block was the cleanest same-class player-state oracle miss. Fresh rows across `Strike`, `Hemokinesis+`, `Pommel Strike+`, `Bash`, `Cleave`, and `Clothesline` had player `Rage:3`; expected block stayed unchanged, while actual block increased by 3 after each Attack. Rows with player `Frail` still gained the full Rage amount, showing this block should not be passed through the card block Frail modifier path.
- Fix: `sim_divergence.py` now adds the player's current `Rage` power amount to expected player block whenever a played card is an Attack. The effect is independent of target damage prediction and deliberately does not use `_modified_block()`, because live evidence showed Frail did not reduce Rage block. Skills continue to use the existing card-block path and do not trigger Rage.
- Regression: `test_rage_adds_block_when_attack_is_played` reproduced the base `Rage:3` attack row. `test_rage_block_is_not_reduced_by_frail` reproduced the `Rage:3` plus `Frail` row. `test_rage_does_not_trigger_on_skill` protects the non-Attack boundary.
- Verification after fix: focused regressions red `2 failed, 1 passed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `58 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1588 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Rage rows quiet, likely candidates are remaining block-card rows such as `Dexterity` or card-specific block effects, `Whirlwind+` block/per-hit evidence, or dynamic cards only when snapshot state is sufficient. Keep `Havoc`, random multi-target `Sword Boomerang`, and EndTurnAction lifecycle churn separate.

## Round 51 - 2026-06-06

- Preflight: git tracked files were clean at `9e3867a Model Rage block in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780710796`; a controlled `-FreshRun` launched the next clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780711660`, `1780711739`, `1780711828`, `1780711905`, and `1780712102`. The corresponding Ironclad run files were `1780711657.run`, `1780711736.run`, `1780711825.run`, `1780711901.run`, and `1780712098.run`; all had `victory=false`. Deaths were `Automaton`, `Hexaghost`, `The Guardian`, `Hexaghost`, and `Automaton`, with the best runs reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `271` total: `235 monster_state_mismatch`, `18 player_state_mismatch`, `8 end_turn_burn_damage`, `6 slime_split_state_mismatch`, and `4 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`158`), `Strike` (`25`), `Twin Strike` (`11`), `Whirlwind` (`9`), `Reaper+` (`7`), `Reaper` (`7`), `Havoc` (`6`), `PotionAction` (`6`), and `Bash` (`5`). `Rage` PlayCard rows fell to zero, confirming Round 50 was effective in the fresh batch.
- Failure type: `Reaper` and `Reaper+` were the cleanest same-card oracle miss. Live rows had `action.card.damage=0`; actual state dealt all-enemy damage and healed the player by the actual unblocked HP damage dealt, capped at max HP. Samples showed base `Reaper` dealing 4, `Reaper+` dealing 5, source-side Strength/Weak and target-side Vulnerable/Block affecting the damage, and Curl Up applying after surviving HP damage.
- Fix: `sim_divergence.py` now models `Reaper` as a 4-damage all-enemy attack with the existing +1 upgrade helper. The shared expected attack path now returns actual HP loss, allowing `Reaper` to heal by unblocked damage dealt while preserving existing block, Vulnerable, Curl Up, Strength, Weak, multi-hit, and all-enemy behavior for other cards.
- Regression: `test_reaper_zero_live_damage_hits_all_and_heals_unblocked_damage` reproduced the base row with one blocked Louse plus two Curl Up Louses and 8 total healing. `test_reaper_plus_heal_caps_at_max_hp_after_strength_damage` reproduced the upgraded Strength row with 14 damage dealt and healing capped at max HP.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `60 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1590 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Reaper rows quiet, likely candidates are `Perfected Strike` or `Pummel` if current hand/deck fields are sufficient, or remaining `Whirlwind`/EndTurnAction lifecycle rows if they can be separated from combat-boundary churn. Keep `Havoc` blocked until draw-pile/top-card state is available.

## Round 52 - 2026-06-06

- Preflight: git tracked files were clean at `fcf0713 Model Reaper drain in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780712102`; a controlled `-FreshRun` launched the next clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780712932`, `1780713001`, `1780713085`, `1780713265`, and `1780713363`. The corresponding Ironclad run files were `1780712928.run`, `1780712997.run`, `1780713081.run`, `1780713262.run`, and `1780713360.run`; all had `victory=false`. Deaths were `Shelled Parasite and Fungi`, `Exordium Thugs`, `Hexaghost`, `Champ`, and `Snecko`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `290` total: `227 monster_state_mismatch`, `43 player_state_mismatch`, `9 slime_split_state_mismatch`, `9 guardian_sharp_hide_reflection`, and `2 end_turn_burn_damage`. Top action-card buckets included `EndTurnAction` (`154`), `Strike` (`31`), `Shrug It Off+` (`11`), `Defend` (`10`), `Clothesline` (`9`), `Pummel` (`9`), `Bash` (`7`), and `Second Wind+` (`6`). `Reaper` rows fell to zero, confirming Round 51 was effective in the fresh batch.
- Failure type: player `Dexterity` was the cleanest same-class block oracle miss. Fresh rows showed `Defend`, `Defend+`, `Shrug It Off+`, `True Grit+`, `Armaments`, and `Iron Wave` gaining base block plus Dexterity, including a negative-Dexterity Lagavulin row. `Second Wind+` rows showed Dexterity applied once per exhausted non-Attack card, not once to the aggregated total.
- Fix: `_modified_block()` now adds the player's `Dexterity` amount before applying Frail, with negative Dexterity allowed and the final block clamped at zero. `Second Wind` now calculates block per exhausted non-Attack card through `_modified_block()` so Dexterity and Frail apply at the same per-card grain as live gameplay.
- Regression: `test_dexterity_increases_skill_block` reproduced a Dexterity 1 `Defend` row. `test_negative_dexterity_reduces_skill_block` reproduced a Dexterity -1 `Defend` row. `test_second_wind_plus_applies_dexterity_per_exhausted_non_attack` reproduced a `Second Wind+` row where two exhausted non-Attack cards produced `(7 + 1) * 2` block.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `63 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1593 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Dexterity block rows quiet, likely candidates are `Pummel` as a zero-live-damage multi-hit attack or remaining Guardian Sharp Hide block/reflection rows. Keep `Havoc` blocked until draw-pile/top-card state is available, and keep EndTurnAction lifecycle churn separate.

## Round 53 - 2026-06-06

- Preflight: git tracked files were clean at `e425fad Model Dexterity block in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780713363`; a controlled `-FreshRun` launched the next clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780714289`, `1780714365`, `1780714457`, `1780714517`, and `1780714638`. The corresponding Ironclad run files were `1780714285.run`, `1780714362.run`, `1780714454.run`, `1780714513.run`, and `1780714634.run`; all had `victory=false`. Deaths were `Slime Boss`, `Hexaghost`, `The Guardian`, `The Guardian`, and `The Guardian`, with all five runs reaching floor 16.
- Trace distribution: post-cutoff sim-divergence rows were `205` total: `165 monster_state_mismatch`, `15 player_state_mismatch`, `10 slime_split_state_mismatch`, `9 guardian_sharp_hide_reflection`, and `6 end_turn_burn_damage`. Top action-card buckets included `EndTurnAction` (`126`), `Strike` (`30`), `Rampage` (`11`), `PotionAction` (`7`), `Pommel Strike` (`5`), `Pommel Strike+` (`3`), and `Bash` (`3`). `Dexterity` player-block rows fell to zero, confirming Round 52 was effective in the fresh batch.
- Failure type: `Rampage` was the cleanest current/dynamic attack oracle miss. Fresh rows had live `action.card.damage=0`, while first-play samples dealt the base 8 damage and later same-combat samples dealt 13 after one previous `Rampage` play. Upgraded `Rampage+` rows still used base 8 on first play, but the per-play scaling should increase by 8 instead of 5.
- Fix: `sim_divergence.py` now models `Rampage` as an 8-damage attack and tracks same-combat Rampage growth by card identity. The tracker resets by floor, keys live cards by `uuid` when present, and increments by 5 for base `Rampage` or 8 for `Rampage+` after the observed play. Card summaries now also include `uuid`, `upgrades`, and `misc` so future dynamic-card trace evidence is easier to attribute.
- Regression: `test_rampage_zero_live_damage_uses_base_damage` reproduced the first-play 8-damage row. `test_second_rampage_play_uses_accumulated_combat_damage` reproduced the second-play 13-damage row for the same card uuid. `test_second_rampage_plus_play_uses_upgraded_combat_scaling` covers the upgraded second-play 16-damage path. `test_rampage_accumulated_damage_resets_on_next_floor` protects the combat-reset boundary for persistent card uuid values.
- Verification after fix: initial focused regressions red `3 failed`; final Rampage focused suite passed `4 passed`; `tests/test_sim_divergence.py` passed `67 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1597 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Rampage` rows quiet, likely candidates are `Pummel` as a zero-live-damage multi-hit attack, `Feed` kill-side max-HP gain if isolated cleanly, or remaining Guardian Sharp Hide rows. Keep `Havoc` blocked until draw-pile/top-card state is available, and keep EndTurnAction lifecycle churn separate.

## Round 54 - 2026-06-06

- Preflight: git tracked files were clean at `d13a748 Model Rampage damage in sim divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780714638`. The first controlled `-FreshRun` launched `javaw` and Python but produced no state callbacks, no new marker, and no visible STS window for screenshot capture; `-AllScreens` failed with an invalid desktop handle. A second controlled `-FreshRun` cleared the stalled processes and produced the valid clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780716889`, `1780716968`, `1780717038`, `1780717176`, and `1780717286`. The corresponding Ironclad run files were `1780716886.run`, `1780716964.run`, `1780717035.run`, `1780717157.run`, and `1780717283.run`; all had `victory=false`. Deaths were `Automaton`, `Slime Boss`, `The Guardian`, `Cultist and Chosen`, and `Cultist and Chosen`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `285` total: `239 monster_state_mismatch`, `23 player_state_mismatch`, `21 slime_split_state_mismatch`, and `2 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`151`), `Strike` (`23`), `Feed` (`13`), `Bash` (`10`), `Whirlwind` (`10`), `Clothesline` (`9`), `Pummel` (`7`), `Hemokinesis+` (`7`), `Anger+` (`7`), `Havoc` (`6`), and `Iron Wave` (`6`). `Rampage` rows fell to zero, confirming Round 53 was effective in the fresh batch.
- Failure type: `Pummel` was the cleanest same-class multi-hit oracle miss. Fresh rows had live `action.card.damage=0`, while actual state showed base `Pummel` dealing `2x4=8`, Strength applying to each hit (`(2+3)x4=20`), and target `Vulnerable` applying per hit (`floor(2*1.5)x4=12`). Upgraded `Pummel+` adds one hit rather than increasing per-hit damage.
- Fix: `sim_divergence.py` now models `Pummel` as a deterministic multi-hit attack: base `Pummel` uses four 2-damage hits, and `Pummel+` uses five 2-damage hits. This reuses the existing per-hit source modifier, target modifier, block, and lifecycle logic shared with `Twin Strike` and deterministic `Sword Boomerang`.
- Regression: `test_pummel_zero_live_damage_uses_four_hits` reproduced the base 8-damage row. `test_pummel_strength_applies_to_each_hit` reproduced the Strength-per-hit row. `test_pummel_vulnerable_applies_to_each_hit` reproduced the Vulnerable-per-hit row. `test_pummel_plus_uses_five_hits` protects the upgraded hit-count behavior.
- Verification after fix: focused regressions red `4 failed`, then green `4 passed`; `tests/test_sim_divergence.py` passed `71 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1601 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Pummel` rows quiet, likely candidates are `Feed` base damage if fresh rows remain clean, or a separate monster-power round for `Malleable` / `Plated Armor` / combat-boundary churn. Keep `Havoc` blocked until draw-pile/top-card state is available, and keep EndTurnAction lifecycle churn separate.

## Round 55 - 2026-06-06

- Preflight: git tracked files were clean at `34da945 Model Pummel multi-hit divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780717286`; a controlled `-FreshRun` launched the next clean batch and entered gameplay without the prior startup stall.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780718234`, `1780718329`, `1780718410`, `1780718519`, and `1780718622`. The corresponding Ironclad run files were `1780718230.run`, `1780718326.run`, `1780718406.run`, `1780718515.run`, and `1780718618.run`; all had `victory=false`. Deaths were `Slime Boss`, `Hexaghost`, `Hexaghost`, `The Guardian`, and `Hexaghost`; all five runs reached floor 16.
- Trace distribution: post-cutoff sim-divergence rows were `189` total: `155 monster_state_mismatch`, `13 player_state_mismatch`, `10 end_turn_burn_damage`, `7 slime_split_state_mismatch`, and `4 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`121`), `Strike` (`28`), `Bash` (`6`), `Pommel Strike` (`5`), `Anger` (`4`), `Hemokinesis+` (`4`), `Sword Boomerang` (`3`), `Bandage Up+` (`3`), `Pommel Strike+` (`3`), `Iron Wave` (`2`), and `Havoc` (`2`). `Pummel` rows fell to zero and the zero-live-damage attack bucket was empty, confirming Round 54 was effective in the fresh batch.
- Failure type: `Bandage Up` was the cleanest player-state oracle miss. Fresh rows had `action.card.damage=0`, `block=0`, `cost=0`, and only `player.current_hp` diffs. Base `Bandage Up` healed 4 but capped at max HP, while `Bandage Up+` healed 6.
- Fix: `sim_divergence.py` now models card healing through a small fixed heal table. `Bandage Up` heals 4, `Bandage Up+` heals 6, and the existing `_heal_player()` helper enforces max-HP capping.
- Regression: `test_bandage_up_heals_and_caps_at_max_hp` reproduced the base live row with 78/80 HP capped to 80. `test_bandage_up_plus_heals_six` reproduced the upgraded 45 -> 51 HP row.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `73 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1603 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Bandage Up` rows quiet, likely candidates are remaining player-state rows such as `Pommel Strike+` energy/draw-adjacent effects only if the fresh evidence is clean, or monster lifecycle/power rows such as `Malleable`, `Plated Armor`, and EndTurnAction intent churn. Keep `Havoc` blocked until draw-pile/top-card state is available.

## Round 56 - 2026-06-06

- Preflight: git tracked files were clean at `f263c3e Model Bandage Up healing divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780718622`; a controlled `-FreshRun` launched the next clean batch without the prior startup stall.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780719312`, `1780719413`, `1780719517`, `1780719600`, and `1780719733`. The corresponding Ironclad run files were `1780719309.run`, `1780719409.run`, `1780719514.run`, `1780719597.run`, and `1780719730.run`; all had `victory=false`. Deaths were `The Guardian`, `Slime Boss`, `Gremlin Leader`, `The Guardian`, and `Cultist and Chosen`, with the best run reaching floor 24.
- Trace distribution: post-cutoff sim-divergence rows were `205` total: `154 monster_state_mismatch`, `20 player_state_mismatch`, `18 slime_split_state_mismatch`, `7 guardian_sharp_hide_reflection`, and `6 end_turn_burn_damage`. Top action-card buckets included `EndTurnAction` (`125`), `Strike` (`22`), `Anger+` (`7`), `Clothesline` (`7`), `Anger` (`5`), `CardRewardAction` (`4`), `Twin Strike` (`4`), `Strike+` (`3`), `Reckless Charge+` (`3`), `Reckless Charge` (`3`), `Bash` (`3`), and `PotionAction` (`3`). `Bandage Up` rows fell to zero and the zero-live-damage attack bucket was empty, confirming Round 55 was effective in the fresh batch.
- Failure type: `Ornamental Fan` was the cleanest same-class player-block oracle miss. The run `1780719730.run` had relics `Burning Blood`, `InkBottle`, `Old Coin`, `Snecko Eye`, and `Ornamental Fan`. Decision trace showed floor 22 turn 1 played `Immolate`, `Sever Soul`, then `Anger`; floor 24 turn 1 played `Immolate`, `Sever Soul`, then `Clothesline`. In both cases the third Attack in the turn added 4 block while the old oracle expected zero. Fresh divergence rows had only `player.block` diffs, player powers only `Confusion:-1`, and no card-damage issue.
- Fix: `sim_divergence.py` now snapshots player relics and tracks played Attack count by floor and turn. When `Ornamental Fan` is present, every third played Attack adds 4 expected player block. The attack counter resets on floor/turn changes and updates only after an observed PlayCardAction, keeping the logic scoped to the diagnostic oracle.
- Regression: `test_ornamental_fan_adds_block_on_every_third_attack` reproduced the third-Attack +4 block row. `test_ornamental_fan_attack_count_resets_each_turn` protects the turn-reset boundary so two attacks on one turn do not trigger the first attack next turn.
- Verification after fix: focused regressions red `1 failed, 1 passed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `75 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1605 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Ornamental Fan` rows quiet, likely candidates are remaining Guardian Sharp Hide reflection rows, `Plated Armor` or other relic/power block rows, or EndTurnAction intent/lifecycle churn only if fresh evidence is clean enough to avoid broad ignores.

## Round 57 - 2026-06-06

- Preflight: git tracked files were clean at `46b7fb9 Model Ornamental Fan divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780719733`. The first post-commit controlled `-FreshRun` reached the main menu `Play` state without CommunicationMod callbacks; per user guidance, restarting was treated as the correct operational response. A later controlled restart cleared stale `javaw` and two AI Python processes and the clean batch then entered gameplay normally.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780722154`, `1780722233`, `1780722311`, `1780722380`, and `1780722536`. The corresponding Ironclad run files were `1780722150.run`, `1780722231.run`, `1780722308.run`, `1780722377.run`, and `1780722533.run`; all had `victory=false`. Deaths were `The Guardian`, `Hexaghost`, `The Guardian`, `The Guardian`, and `Champ`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `224` total: `201 monster_state_mismatch`, `12 player_state_mismatch`, `6 end_turn_burn_damage`, and `5 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`124`), `Strike` (`19`), `Searing Blow` (`15`), `Havoc+` (`9`), `PotionAction` (`6`), `Pommel Strike` (`5`), `Hemokinesis` (`5`), `Anger` (`4`), `Anger+` (`4`), and `Feed` (`4`). `Ornamental Fan` player-block rows fell to zero, confirming Round 56 was effective in the fresh batch.
- Failure type: `Searing Blow` was the cleanest same-class attack oracle miss. Fresh rows had live `action.card.damage=0`, `cost=2`, and `upgrades=0`, while actual state dealt the static 12 base damage. A representative floor 4 row hit a Vulnerable Jaw Worm for 18 damage, matching the existing target-side Vulnerable modifier. The code already had `Searing Blow`'s special upgrade scaling helper, so the missing piece was only card recognition in the base attack map.
- Fix: `sim_divergence.py` now models `Searing Blow` as 12 base attack damage. Upgraded `Searing Blow+N` uses the existing `known_damage_upgrade_bonus()` special formula, so no new card-specific branch was added.
- Regression: `test_searing_blow_zero_live_damage_uses_base_damage` reproduced the base 12-damage zero-live-damage row. `test_searing_blow_vulnerable_target_uses_base_damage` reproduced the live Vulnerable row with 18 damage. `test_searing_blow_plus_two_uses_special_upgrade_scaling` protects the existing special upgrade curve by expecting 21 damage for `Searing Blow+2`.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `78 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1608 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Searing Blow` rows quiet, likely candidates are `Feed` base-damage plus kill-side max-HP behavior only if isolated cleanly, `Fiend Fire` if hand/exhaust state is sufficient, or remaining `Havoc` rows only after draw-pile/top-card state is available. Keep EndTurnAction monster intent/lifecycle churn separate from card mechanics fixes.

## Round 58 - 2026-06-06

- Preflight: git tracked files were clean at `aabd3d9 Model Searing Blow divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780722536`. A controlled `-FreshRun` launched gameplay normally after the prior main-menu `Play` restart issue, and the batch reached `Max games reached (5); exiting.`
- Validation: the clean batch produced AI markers `1780723379`, `1780723429`, `1780723533`, `1780723768`, and `1780723839`. The corresponding Ironclad run files were `1780723376.run`, `1780723426.run`, `1780723531.run`, `1780723765.run`, and `1780723836.run`; all had `victory=false`. Deaths were `Hexaghost`, `Hexaghost`, `Hexaghost`, `Champ`, and `The Guardian`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `249` total: `202 monster_state_mismatch`, `31 player_state_mismatch`, `9 end_turn_burn_damage`, and `7 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`147`), `Strike` (`29`), `Bash` (`17`), `Havoc` (`15`), `PotionAction` (`10`), `Uppercut` (`8`), `Reckless Charge` (`6`), `Sentinel` (`3`), and `Whirlwind` (`3`). `Searing Blow` rows and the zero-live-damage attack bucket both fell to zero, confirming Round 57 was effective in the fresh batch.
- Failure type: `Sentinel` was the cleanest current player-block oracle miss. Three fresh rows had live `action.card.block=0`, cost/energy already matching, no player powers that modify block, and only `player.block` diffs of exactly `+5`. `card_upgrades.py` already knew `Sentinel+` adds `+3` block, but `sim_divergence.py` did not include `Sentinel` in its base skill-block map.
- Fix: `sim_divergence.py` now models `Sentinel` as 5 base block through the shared skill-block path. `Sentinel+` uses the existing `known_block_upgrade_bonus()` +3 path; no exhaust/energy-on-exhaust behavior was modeled because current fresh rows were normal played-card block rows.
- Regression: `test_sentinel_zero_live_block_uses_base_block` reproduced the base live row with block 0 -> 5 and energy 1 -> 0. `test_sentinel_plus_zero_live_block_uses_upgrade_block` covers upgraded `Sentinel+` as 8 block via the shared upgrade helper.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `80 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1610 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Sentinel` rows quiet, the next likely candidates are exhaust-triggered `Feel No Pain` player-block rows across `Havoc` and `Shockwave`, but only if the fresh evidence separates self-exhaust from Havoc's top-card play. Keep EndTurnAction intent/lifecycle churn and Guardian reflection rows out of simple card-mechanics fixes.

## Round 59 - 2026-06-06

- Preflight: git tracked files were clean at `78eec0c Model Sentinel block divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780723839`. The first controlled `-FreshRun` launched Python but produced no CommunicationMod callbacks or markers for about 70 seconds, matching the main-menu `Play` stall pattern. Per user guidance, a second controlled restart cleared stale `javaw` plus two AI Python processes and produced the valid clean batch.
- Validation: the clean batch reached `Max games reached (5); exiting.` It produced AI markers `1780724678`, `1780724824`, `1780724913`, `1780724983`, and `1780725062`. The corresponding Ironclad run files were `1780724675.run`, `1780724821.run`, `1780724910.run`, `1780724981.run`, and `1780725059.run`; all had `victory=false`. Deaths were `Slime Boss`, `Automaton`, `The Guardian`, `The Guardian`, and `The Guardian`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `228` total: `190 monster_state_mismatch`, `21 player_state_mismatch`, `10 slime_split_state_mismatch`, and `7 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`121`), `Strike` (`28`), `Wild Strike` (`13`), `Thunderclap` (`9`), `Cleave` (`6`), `PotionAction` (`5`), `Blood for Blood+` (`4`), `Rampage` (`4`), and `Reckless Charge` (`4`). `Sentinel` and `Searing Blow` rows both fell to zero, confirming Rounds 58 and 57 were effective in the fresh batch.
- Failure type: `Wild Strike` was the cleanest same-class attack oracle miss. Fresh rows had live `action.card.damage=0`, cost/energy already matching, and monster HP dropping by 12 base damage, 18 against a Vulnerable target, or 9 when the player was Weak. The Wound insertion side effect is outside the current compared snapshot surface, so this round only modeled the attack damage.
- Fix: `sim_divergence.py` now models `Wild Strike` as 12 base attack damage. `Wild Strike+` uses the existing `known_damage_upgrade_bonus()` +5 path; no draw-pile or status-card modeling was added.
- Regression: `test_wild_strike_zero_live_damage_uses_base_damage` reproduced the base 12-damage row. `test_wild_strike_vulnerable_target_uses_base_damage` covers the target-side Vulnerable 18-damage shape. `test_wild_strike_plus_zero_live_damage_uses_upgrade_damage` protects upgraded 17-damage behavior through the shared helper.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `83 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1613 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Wild Strike` rows quiet, likely candidates are `Body Slam+` as a block-dependent damage oracle if player block is sufficient in fresh rows, or exhaust-triggered `Feel No Pain` rows if they separate cleanly from `Havoc` top-card behavior. Keep Guardian reflection, slime split, and EndTurnAction intent churn separate.

## Round 60 - 2026-06-06

- Preflight: git tracked files were clean at `de5f339 Model Wild Strike divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780725062`. The controlled `-FreshRun` entered gameplay and the batch reached `Max games reached (5); exiting.`
- Validation: the clean batch produced AI markers `1780725726`, `1780725818`, `1780725923`, `1780725995`, and `1780726112`. The corresponding Ironclad run files were `1780725722.run`, `1780725815.run`, `1780725920.run`, `1780725992.run`, and `1780726109.run`; all had `victory=false`. Deaths were `Hexaghost`, `Slime Boss`, `Slime Boss`, `Slime Boss`, and `Shelled Parasite and Fungi`, with the best run reaching floor 25.
- Trace distribution: post-cutoff sim-divergence rows were `202` total: `156 monster_state_mismatch`, `25 slime_split_state_mismatch`, `20 player_state_mismatch`, and `1 end_turn_burn_damage`. Top action-card buckets included `EndTurnAction` (`124`), `Strike` (`28`), `Carnage+` (`11`), `Iron Wave` (`4`), `Havoc` (`4`), `Strike+` (`3`), `Clothesline` (`3`), `Heavy Blade` (`3`), `Carnage` (`3`), and `Fiend Fire` (`3`). `Wild Strike`, `Sentinel`, and `Searing Blow` rows fell to zero, confirming the prior focused fixes remained quiet in the fresh batch.
- Failure type: `Fiend Fire` was the cleanest dynamic attack oracle miss with sufficient snapshot state when `target_index` was present. Fresh rows had live `action.card.damage=0`, but actual combat applied one hit per other card in hand: seven-card hands produced six 7-damage hits for base `Fiend Fire`, and a six-card hand produced five 7-damage hits. A Spheric Guardian sample reduced block from 44 to 2, confirming this is multi-hit behavior rather than one 42-damage hit. The upgraded path uses 10 damage per exhausted other hand card. A separate `target_index=None` row remains unresolved because the trace does not identify the target.
- Fix: `sim_divergence.py` now models `Fiend Fire` as 7 base attack damage and derives its hit count from the pre-action hand, excluding the played `Fiend Fire` card. `Fiend Fire+` reuses the existing +3 damage upgrade helper, and the implementation reuses the existing per-hit source modifiers, target Vulnerable, block, and monster lifecycle handling. Hand-exhaust side effects were not modeled because the current divergence comparison does not include hand contents.
- Regression: `test_fiend_fire_zero_live_damage_uses_other_hand_cards_as_hits` reproduced the six-hit 42-damage live row. `test_fiend_fire_zero_live_damage_repeats_against_block` reproduced the Spheric Guardian block 44 -> 2 row. `test_fiend_fire_plus_uses_ten_damage_per_other_hand_card` covers the upgraded 10-damage-per-hit path.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `86 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1616 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Fiend Fire` rows quiet, likely candidates are monster-power rows such as `Malleable` or `Plated Armor`, or `Havoc`/Rage player-block rows only if fresh evidence separates Havoc's top-card play from the played card itself. Keep `target_index=None` Fiend Fire rows unresolved until target evidence is available, and keep slime split plus EndTurnAction intent churn separate.

## Round 61 - 2026-06-06

- Preflight: git tracked files were clean at `b18b028 Model Fiend Fire divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780726112`. The controlled `-FreshRun` started gameplay normally and the batch reached `Max games reached (5); exiting.`
- Validation: the clean batch produced AI markers `1780731359`, `1780731407`, `1780731568`, `1780731690`, and `1780731804`. The corresponding Ironclad run files were `1780731356.run`, `1780731404.run`, `1780731565.run`, `1780731687.run`, and `1780731801.run`; all had `victory=false`. Deaths were `None` at floor 12, `Slime Boss`, `Automaton`, `None` at floor 31, and `Shell Parasite`, with the best run reaching floor 33.
- Trace distribution: post-cutoff sim-divergence rows were `236` total: `220 monster_state_mismatch`, `8 slime_split_state_mismatch`, `6 player_state_mismatch`, and `2 end_turn_burn_damage`. Top action-card buckets included `EndTurnAction` (`131`), `Strike` (`24`), `Headbutt` (`8`), `Bash` (`6`), `Hemokinesis+` (`6`), `Heavy Blade` (`6`), `Havoc+` (`6`), `Clothesline` (`5`), `Whirlwind` (`5`), and `Sever Soul` (`5`). `Fiend Fire` fell to two rows, both with `target_index=None` and combat-boundary diffs, so there was no fresh actionable target-known Fiend Fire row.
- Failure type: Byrd `Flight` was the cleanest monster-power oracle miss. There were 12 explicit-target Flight rows across `Strike`, `Heavy Blade`, `Sever Soul`, `Headbutt`, `Bash`, and `Clothesline`: `Strike` 6 dealt 3, `Heavy Blade` 14 dealt 7, `Sever Soul` 16 dealt 8, and `Headbutt` 9 dealt 4. A Flight amount 1 sample left the Byrd alive at 12 HP and changed live intent to `Intent.STUN`, while the old oracle expected 5 HP and kept the prior intent.
- Fix: `sim_divergence.py` now applies target-side `Flight` as 50% attack damage reduction with integer flooring. When an attack causes HP loss, the expected Flight amount is decremented inside the snapshot; when it reaches zero on a surviving target, expected intent becomes `Intent.STUN`. This stays within the existing monster-side attack path and does not add card-specific exceptions.
- Regression: `test_flight_halves_attack_damage` reproduced the `Strike` 6 -> 3 row. `test_flight_halves_odd_attack_damage_with_flooring` covers the odd `Headbutt` 9 -> 4 row. `test_flight_zero_after_attack_sets_stun_intent` covers the Flight-to-stun transition.
- Verification after fix: focused regressions red `3 failed`, then green `3 passed`; `tests/test_sim_divergence.py` passed `89 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1619 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If `Flight` rows quiet, likely candidates are `Malleable` or `Plated Armor` monster-power rows with explicit targets, while `Havoc+` remains blocked until top-card evidence is available and `target_index=None` combat-boundary rows should stay unresolved.

## Round 62 - 2026-06-06

- Preflight: git tracked files were clean at `cf589b3 Model Flight divergence`; only known untracked pytest/ACL temp directories remained. The prior AI marker cutoff was `1780731804`. The first controlled `-FreshRun` produced only startup lines for about 90 seconds with no callbacks or new markers, matching the main-menu `Play` stall signature. A second controlled restart cleared stale `javaw` and two AI Python processes, then entered gameplay normally.
- Validation: the clean batch produced AI markers `1780732749`, `1780732870`, `1780732951`, `1780733019`, and `1780733104`, then reached `Max games reached (5); exiting.` The corresponding Ironclad run files were `1780732746.run`, `1780732867.run`, `1780732948.run`, `1780733016.run`, and `1780733101.run`; all had `victory=false`. Deaths were `The Guardian`, `Sentry and Sphere`, `Slime Boss`, `The Guardian`, and `Slime Boss`, with the best run reaching floor 25.
- Trace distribution: post-cutoff sim-divergence rows were `209` total: `168 monster_state_mismatch`, `18 player_state_mismatch`, `15 slime_split_state_mismatch`, and `8 guardian_sharp_hide_reflection`. Top action-card buckets included `EndTurnAction` (`116`), `Strike` (`24`), `Havoc+` (`13`), `Bash` (`7`), `Pommel Strike+` (`7`), `Blood for Blood` (`6`), `Heavy Blade` (`5`), and `Sword Boomerang` (`4`). Explicit-target `Flight`, `Malleable`, `Plated Armor`, and `Curl Up` rows fell to zero, confirming Round 61's Flight fix was effective in the fresh batch.
- Failure type: `guardian_sharp_hide_reflection` was now the cleanest actionable class. Fresh rows showed The Guardian with `Sharp Hide` amount 3 reflecting 3 damage to the player on Attack cards, reducing block when available and otherwise reducing HP. This had already been classified as a distinct reason, but the oracle still did not predict the reflection. The existing Twin Strike attribution sample also showed Sharp Hide triggers once per Attack card, not once per hit.
- Fix: `sim_divergence.py` now predicts The Guardian's `Sharp Hide` reflection for targeted attacks and all-enemy attacks that hit a live Guardian with the `Sharp Hide` power. Reflection damage is applied once per Attack card, after attack-side block gains, and spends player block before HP. No generic ignore was added.
- Regression: `test_guardian_sharp_hide_reflection_spends_block` covers a multi-hit `Twin Strike` reflecting only 3 damage into player block. `test_guardian_sharp_hide_reflection_damages_hp_without_block` covers the no-block HP loss path.
- Verification after fix: focused regressions red `2 failed`, then green `2 passed`; `tests/test_sim_divergence.py` passed `90 passed`; full pytest with disabled cache provider and repo-local basetemp passed `1620 passed`.
- Next candidate: rerun another clean 5-game batch from this commit. If Sharp Hide rows quiet, likely candidates are `Blue Candle` / curse-play HP loss if it repeats beyond the two `Parasite` rows, or remaining monster-power rows with explicit target evidence. Keep `Havoc+` blocked until draw-pile/top-card evidence is available.
