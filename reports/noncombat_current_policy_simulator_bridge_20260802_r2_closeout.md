# Current Policy Simulator Bridge r2 Closeout

## Status

The source-bound event-semantics successor is complete. Its unchanged frozen
Stage 1 gate is `frozen_bridge_structurally_compatible`; its single authorized
Stage 2 compatibility execution is a valid fail-closed result:
`event_option_semantics_event_unsupported` for `The Cleric`.

- Implementation commit: `b789131971636437367424e69987a84937f3856d`
- Implementation source SHA-256: `02ef3cb69babc7c9d8b1a685b3269332ed87b89e76f08fb18319d3f18ae65a46`
- Registration SHA-256: `b251f7eefa558610b58c2dc23525f60302e6ae9f99bd11da72faa6c9a138f8f6`
- Manifest file SHA-256: `f5898b84d8ed0deced0c1642ba319f6d3f247932800eae4d80f0a1c93bd57489`
- Predecessor registration SHA-256: `2c8cf7fae0acc707cd37d31fa75f79066fa3764f827962979e877be6581ba192`
- Current identity: `current_optimized_ironclad_a0_conservative_snapshot_v1`

## Semantic Contract

The adapter-layer resolver supports exactly `Liars Game` phase zero with legal
indices zero and one. It binds `Agree` and `Disagree` to upstream event enum,
event id, legal-action, console-label, and execution sources under simulator
commit `7476a81954020087da31d41d16fddf475746ec2d` and full 79-file source digest
`a3f98721ec37373b1b00aef660832a3307f0186ba0614d07a3b1e7de8ab2e46a`.
Unsupported identities, phases, candidate sets, or provenance fail closed.

## Stage 1

| Category | Decision | Result |
| --- | ---: | --- |
| route | 0 | `route:map_node:0:0` |
| card reward | 1 | `card_reward:take:0:0:armaments` |
| shop | 5 | `shop:remove_card` |
| event | 11 | `event:the_ssssserpent:option:1` |

Every row passed two fresh-session replays without fallback, tracker activity,
gameplay I/O, or source mutation. The successor comparison proves equality of
authority, Current configuration, adapter provenance, frozen demonstrations,
metadata, prior-seed evidence, runtime, Stage 1 rows/settings, and Stage 2
seeds/settings.

## Stage 2

The native module hash, build identity, simulator commit/dirty state, full
source digest, and submodule commits matched the registration before execution.
The gate uses only registered seeds `2000..2003`, two replays per seed, and a
fixed 500-decision limit. It failed before completing a trajectory when Current
reached `The Cleric`, for which the narrow resolver has no registered semantics.

No seed was added, no threshold changed, and no retry occurred. To preserve the
historical policy-validity hash closure, the resolver was moved byte-for-byte
out of the core adapter into its own registered source module. The old
configuration and all five payload artifacts first matched their old manifest;
the stored Stage 2 result then matched its registered SHA-256
`5629d19955c53bc6af7469355f8e2faab32b0fcb33f3338626052f9c529605c7`.
Only the implementation commit, source-file list, and source hash changed in
the successor registration. The native Stage 2 executor was not entered again.
The final six canonical artifacts were rebuilt from a fresh four-row Stage 1
replay plus that verified Stage 2 result, then independently recomputed with no
artifact mismatch.

## Authority And Next Boundary

All baseline-floor, formal-readiness, fresh-evidence, gameplay, promotion,
reward, and training authority remains false. This report establishes frozen
bridge compatibility and one concrete own-trajectory coverage blocker, not
Current policy quality.

Do not add only `The Cleric` and rerun the same seeds. The next change should
first perform a read-only static audit of all event identities and phases whose
semantic labels can affect Current, then define a separately reviewed total
source-bound adapter contract. No live gameplay, fresh simulator cohort, model
fitting, reward change, or formal RL training is authorized.
