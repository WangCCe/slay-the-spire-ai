## Why

The 2026-07-15 v1 outcome-evidence study is permanently blocked and cannot be resumed, while the underlying evidence question remains unresolved. Three protocol-era pre-lock v2 qualification identities are immutable failures: r1 stopped on an external Windows log-sharing error, r2 proved that the registered 30-second readiness deadline was too short for cold CommunicationMod startup, and r3 reached a valid ready record but no release was preserved. R4 is obsolete; r5 and r6 are retired static-only launcher-timeout identities; r7 is retired after `source_validation_failed`; and r8 is retired after reaching `source_verified` and then failing `request_validation_failed` before active-request publication. The r7 source defect and r8 request-source binding defect were fixed separately without retrying either identity. Every identity remains non-authorizing, the registered study root is absent, and collection cannot start. The bound B3-B7 feasibility audit additionally reports `not_demonstrated`: 125 complete trajectories contain one raw victory but zero deterministic-Current-supported victories, so no evidence currently justifies another qualification identity or the fixed 600-attempt collection.

## What Changes

- Create one canonical v2 registration for a new 24-by-25 known-propensity eval schedule with new deterministic slot IDs, seeds, paths, and artifact root while preserving the registered exploration rates, action budget, evidence thresholds, deterministic-Current target, and validated estimator contracts.
- Regenerate the canonical v2 registration under the reviewed 120-second readiness contract and current source, while keeping the study ID, study root, seed schedule, behavior, thresholds, target, estimator, command, and 10-second release contract unchanged. Preserve the superseded 30-second registration bytes as historical pre-lock evidence until this tracked amendment replaces them.
- Preserve r4 through r8 as immutable pre-lock history. R8 source snapshot S `519cf55fa17706e7faee05a05c3a9c85f4238b75`, direct-child R `1d805b450862d2b5f34aeb2d4bbbeb9884510c84`, six-file root, standalone verifier, and independent closeout prove only a consumed `request_validation_failed` boundary with exact restoration. Preserve the separate regression-backed request-source diagnosis/fix and the later feasibility `not_demonstrated` report; do not retry r8, prepare r9, or launch this study until a source-comparable feasibility review passes under a separate amendment.
- Freeze a clean source commit and v2 run lock only after qualification and independent review pass. Reject any implementation, registration, command, CommunicationMod, checkpoint, or qualification-binding drift before or during collection.
- Execute the 24 ordered slots blindly through the v2 handshake. Preserve launch-at-most-once, non-replacement, fixed-rate, fixed-threshold, and global-stop behavior; expose only structural monitor fields until collection terminates.
- Finalize exactly once after either all slots are terminal or one registered global stop closes collection, then require the standalone verifier to replay the applicable normal or blocked branch.
- Keep OPE interpretation, formal non-combat RL training, reward design, Bottled-driven live actions, gameplay-policy edits, and live promotion unauthorized. A passing evidence gate permits only a later separately approved analysis or training proposal.

Success is an independently verified `ready`, `inconclusive`, or `blocked` closeout from the new v2 root. A favorable policy estimate is not required, and no observed result may extend the schedule, lower a threshold, replace a slot, or change the target policy.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `noncombat-outcome-evidence-expansion`: require a bound no-action launch qualification before a fresh v2 run lock, and define execution and closeout of a separately registered v2 study without changing historical v1 evidence.

## Impact

- Primary code and tools: historical snapshots and request bindings through r8 remain immutable. The separate request-source fix establishes the r8 implementation cause without changing its retired closeout, and no active replacement candidate exists.
- Planning and evidence artifacts: one v2 study identity, historical registration reviews, immutable r1-r8 qualification evidence, one bound feasibility `not_demonstrated` report, one still-absent registered study root, and no current qualification, start, collection, OPE, training, gameplay, or promotion authority.
- Live execution: Windows-only CommunicationMod eval through `D:\anaconda\envs\stsai\python.exe`; no WSL gameplay process, training flag, checkpoint mutation, or ordinary-gameplay handshake is introduced.
- Historical compatibility: the immutable 2026-07-15 v1 root remains read-only and excluded from every v2 pool, estimate, and authority decision.
- Rollback boundary: before `start`, preserve r1-r8 and every superseded registration, request, configuration, verifier, closeout, and feasibility-audit byte, and collect no evidence. R8 cannot be retried, relabeled, repaired in place, or used as authority for r9. A future replacement requires a source-comparable feasibility pass plus an explicit qualification amendment. Once `start` publishes any registered study artifact, any integrity failure follows the registered blocked or recovery path and restores live configuration only after independent verification.
