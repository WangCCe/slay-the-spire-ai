## 1. Freeze The Historical Boundary And V2 Identity

- [x] 1.1 Replay the standalone verifier against the immutable 2026-07-15 v1 root, record its registration/run-lock/ledger/claim/closeout and root-inventory hashes, and confirm every normal pool/OPE artifact remains absent without modifying any historical byte.
- [x] 1.2 Confirm the approved v2 study ID, registration path, seed base, study root, and qualification root are unused; verify no stale Slay the Spire, Java, runner, or production Python process can contaminate the candidate.
- [x] 1.3 Generate `reports/noncombat_outcome_evidence_expansion_20260716_v2_registration.json` canonically with schema v2, study ID `noncombat-outcome-evidence-expansion-20260716-v2`, seed base `2026071600`, the approved new root, and the unchanged 24-by-25 behavior, analysis, threshold, command, and handshake contracts.
- [x] 1.4 Add an exact-artifact regression that loads the committed v2 registration, reproduces its canonical bytes and hash, verifies all 24 unique slots and paths, and proves no v1 root, ID, seed, or evidence binding appears.
- [x] 1.5 Generate the registration review Markdown with the exact schedule, rates, budget, thresholds, implementation paths, v2 handshake fields, authority boundary, and explicit statement that no game, run lock, ledger, OPE, or training has started.

## 2. Verify And Review The Candidate Offline

- [x] 2.1 Run focused Windows pytest for registration, runner dry-run, handshake, monitor, finalizer, and independent verifier using `-p no:cacheprovider` and a fresh writable repository basetemp; fix only registration-artifact or test defects attributable to this change.
- [x] 2.2 Run the full Windows pytest suite, `openspec validate --all --strict`, `git diff --check`, Python compile/import checks, registration byte replay, and v1 blocked-verifier replay; preserve exact results in the pre-collection review.
- [x] 2.3 Obtain independent code/spec/registration review for identity isolation, v2 schema bindings, command safety, blinding, thresholds, and normal/blocked closeout behavior. Resolve documentation or exact-artifact test findings; stop for a separate regression-backed change if any runtime implementation defect is found.
- [x] 2.4 Commit the candidate registration, review, tests, archived v1 change, and this OpenSpec change as one pre-collection planning commit; require tracked-clean source before operational qualification.

## 3. Preserve Historical Identities And Replace R4 Safely

- [x] 3.1 Preserve the original `noncombat_outcome_evidence_expansion_20260716_v2_qualification` root after its pre-ready external monitor failure; exclusively publish and replay failure-record self-hash `ccd76824c90a9726c57b48a7f71d8bc1d8da94df6c686ae36eff10a1b72db41f`, prove the registered study root/ready/release/ledger remained absent, restore CommunicationMod byte-for-byte, and obtain an independent no-retry governance verdict.
- [x] 3.2 Amend and independently review the candidate for the previously absent `noncombat_outcome_evidence_expansion_20260716_v2_qualification_r2` root without changing registration bytes, runtime implementation, study root, seeds, behavior, thresholds, or authority; rerun focused/full offline verification and commit a tracked-clean replacement-qualification candidate.
- [x] 3.3 Run the r2 dry-run and bounded no-action attempt, preserve its ten-file root after the child readiness deadline failure, exclusively publish and replay failure-record self-hash `8bb9b396e129e017fd84fb4aabe7fa6d02bb51b0c7c147836dbf29e7c289c95c` and file SHA-256 `3fbace492dd0f849bdf86deff1df97dfc4ae3b77a427545413ac48170c9c2540`, prove no ready/release, study root, run lock, ledger, gameplay evidence, marker, checkpoint, global-log, or surviving-process contamination, restore CommunicationMod byte-for-byte, and forbid r2 retry.
- [x] 3.4 Complete `fix-cold-start-study-handshake-timeout` separately: reproduce a 45-second cold state against the old 30-second bound, implement the fixed 120-second readiness contract plus exact-deadline fail-closed checks, preserve release 10, pass focused/full pytest and strict OpenSpec review, and commit/push the verified fix as `79cf98f89` without launching the game.
- [x] 3.5 Regenerate the exact v2 registration and review under the 120-second contract and current implementation hashes while preserving study ID, root, seed schedule, behavior, target, estimator, thresholds, command, and release 10; update exact-artifact tests, independently review the amendment, confirm `qualification_r3` and the study root are absent, and commit a tracked-clean r3 candidate before any live process starts.
- [x] 3.6 Preserve and independently replay r3 failure-record self-hash `e495ce302f0ddf9628962e0d4147614a0cf9b9c7c010f256662a98eae76b033d`, file SHA-256 `5a3c47f5b93d7c1f66b5de6c32d3af139188b60735fa36733c6b3c6ee772cfec`, and final inventory `2a63cf3b7505ebf6d9e2f605eade7deec3c0afdaaa4da90ca6a99b517c82cb16`; retain valid attempt/ready, absent release, restored isolation, no implementation-defect finding, no retry, and no start, while describing only the independently supported release-side external orchestration failure.
- [x] 3.7 Re-render the then-current registration exactly, bind source snapshot `76ed7135ad2e8236c77b20957a0617262c9e77b6`, create r4 with only its canonical static config, and generate prep-only request self-hash `f21313b80fedfccdea76c0e69d3d3d44f06289ba033159537d73f0202f3c039e`, file SHA-256 `1e04b2a378e434c16f45cdb8a389dd527d2419ae6625d6a141e1a9fb0be4792c`, and size 5996. Stop before R or launch after review proves the v1 request/result/verifier chain lacks bound broad isolation; preserve r4 as unconsumed and non-authorizing.
- [x] 3.8 Complete `bind-qualification-isolation-evidence` offline, pass the 253-test qualification slice, 3120-test full suite, strict OpenSpec validation, and independent review, and commit it as fresh source snapshot S `22cfd8b0c235af51b12911c0cb949f7e28a31ce6`. Re-render the unchanged registration, prepare previously absent r5 with static config only, generate canonical request-v2 self-hash `b80b5311018c6c39de6df55c8e1d9090826e07bbd606711b4d0c4d68b4d1cfce`, file SHA-256 `40e978059c31f90c2da52435d50193deea95b1f3f19c28a865ef96f80c20ed26`, size 8813, and isolation baseline `f9902d1403bdc73101a1c665e06dd3e1d717c1d4a86faa02744fd168ffbfea43`; prepare only its declared seven-path inert allowlist for direct-child R without launching.
- [x] 3.9 Commit and push r5 direct-child R `2936c547bd7917fdbbc487470326716129e3fbe2`, pass the source-only independent verifier with 95 checks, replay r1/r2/r3 plus obsolete r4, and invoke r5 once through real CommunicationMod. Preserve the root as static-config-only after the implicit 10-second external-process initialization timeout killed the qualifier before active request publication; prove no Python child, active request, handshake, terminal, study artifact, marker, run, checkpoint, global-log, run-lock, or ledger change, restore the exact 505-byte baseline, stop Java, and retire r5 without retry.
- [x] 3.10 Prepare previously absent r6 at source snapshot S `2936c547bd7917fdbbc487470326716129e3fbe2` with static config only; transition the CommunicationMod baseline from SHA-256 `374806e6386940a5945ffd03411b526d6a21c002b938bb4db253780f787b8e9a`, size 505, to SHA-256 `a404525790c925423d6298b639322b370ccf37414f9808362bd24a8dc9feb202`, size 535, by adding only `maxInitializationTimeout=120`; generate request-v2 self-hash `fc5332ffca8b00a1e5132047d07538825369f187db030d9e080a91d37fa8496c`, file SHA-256 `28c174d6fba875ba110b107c92da5d522664ead81d9bf5c0db71db6fc3748b69`, size 8886, and isolation baseline `a37a8e64fff42339b9d13bcede2dba5370d678c9e08b74d1e31107ccb4d7aed4`; prepare exactly seven inert paths for direct-child R without launching.
- [x] 3.11 From tracked-clean `HEAD == R`, replay all historical identities through r5, run the 24-launch dry-run, and invoke r6 exactly once through real CommunicationMod with explicit initialization timeout 120. Preserve r6 as static-config-only after CommunicationMod killed the silent external process at 120 seconds before active request publication; preserve the independently supported stage boundary without inventing a terminal, restore exact 535-byte configuration and zero-drift isolation, stop Java, and retire r6 without retry or `start` authority.
- [x] 3.12 Complete a separate reviewed change that makes failures between trusted launcher entry and active request publication durably diagnosable, or obtain equivalent bounded evidence through an explicit amendment. Only then may this change prepare a previously absent replacement qualification root and fresh request/review bindings; do not tune timeout, retry r6, or launch another candidate as diagnosis.
- [x] 3.13 Prepare and invoke the separately reviewed r7 replacement exactly once. Preserve its claim, `launcher_verified`, `runner_entered`, and `source_validation_failed` bootstrap chain; independently verify the consumed partial boundary, restore exact CommunicationMod and protected inventories, retire r7 without retry, and leave `start` blocked pending a separate regression-backed source fix and a successful replacement qualification.
- [x] 3.14 Complete and archive the r7 source-byte fix, then prepare and invoke the separately reviewed r8 replacement exactly once. Preserve its claim through `source_verified` plus terminal bootstrap failure `request_validation_failed`; independently verify the consumed partial boundary, import the `retired_after_live_boundary` closeout, restore exact CommunicationMod and protected inventories, retire r8 without retry or r9 authority, and leave `start` blocked pending a separate regression-backed request-validation diagnosis/fix and a later successful replacement qualification.
- [x] 3.15 Diagnose the r8 request-validation boundary offline, prove that its qualifier CLI bound `--request` to the absent active publication path rather than `request_source_path`, and complete the separate regression-backed canonical builder/validator fix without retrying r8, preparing a replacement identity, launching the game, or starting training.

## 4. Create And Independently Check The Frozen Study

- [ ] 4.1 Replay every preserved r1-r8 boundary and, only after a later separately reviewed replacement succeeds, its request/terminal/attestation chain; apply the approved study-launch CommunicationMod configuration only from that qualified identity, then require exact equality for S/R, registration bytes, implementation map, registered command, `D:\anaconda\envs\stsai\python.exe`, semantic configuration, checkpoint snapshot, and dry-run digest. Require `--eval`, no training or mutation flag, and no stale process; on any failure before `start`, restore and verify the request-bound baseline.
- [ ] 4.2 Invoke `start` exactly once from the tracked-clean qualified commit to create the v2 run lock, append-only ledger, and 24 registered configs. If it exits before publishing any run lock, ledger, child, or study artifact, prove that absence, restore the baseline, and require fresh qualification before retry; if any registered artifact exists, enter the fail-closed blocked/recovery path and do not restore configuration before independent verification.
- [ ] 4.3 Independently validate the run lock, registration bytes, source commit, implementation hash map, child command, CommunicationMod semantics, checkpoint inventory, and ledger genesis; rerun the 24-slot dry-run under the real lock before slot 01.

From completion of task 4.2 through completion of task 6.4, do not edit any tracked file. Record progress only in the append-only external ledger and blinded artifacts. If a global stop occurs, never repair, retry, replace, or resume this registration.

## 5. Execute The Blinded 600-Attempt Schedule

- [ ] 5.1 If no global stop exists, execute slots 01-04 in order through the v2 preclaim handshake; after each terminal slot inspect only the blinded structural monitor. If collection has already stopped, verify these and all later inapplicable slots remain unlaunched.
- [ ] 5.2 If no global stop exists, execute slots 05-08 under the unchanged lock and repeat only structural handshake, lifecycle, artifact, replay, join, and isolation checks; otherwise verify launch blocking remains exact.
- [ ] 5.3 If no global stop exists, execute slots 09-12 under the unchanged lock without reading outcomes or adapting source, rates, thresholds, or schedule; otherwise verify launch blocking remains exact.
- [ ] 5.4 If no global stop exists, execute slots 13-16 and mark any postclaim early exit interrupted without retry or replacement; otherwise verify launch blocking remains exact.
- [ ] 5.5 If no global stop exists, execute slots 17-20 with the same blinded monitor and preserve every handshake and terminal artifact; otherwise verify launch blocking remains exact.
- [ ] 5.6 If no global stop exists, execute slots 21-24, then require every registered slot to be terminal and no unregistered slot to have launched; otherwise require the registered terminal prefix followed only by unlaunched slots.

## 6. Finalize And Verify Exactly Once

- [ ] 6.1 Capture the post-collection CommunicationMod semantic and checkpoint snapshots, process inventory, fresh `ai_debug.log`, and `communication_mod_errors.log`; require the run-lock-bound live state to remain unchanged and do not restore configuration or inspect outcome fields before the registered unblind point.
- [ ] 6.2 Invoke `finalize` exactly once. With no global stop, require the all-slot pool, deterministic-Current target, readiness, 10,000-replicate estimate, influence, comparison, and normal closeout; with a global stop, require only the blocked claim and closeout plus absence of every normal artifact.
- [ ] 6.3 Run the standalone verifier over the complete v2 root and require exact replay of the ledger-selected normal or blocked branch, including registration, run lock, handshakes, slot accounting, evidence membership or forbidden-artifact absence, hashes, and authority booleans.
- [ ] 6.4 After independent verification, restore CommunicationMod configuration byte-for-byte, require no surviving process or checkpoint drift, and classify the result only as ready, inconclusive, or blocked using the registered gates. Do not infer causal uplift, start formal training, change rewards or gameplay policy, execute Bottled actions live, or promote a policy regardless of the observed result.

## 7. Close The Repository Change

- [ ] 7.1 After the run-lock window closes, update deferred checkboxes and write a deterministic repository closeout report that incorporates every immutable r1-r8 qualification boundary and disposition, plus any later successful replacement request/terminal/attestation chain only if one actually exists; bind the superseded registration identity, external study artifact hashes, verifier result, observed gates, limitations, live-isolation comparison, and the still-closed authority boundary.
- [ ] 7.2 Run focused Windows pytest for the registration, runner, handshake, monitor, finalizer, and verifier, then run the full Windows pytest suite with cache disabled and a fresh writable basetemp; treat any frozen-study implementation failure as a separate recovery change rather than editing evidence.
- [ ] 7.3 Run `openspec validate --all --strict`, `git diff --check`, canonical registration and closeout byte replay, v1 and v2 standalone verifier replays, and an independent final review.
- [ ] 7.4 Commit the closeout, sync the delta spec to the main capability, and archive this change while stating that any later OPE interpretation or non-combat RL work requires a separately approved OpenSpec change.
