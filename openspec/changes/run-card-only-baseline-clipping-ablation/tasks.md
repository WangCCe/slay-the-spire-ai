## 1. Shared-Trajectory Runtime

- [ ] 1.1 Add regressions for one-cohort reuse, branch-local policy-term ownership, and clipped versus unclipped advantage construction.
- [ ] 1.2 Implement exact stored-decision term reconstruction for an independently restored branch model.
- [ ] 1.3 Implement clipped and unclipped one-step updates from one fitted cross-fitted baseline with atomic rollback.
- [ ] 1.4 Summarize advantage, objective, gradient, parameter, and branch-difference telemetry without serializing full tensors.

## 2. Bound Runner

- [ ] 2.1 Bind source, checkpoint `004`, historical checkpoint `005`, r1 registration, native module, corpus, fixed probe, consumed seed schedule, and production isolation.
- [ ] 2.2 Enforce one 64-access collection, bounded known censoring, no replacement, and no resume after a started update.
- [ ] 2.3 Require branch A candidate model bytes to reproduce historical checkpoint `005` before classifying branch B.
- [ ] 2.4 Classify the fixed material-effect criteria and deny all downstream training, fresh-evaluation, gameplay, loading, and promotion authority.

## 3. Verification And Execution

- [ ] 3.1 Run py_compile and focused runtime/runner regressions in a scoped system-temp pytest directory.
- [ ] 3.2 Validate the OpenSpec change strictly and verify the runner source-only preflight.
- [ ] 3.3 Execute the registered ablation once, within 64 environment accesses, and publish compact terminal/report artifacts.
- [ ] 3.4 Verify input hashes, reproduction, support, authority, output isolation, and absence of CommunicationMod/game/production-checkpoint changes.
- [ ] 3.5 Record the mechanism go/no-go and commit only code, tests, OpenSpec state, and compact evidence; retain model checkpoints locally.
