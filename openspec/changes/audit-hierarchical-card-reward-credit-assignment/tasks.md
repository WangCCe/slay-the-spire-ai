## 1. Freeze Planning And Existing Evidence

- [x] 1.1 Strictly validate and independently review the proposal, design,
  delta spec, fixed formulas, strata, support thresholds, verdict precedence,
  and no-authority boundary; commit and push planning before implementation.
- [ ] 1.2 Publish a canonical source-only preimplementation record binding the
  consumed terminal manifest, training rows, checkpoints, postmortem, verifier
  result, planned source/test/report paths, and all-false authority without
  importing Torch/native or reading an empirical environment.
- [ ] 1.3 Add RED tests for canonical input binding, gzip/canonical hashes,
  inactive output identity, tracked-source identity, mutation rejection, and
  standard-library import isolation.
- [ ] 1.4 Add RED fixtures for registered float32 return reconstruction, direct
  policy/family-entropy/expected-conditional-entropy/combined take-logit
  pressure, fixed strata, seed clusters, support labels, verdict precedence,
  and deterministic report bytes.

## 2. Implement The Standard-Library Audit

- [ ] 2.1 Implement strict canonical JSON, tracked-file and terminal-manifest
  binding, inactive-bundle validation, bounded gzip loading, and exact required
  artifact identity without importing consumed runtime code.
- [ ] 2.2 Implement contiguous chunk/seed/decision validation, pre-decision
  floor and card-reward ordinal reconstruction, reward to go, float32
  normalization, and chunk-objective reconciliation.
- [ ] 2.3 Implement separate policy, family-entropy, expected-conditional-
  entropy, and combined direct take-logit pressures with chunk-mean scaling
  and explicit full-gradient and causal limitations.
- [ ] 2.4 Implement the fixed chunk/floor/ordinal/propensity/margin strata,
  deterministic support classification, selected-family associations, and one
  cluster summary per training seed without adaptive bins or inference.
- [ ] 2.5 Implement bounded verdict precedence, all-false authority, exact
  source/input bindings, canonical JSON, generated Markdown, and a source-only
  CLI that rejects caller overrides.

## 3. Verify And Publish The Audit

- [ ] 3.1 Turn every RED boundary green; run the full focused audit suite plus
  relevant hierarchical verifier/runtime regressions with a fresh system-temp
  basetemp, `py_compile`, `git diff --check`, and strict change/global OpenSpec;
  review, commit, and push the implementation source, tests, and
  preimplementation record before publication.
- [ ] 3.2 Run two fresh source-only audit processes against the immutable
  terminal evidence from the exact committed source; require byte-identical
  JSON/Markdown, exact reconciliation, and proof that Torch, native,
  environment, seed, model, and game effects stay absent.
- [ ] 3.3 Review the implementation and report interpretation, resolve findings,
  then run one repository `commit` test gate; any source, test, or report-
  generator change loops back through a new 3.1 implementation commit/push and
  both 3.2 processes before proceeding. Do not launch gameplay because no
  production behavior path changes.
- [ ] 3.4 Publish the canonical audit reports and update project direction with
  the bounded verdict and next proposal gate while every downstream authority
  remains false.

## 4. Close Out

- [ ] 4.1 Sync the accepted capability to main specs, archive the completed
  change, rerun final applicable verification, and commit/push only scoped
  source, tests, reports, direction, and OpenSpec files while preserving the
  consumed experiment and unrelated local artifacts.
