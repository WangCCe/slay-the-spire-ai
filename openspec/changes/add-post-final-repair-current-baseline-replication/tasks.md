## 1. Planning And Evidence Freeze

- [ ] 1.1 Strict-validate the complete change, review the consumed study,
  post-repair readiness review, static closure evidence, current source drift,
  and tracked inventory without native loading; then commit and push all
  planning artifacts before editing source or tests.
- [ ] 1.2 Add a canonical preimplementation record that binds the planning
  commit, old terminal artifacts, repair/readiness evidence, current
  policy/bridge/adapter sources, fixed `60000` selection algorithm, unchanged
  gates, limits, expected external identities, absent output, and all-false
  execution and downstream authority.

## 2. Red Successor Contract Regressions

- [ ] 2.1 Add red registration and lineage tests for exact source, predecessor,
  repair, external, and authorization bindings; pushed-state/output checks;
  fixed cohort algorithm and partition; policy isolation; unchanged gates and
  limits; and rejection before a started journal or native loading.
- [ ] 2.2 Add red episode and classification tests for independent Current and
  first-candidate environments, production-shaped candidates, complete source
  and action hashes, terminal rows, exact Courier conservative rows, every
  non-Courier failure, replay mismatch, resource bounds, complete denominators,
  canary stop, holdout gates, and terminal lane semantics.
- [ ] 2.3 Add red lifecycle and publication tests for repeatable source-only
  preflight, one durable empirical start, interruption/failure preservation,
  holdout non-access, deterministic bootstrap, canonical artifacts, and
  fresh-process no-native byte verification.
- [ ] 2.4 Add readiness-delta regressions proving the exact historical
  `card_metadata_cost_invalid: Injury` exception permits only this final
  proposal while every complete quality result, other blocker, and final
  successor result remains terminal.

## 3. Focused Replication Implementation

- [ ] 3.1 Implement
  `analysis_scripts/noncombat_current_baseline_replication.py` as a new
  versioned runner that composes the existing adapter, Current bridge,
  metadata/event contracts, tracked inventory, and canonical helpers without
  modifying the consumed study runner or Current policy.
- [ ] 3.2 Implement deterministic preregistration selection from fixed start
  `60000`, exact 16/64 cohort partition, lineage and all-false authority
  validation, repeatable no-native preflight, and separate exact execution
  authorization validation.
- [ ] 3.3 Implement Current/first-candidate episode orchestration, two-replay
  identity, exact support accounting, fixed canary and holdout classification,
  10,000-resample bootstrap, resource bounds, and final verdict precedence.
- [ ] 3.4 Implement durable started-journal handling, partial-row preservation,
  atomic configuration/rows/draws/metrics/report/manifest publication,
  fresh-process no-native verification, and distinct prepare/execute/verify
  CLI commands.

## 4. Offline Verification And Implementation Gate

- [ ] 4.1 Run focused replication, predecessor study, Current bridge, adapter,
  event, shop, metadata, seed-inventory, and readiness pytest with a fresh
  writable basetemp; run `py_compile` and `git diff --check`.
- [ ] 4.2 Recompute the consumed study artifacts byte-for-byte without native
  loading, prove its runner and all terminal evidence remain unchanged, and
  verify that implementation tests access no native module, environment, seed,
  gameplay, model, reward, OPE, or trainer.
- [ ] 4.3 Run strict change and global OpenSpec validation plus the repository
  partitioned `commit` gate as the full-suite gate; review scope and authority,
  then commit and push the source-only implementation.

## 5. Preregistration Without Execution

- [ ] 5.1 From the clean pushed implementation, regenerate the tracked seed
  inventory, apply the fixed `60000` algorithm, confirm all 80 materialized
  seeds are untouched and disjoint from every historical/reserved cohort, bind
  external bytes without native import, and construct no environment.
- [ ] 5.2 Prepare one canonical registration and source-only preflight, verify
  every source, lineage, cohort, gate, limit, external identity, output,
  artifact, and all-false authority field, then commit and push
  preregistration.
- [ ] 5.3 Stop before the started journal, native loading, and seed access until
  the user explicitly approves the exact pushed registration hash, command,
  limits, cohort, and final-attempt semantics in a tracked authorization.

## 6. Final Replication And Closeout

- [ ] 6.1 After exact execution approval, create the durable started journal
  and run canary once; access holdout automatically only if the immutable
  canary passes, with no retry, repair, replacement, resume, or override after
  start.
- [ ] 6.2 Preserve the terminal positive, negative, blocked, interrupted, or
  partial result; run the no-native verifier and publish exact cohort, support,
  floor, bootstrap, category, outcome, lifecycle, and authority findings.
- [ ] 6.3 Perform a separate read-only baseline/formal-readiness refresh, keep
  target-supported outcomes independent, update project direction, sync specs,
  archive the change, run final focused and commit-gate verification as
  applicable, and commit/push all canonical evidence.
