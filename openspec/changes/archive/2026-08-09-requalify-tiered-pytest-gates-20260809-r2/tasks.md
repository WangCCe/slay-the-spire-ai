## 1. Freeze The Recovery Boundary

- [x] 1.1 Record the exact 3,638-pass/16-skip/one-failure result, 300.24-second
  pytest duration, 303.60-second gate duration, float-rounding root cause,
  measured two-file candidate set, fixed success metric, non-goals, and
  rollback boundary without retrying the invalidated gate.
- [x] 1.2 Strictly validate, independently review, commit, and push one
  proposal-only commit containing exactly this complete planning directory,
  without staging pending audit source or unrelated artifacts.

## 2. Close The Separate Correctness Prerequisite

- [x] 2.1 Complete the deadline-boundary RED-to-GREEN repair as a separate
  direct bug-fix commit, preserving its exact focused result outside this
  capability's final publication commit.
- [x] 2.2 Reconfirm that no test file other than the 46-test pending audit was
  added since the 3,593-pass qualification and that the one failed runtime
  file is the only second candidate permitted by the frozen margin rule.

## 3. Freeze The Measured Test Boundary

- [x] 3.1 Add RED manifest regressions for exact 17-file `full_only`
  membership, nonblank measured rationales, direct-ownership documentation,
  inclusive ordinary collection, and unchanged `full` construction.
- [x] 3.2 Add exactly the measured audit and runtime files to `full_only` and
  update testing documentation without changing the runner, pytest
  configuration, test assertions, or complete-suite membership.
- [x] 3.3 Draft the dated requalification report and project-direction entry
  explicitly reconciling the old 3,593-pass/15-file baseline, the new 46-test
  delta, the failed 3,638-pass invocation, and the 3.79-versus-22.96-second
  predicted margins; leave final counts and durations open until the one-shot
  gates complete.

## 4. Qualify Once

- [x] 4.1 Run the runtime and audit files directly, runner/manifest focused
  regressions, command-construction probes, Python compilation, strict OpenSpec
  validation, `git diff --check`, and independent review.
- [x] 4.2 Run the finalized `commit` profile once; require exit zero and at
  most 300 seconds, preserving any failure or slow result without retry or
  post-result tuning.
- [x] 4.3 Run the unchanged `full` profile once because the `full_only`
  boundary changed; preserve its exact result and duration without retrying.

## 5. Publish And Return To The Audit

- [x] 5.1 Finalize the qualification report and direction entry, sync the
  modified tiered-gate capability, archive this change, run final strict
  validation and diff review, then make one final capability-publication commit
  containing only manifest/tests/docs/report/direction/synced archived change
  files and push `master`. Record fresh gameplay validation as not applicable.
- [x] 5.2 Return to `audit-card-acceptance-conditional-choice`, use the new
  direct-file plus requalified-gate evidence for its source boundary, and do
  not rerun the invalidated 303.60-second invocation.
