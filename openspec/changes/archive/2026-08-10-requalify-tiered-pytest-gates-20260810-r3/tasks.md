## 1. Freeze The Candidate Boundary

- [x] 1.1 Verify from Git that the seven named files are the complete ordinary test-file delta after `52edf92f8`, excluding only the already-full-only conditional-choice file; record the 262.89s, 515.35s, and 528.59s timing evidence.
- [x] 1.2 Strict-validate and independently review the complete planning artifacts, then commit and push them alone as the immutable candidate-set boundary.

## 2. Measure Attribution Once

- [x] 2.1 Run the exact seven-file suite once on the production Windows interpreter with a fresh registered pytest temp child, `--durations=50`, xUnit1 JUnit XML, and total-phase durations; preserve its complete result without retry, source edits, or candidate changes.
- [x] 2.2 Verify XML hash, counts, unique file mapping, finite nonnegative times, per-file presence, and at most 30.00s absolute XML-to-pytest-wall residual; only then apply the preregistered 5.00-second per-file and 265.70-second aggregate rules. The trusted 258.760-second selected aggregate failed the 265.700-second rule, so r3 entered terminal closeout without manifest edits.

## 3. Publish The Frozen Test Boundary

- [x] 3.1 Not applicable on the terminal path: the aggregate rule failed before RED manifest work was permitted.
- [x] 3.2 Not applicable on the terminal path: no manifest, ownership documentation, runner, configuration, assertion, or complete-suite edit was permitted.
- [x] 3.3 Write the dated r3 terminal report with fixed baseline, invalidation, attribution, candidate identities, trust checks, rollback, and skipped final-gate fields.

## 4. Verify Before Final Gates

- [x] 4.1 Not applicable on the terminal path: no runner/manifest selection diff exists; strict OpenSpec, diff checks, and terminal-report review remain required for closeout.
- [x] 4.2 Not applicable on the terminal path: no gate-affecting selection files were edited or frozen.

## 5. Qualify Once

- [x] 5.1 Not applicable on the terminal path: the failed attribution aggregate prohibited the final `commit` invocation.
- [x] 5.2 Not applicable on the terminal path: no in-budget r3 `commit` result existed to authorize `full`.
- [x] 5.3 Finalize and independently review the terminal report using only preserved outcomes; fresh gameplay validation is not applicable.

## 6. Closeout

- [x] 6.1 On the failure path, publish only independently reviewed terminal evidence and archived planning without syncing the failed delta; strict-validate and push the completed closeout without changing policy/RL authority.
