## 1. Freeze The R8 Boundary

- [x] 1.1 Replay and bind the immutable r7 partial closeout, archived source diagnosis/fix, unchanged v2 registration, absent registered study root, and uniformly false current `start`, gameplay, OPE, policy, training, and promotion authority.
- [x] 1.2 Prove the proposed r8 root and every fixed claim, stage, request, handoff, result, failure, and attestation path absent through lexical no-follow inspection; require zero target processes before candidate creation.
- [x] 1.3 Update the canonical project direction to record the established r7 root cause and make one independently verified r8 no-action qualification the current blocker; preserve r1-r7 as immutable historical evidence.
- [x] 1.4 Freeze the source-neutral stop rule and exact disposition schema. Any implementation finding stops r8 and routes through a separate change; this amendment cannot repair r8 or prepare r9.

## 2. Build And Review The Offline Candidate

- [x] 2.1 Commit the approved amendment, select one clean source snapshot S, and bind exact implementation, runner, launcher, verifier, registration, source-fix, and relevant test input hashes without changing live configuration.
- [x] 2.2 Capture exact CommunicationMod bytes and semantics, protected marker/run/checkpoint/global-log inventories, registered-root absence, rollback material, and zero target processes without launching Java or the game.
- [x] 2.3 Deterministically render the r8 bootstrap-v1/request-v3 candidate, launch token, static-root image, inert direct-child allowlist, and all-false authority twice; require byte-identical outputs while the external root remains absent.
- [x] 2.4 Run candidate-specific focused qualification tests, canonical/source-only replay, Python compile checks, strict OpenSpec validation, and `git diff --check`. Reuse the registered commit-gate result only if every bound executable and relevant test input hash matches; otherwise rerun the registered gate. Do not run an unregistered raw full suite.
- [x] 2.5 Obtain an independent review of source identity, request and launcher anchors, S-to-R inert diff, rollback package, one-shot boundary, and authority fields; resolve documentation findings, create one final direct-child R, recheck it from tracked-clean source, and publish one externally anchored offline go/no-go decision.

## 3. Publish And Invoke At Most Once

- [x] 3.1 On offline no-go, preserve obsolete preparation, leave the external root and CommunicationMod unchanged, skip the remaining live tasks, and continue only to the absence closeout. On go, recheck exact S/R, candidate bytes, root absence, baseline inventories, rollback material, and zero target processes.
- [x] 3.2 Exclusively publish the reviewed static r8 root and request-bound CommunicationMod configuration, then compare every byte, semantic field, path, inventory, and launch anchor with the go record before invocation; preserve and retire the root if this comparison fails.
- [x] 3.3 After an exact post-publication match, invoke ModTheSpire once through real CommunicationMod and `D:\anaconda\envs\stsai\python.exe`; permit only the qualifier-owned no-action lifecycle and forbid retry, second invocation, gameplay action, study `start`, outcome access, OPE, training, or policy change.
- [x] 3.4 Observe only bounded qualification/process state, preserve every resulting root byte and declared absence, stop controlled target processes when required, restore exact request-bound CommunicationMod bytes, and recollect protected inventories without repair or retry.

## 4. Replay And Close By Disposition

- [x] 4.1 Run the standalone verifier when its required anchors exist and independently attest the exact complete, partial, failed, or non-live boundary together with configuration restoration, protected-state equality, invocation evidence, registered-root absence, and zero surviving target processes.
- [x] 4.2 Select exactly one disposition: `obsolete_before_publication`, `retired_after_publication_without_invocation`, `retired_after_live_boundary`, or `qualified_for_start_review`; leave every study, gameplay, OPE, policy, causal, training, and promotion authority false.
- [x] 4.3 Render a deterministic closeout that binds all source, request, review, root, result, verifier, attestation, configuration, inventory, process, limitation, rollback, and authority evidence and independently replay its canonical bytes.
- [x] 4.4 For an obsolete or retired branch, import the closeout, update the blocked study handoff, run focused and registered gates as required, sync specs, commit, and archive. For a qualified branch, preserve tracked-clean R and externally anchored handoff/closeout until a separate `start` review declines or the study's no-write window releases it.
