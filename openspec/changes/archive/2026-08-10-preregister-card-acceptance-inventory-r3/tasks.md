## 1. Freeze R3 Planning

- [x] 1.1 Reconfirm the pushed isolated-dispatch source commit/digests, exact `build_inventory` authority-to-receipt ordering, r1/r2 terminal evidence, parent task 6.2 state, and absent r3 output/staging/attempts/receipt paths without reading candidate blobs or seeds.
- [x] 1.2 Strict-validate and independently review the complete r3 proposal/spec/design/tasks, resolve every actionable finding, then commit and push the planning artifacts alone.

## 2. Publish Dispatch, Source, And Request Boundary

- [x] 2.1 Run and preserve the exact side-effect-free production `-I ... check-dispatch` observation; independently review its command, isolation, script/control identities, deterministic bytes, and absence of authority/Git/seed/receipt/output access.
- [x] 2.2 Render the fixed r3 source inventory and path-only preflight binding source `3dd915e7c`, exact dispatch, current pushed/clean ancestry, both terminal predecessors, complete candidate path identity, generated-root exclusions, and absent r3 output/staging/attempts/receipt paths; independently review without candidate blob or seed-value access.
- [x] 2.3 Render and independently review the distinct r3 inventory request with fixed request id/output root, `max_materialized_seeds=1152`, only repository-evidence/seed-discovery/cohort-materialization authority, and every native/model/environment/training/evaluation/gameplay/qualification/promotion/downstream authority false.
- [x] 2.4 Commit and push the dispatch/source/preflight/request artifacts and reviews as one immutable request boundary; do not edit source or tests.

## 3. Publish Delegated Authority Boundary

- [x] 3.1 Reobserve approval-time revocation state, resolve only the immutable standing external-human grant to the exact r3 request, and independently review the new request-bound delegated approval without creating or broadening the grant.
- [x] 3.2 Render and independently review the exact r3 inventory authorization, requiring all request/source/path/digest/resource/authority bindings and rejecting every r1/r2 or modified substitute.
- [x] 3.3 Commit and push delegated approval, authorization, and their reviews together; verify current HEAD equals pushed `origin/master` and remains tracked-clean.

## 4. Freeze Launch And Pre-Start Gate

- [x] 4.1 After the authority commit is pushed, capture and independently review one fresh launch-time revocation observation bound to the exact request/approval/authorization and current conversation state; commit and push the launch boundary.
- [x] 4.2 Reproduce the exact dispatch bytes, run the complete owning seed-inventory pytest file plus isolated-dispatch regression under the registered Windows temp parent, strict-validate OpenSpec, validate authority/source identity, and record the unqualified repository commit timing profile as not applicable because no code/test behavior changed.
- [x] 4.3 From a pushed tracked-clean base, build and independently review a final pre-start gate proving all authority/source/predecessor/preflight identities match and r3 output/staging/attempts/receipt paths remain absent; then commit and push that gate.
- [x] 4.4 After the gate commit is pushed, perform a separate no-write recheck that current HEAD equals `origin/master`, tracked files are clean, the committed gate bytes match, no unexpected child exists, and every r3 absence condition still holds. Stop before launch on any drift or if a code/test edit is required.

## 5. Materialize Or Close Terminal

- [x] 5.1 Invoke the exact r3 `build-inventory` command under a 3,600-second outer observer. On an initial pre-receipt failure, publish and independently review the bounded command/phase/identity/path/process/access evidence before any eligibility decision; permit no automatic retry. Only an exact external repair with every registered surface proven clean may publish a fresh launch observation plus the sole canonical ordinal-two reinvocation review binding the failure, immutable original launch, replacement launch, and unchanged request/approval/authorization. Starting ordinal two consumes that eligibility; a second pre-start failure publishes reviewed `prestart-terminal` evidence and closes r3. On any ambiguity, existing/partial receipt, or post-receipt failure, close r3 terminal without retry, resume, repair, tuning, replacement, or registration.
- [x] 5.2 On build success only, run the distinct read-only `verify-inventory` command and independently reconstruct source registry, rows, exclusions, ordered `512/128/512` cohorts, role/whole digests, authority bindings, output closure, and receipt identity. Not applicable because the CLI invocation failed after receipt and artifact publication; the published artifact remains unverified and `verify-inventory` was not invoked.
- [x] 5.3 On exact verification only, publish and independently review one canonical all-false r3 registration, update parent task 6.2 while leaving 6.3 unchecked, and commit/push the bounded success outcome. On failure, publish/review only the terminal outcome and leave 6.2 unchecked.

## 6. Closeout

- [x] 6.1 Publish a bounded r3 postmortem recording exact operations, outcome, downstream denial, and fresh gameplay validation as not applicable. Sync applicable delta specs, archive the completed change, strict-validate the global OpenSpec set, and push closeout without granting training or later-stage authority.
