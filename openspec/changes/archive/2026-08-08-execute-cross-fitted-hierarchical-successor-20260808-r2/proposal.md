## Why

The exact 20260808-r2 registration, request, delegated approval, and tracked
authorization are now pushed and independently verified, while the registered
output root remains absent. The next irreversible boundary is the single
registered simulator execution that can finally collect cross-fitted mechanism
evidence.

## What Changes

- Revalidate the exact pushed registration, request, standing delegation,
  delegated approval, authorization, source/readiness bindings, native/runtime
  identity, output path, schedule, resources, isolation, and retry/resume
  contract before execution.
- Invoke the tracked `execute` entry point for registration identity
  `noncombat-cross-fitted-hierarchical-learning-successor-20260808-r2` on CPU,
  using only the authorized 8x64 schedule, at most 576 environment accesses,
  eight optimizer updates, and 14,400 charged seconds.
- While the true execution process is alive, monitor process liveness only and
  do not inspect or mutate the active output root. Do not launch Slay the Spire
  or CommunicationMod.
- After process exit, independently verify and preserve the complete terminal
  bundle, then publish a read-only postmortem that distinguishes completion,
  family saturation, pre-start blocking, and terminal failure without making a
  policy-quality or formal-RL claim.
- Permit only the registered manual same-identity pre-start reopening or the
  one registered post-start infrastructure resume when the durable evidence
  proves it eligible. Never change source, path, seed, cohort, estimator,
  threshold, resource, authority, or retry terms.
- Success means one closed, independently verified terminal identity. A valid
  negative family-saturation result is operational success; floor or victory
  improvement is not required.
- Before the first-seed marker, rollback means stop and preserve or exactly
  remove only an unconsumed additive setup root when the contract permits it.
  After the marker, rollback means preserve immutable evidence and close or
  resume only the same identity under the registered lifecycle.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `noncombat-cross-fitted-hierarchical-learning-successor`: consume the exact
  20260808-r2 authorization in one bounded mechanism execution and independently
  close its terminal evidence.

## Impact

- Creates the registered output root and bounded simulator-only training
  evidence under `reports/noncombat_cross_fitted_hierarchical_learning_successor_20260808_r2/`.
- Loads the exact bound native module and Torch runtime only after all source-
  only preflight checks pass; does not load a production checkpoint.
- Adds a terminal verification/postmortem report, one delta requirement,
  project-direction update, and archived OpenSpec change.
- Does not change implementation, tests, registration terms, gameplay policy,
  CommunicationMod configuration, or any historical output.
