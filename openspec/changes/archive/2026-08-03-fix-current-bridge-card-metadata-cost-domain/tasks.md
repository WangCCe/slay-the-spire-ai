## 1. Freeze The Repair Boundary

- [x] 1.1 Bind implementation work to the committed card metadata cost-domain audit and confirm the exact 20-entry stable-ID, display-name, and metadata-type table is represented independently in tests.
- [x] 1.2 Add exhaustive red regressions for all 20 exact empty-cost `Unplayable.` records, upgraded `REFLEX`/`TACTICIAN`/`DEUS_EX_MACHINA`, and production-shaped `INJURY` deck hydration; run the focused selection and record the expected pre-fix failure.
- [x] 1.3 Add reverse and non-mutation regressions for source-name, metadata-name, type, cost, and description drift; unknown and Wish IDs; `SLIMED`, `PRIDE`, and `WHIRLWIND`; and unchanged snapshot, candidate, source-card, and metadata inputs.

## 2. Implement Closed Cost Compatibility

- [x] 2.1 Add one closed module-level mapping for exactly the 20 audited stable native IDs and their exact display names and metadata types.
- [x] 2.2 Route listed IDs through exact source and metadata validation before generic cost parsing, hydrate only the registered empty `Unplayable.` shape as SpireComm cost `-2`, and emit field-specific structural blockers on drift.
- [x] 2.3 Preserve existing integer and `X -> -1` handling, upgrades and all other card fields, source non-mutation, and fail-closed behavior for every unlisted empty-cost record.

## 3. Verify The Offline Repair

- [x] 3.1 Run the exhaustive focused bridge regressions and adjacent Current bridge, metadata, adapter, diagnostic, and study-verifier tests without native module loading, environment construction, or seed access.
- [x] 3.2 Run Python compilation checks, inspect the scoped diff, and pass strict validation for this change and the complete OpenSpec tree.
- [x] 3.3 Run the registered repository commit gate once as the required full-pytest verification; do not launch a separate raw full suite or any gameplay validation because this analysis-only bridge is outside the live Communication Mod path.

## 4. Close Out The Change

- [x] 4.1 Record focused and repository-gate evidence in a deterministic closeout report and update project direction to mark only the offline compatibility repair complete, with baseline and formal-RL readiness unchanged.
- [x] 4.2 Confirm the terminal Current baseline registration, authorization, journal, rows, artifacts, blocked verdict, and readiness refresh remain byte-for-byte unchanged and that no study retry or reinterpretation occurred.
- [x] 4.3 Sync the delta requirement to the main specification, archive the completed change, validate the post-archive OpenSpec tree, and commit and push one cohesive compatibility fix without staging unrelated historical artifacts.
