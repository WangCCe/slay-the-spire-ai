## 1. Trainer Contract

- [x] 1.1 Add failing focused tests for deterministic combat-group splitting, stored one-step targets, and fail-closed input validation.
- [x] 1.2 Add failing focused tests for embedding-only updates, zero/unobserved row preservation, and deterministic candidate bytes.

## 2. Offline Candidate

- [x] 2.1 Implement the isolated inventory-embedding trainer and fixed development metrics without changing runtime imports.
- [x] 2.2 Implement atomic candidate/report publication with exact input hashes, tensor-isolation evidence, fixed gates, and development-only authority.

## 3. Execution And Verification

- [x] 3.1 Run focused tests and strict OpenSpec validation.
- [x] 3.2 Execute one fixed r2 development training run and publish the result without same-corpus tuning.
- [x] 3.3 Run the optimized commit gate, document whether a separate fresh holdout is justified, and commit the coherent capability boundary.
