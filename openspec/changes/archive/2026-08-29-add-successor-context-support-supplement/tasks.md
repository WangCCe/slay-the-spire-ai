## 1. Contracts And Regression Fixtures

- [x] 1.1 Add failing fixtures for exact slice cohorts, lineage collision rejection,
  immutable r2 bindings, and development-only authority.
- [x] 1.2 Add failing merge fixtures covering pair source-row offsets, stable
  partition order, seed isolation, and malformed predecessor or supplement
  rejection.
- [x] 1.3 Add failing support/publication fixtures covering unchanged gate
  delegation, stop-before-fit behavior, round-trip identity, and atomic output.

## 2. Supplemental Collector

- [x] 2.1 Implement the focused supplement registration, validation, source-only
  preflight, and practical identity-scoped recovery boundary.
- [x] 2.2 Implement partition-specific battle-3 train, battle-3 fresh, and
  battle-10 fresh collection by reusing the immutable r2 collector helpers.
- [x] 2.3 Implement complete-corpus concatenation with pair-row offsets and
  deterministic merged fit, calibration, and fresh validation.
- [x] 2.4 Implement merged context-support evaluation, bounded report rendering,
  round-trip checks, manifest hashing, and atomic publication.
- [x] 2.5 Implement the small source-bound deterministic smoke path without
  gameplay, CommunicationMod, model fitting, or production checkpoint loading.

## 3. Focused Verification And Registration

- [x] 3.1 Run focused collector and predecessor regression tests using a fresh
  system-temp pytest child.
- [x] 3.2 Run strict OpenSpec validation and the source-only collision/input
  inventory without native loading.
- [x] 3.3 Run the dedicated native smoke once and verify exact repeat identity and
  all-false downstream authority.
- [x] 3.4 Run exactly one full commit gate for the cohesive source boundary with
  timing report, then commit and push the implementation and formal
  registration.

## 4. Bounded Formal Evidence

- [x] 4.1 Execute the registered formal supplement once with the Windows
  interpreter and fixed CPU, wall-time, storage, cohort, and output bounds.
- [x] 4.2 Verify the published manifest, merged corpus identities, seed
  isolation, unchanged support conditions, and absence of gameplay/training
  authority.
- [x] 4.3 Publish and commit the support decision; if it passes, hand off only to
  a separate weighted-fit change, and if it fails, close without fitting or
  tuning.
- [x] 4.4 Sync and archive the completed OpenSpec change after evidence review.
