## 1. Regression Boundary

- [x] 1.1 Add RED tests for selected identity, exact hierarchical log-probability sums, separate entropy terms, score-greedy ties, permutation behavior, and one-family fallback.
- [x] 1.2 Add RED tests for float32-limit finiteness, backward gradients, metadata, deterministic report reproduction, and runtime import isolation.

## 2. Source-Only Contract

- [x] 2.1 Implement a frozen objective-terms dataclass and strict selected-action adapter over the checked-in action-family distribution.
- [x] 2.2 Implement permutation-stable raw-score and two-stage score-greedy metadata without a joint-probability selection API.
- [x] 2.3 Implement exact metadata and deterministic synthetic design evidence with all authority false and no coefficient or loss surface.

## 3. Verification And Publication

- [x] 3.1 Run focused tests and obtain an independent review of factorization, gradients, tie semantics, and scope isolation.
- [x] 3.2 Publish the deterministic design report and update project direction with the bounded next gate.
- [x] 3.3 Run the repository commit gate once, sync the capability spec, archive the completed change, commit the cohesive change, and push `master`.
