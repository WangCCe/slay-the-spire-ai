## 1. Freeze Evidence And Add RED Boundaries

- [x] 1.1 Publish a source-only preimplementation record that binds the
  consumed experiment, collapse audit, action-family distribution,
  counterfactual audit, hierarchical objective, formal reward, ranker/input,
  adapter, governing specs, and exact unchanged old runner/verifier/test and
  terminal artifact identities.
- [x] 1.2 Add failing runtime tests for family-first then conditional RNG
  replay, stable family/candidate alignment, one-family fallback, selected
  joint-log-probability identity, separate `0.01` entropy terms, finite extreme
  gradients, and raw-score tie failure.
- [x] 1.3 Add failing rollout/training tests for exact API v3 legality, source
  non-mutation, formal reward, atomic chunk rollback, checkpointed two-stage
  generator state, family diagnostics, four-chunk saturation stop, and bounded
  resource accounting.
- [x] 1.4 Add failing control-plane and verifier tests for standard-library
  import isolation, fresh exclusion inventory, immutable registration and
  authorization, pre-seed setup retry, post-seed same-identity resume,
  zero-checkpoint bootstrap recovery, leased resource-prefix reconciliation,
  canary-protected holdout, canonical publication, and all-false downstream
  authority.

## 2. Implement The Hierarchical Runtime

- [x] 2.1 Add the successor-owned runtime dataclasses and deterministic
  state-conditioned model/optimizer/Python-RNG/Torch-generator initialization
  without importing or editing the consumed experiment.
- [x] 2.2 Implement exact API v3 projection and score validation, two-stage
  family/candidate sampling, selected objective-term construction, legal cloned
  action application, formal reward, and source-branch non-mutation.
- [x] 2.3 Implement chunked normalized-return REINFORCE with selected joint log
  probabilities, separate family and expected-conditional entropy terms,
  unchanged gradient clipping, finite-state checks, and atomic rollback.
- [x] 2.4 Implement successor checkpoint encode/decode and restoration for
  model, optimizer, both registered coefficients, Python RNG, Torch generator,
  exact coordinates, and cumulative resource use.
- [x] 2.5 Implement family-aware training rows/chunk summaries, trailing-window
  saturation classification, frozen raw-score evaluation with tie failure,
  exact replay, paired bootstrap evidence, canary gates, holdout gating, and
  bounded terminal verdicts.

## 3. Implement The Standard-Library Control Plane

- [x] 3.1 Add successor-specific canonical JSON, schemas, all-false authority,
  source/runtime/native/isolation binding, contract validation, and CLI parsing
  without importing Torch or native code on source-only paths.
- [x] 3.2 Add tracked fixed-tree seed discovery, explicit exclusion of all
  historical and prior untouched holdout identities, deterministic ascending
  `1024/128/512` cohort materialization, and caller-override rejection.
- [x] 3.3 Add immutable registration and exact authorization validation for the
  two-stage algorithm, split coefficients, gates, resources, output inventory,
  clean pushed commit, and delayed runtime/native loading.
- [x] 3.4 Add Windows lease and journal handling that distinguishes repeatable
  pre-seed setup attempts from one evidence-bearing logical identity, permits
  only unchanged same-identity checkpoint resume, and forbids active-output
  reads while the process is alive.
- [x] 3.5 Add atomic checkpoints, compressed canonical training-row binding,
  immutable bootstrap runtime, monotonic resource ledger,
  evaluation/model/metrics/report publication, artifact manifest, isolation
  comparison, and complete negative/interrupted/holdout terminal bundles.

## 4. Add Independent Verification

- [x] 4.1 Add a standalone standard-library verifier that duplicates no
  runtime imports and validates canonical bytes, source/cohort/native identity,
  checkpoints, coordinates, declared two-stage sampling metadata,
  registered seed order, bootstrap/final-model identity, generator-state hash
  chains, coefficients, diagnostics, resource accounting, holdout access,
  isolation, verdict, and authority.
- [x] 4.2 Add mutation and truncation tests proving the verifier rejects schema,
  hash, source, seed, generator, coefficient, gate, journal, checkpoint,
  terminal-inventory, and verdict drift without Torch/native/environment access.

## 5. Verify And Push The Source-Only Implementation

- [x] 5.1 Turn every RED boundary green and run focused successor, action-family,
  objective, ranker/input, adapter, formal-reward, old-verifier, import-isolation,
  and publication tests with a fresh system-temp basetemp.
- [x] 5.2 Prove the consumed runner, verifier, tests, registration, checkpoints,
  terminal artifacts, prior untouched holdout, CommunicationMod configuration,
  production checkpoints, agent, and `main.py` remain unchanged; do not launch
  gameplay because no live behavior path is modified.
- [x] 5.3 Run `py_compile`, strict change/global OpenSpec validation,
  `git diff --check`, one repository `commit` test gate, and independent
  code/spec/authority review; resolve findings, then commit and push the
  source-only implementation without cohort, native, seed, or training access.

## 6. Register Fresh Evidence Without Execution

- [x] 6.1 From the clean pushed implementation, build and independently replay
  the fixed-tree seed inventory; materialize the exact fresh disjoint train,
  canary, and holdout cohorts without loading native code or constructing an
  environment.
- [x] 6.2 Generate canonical registration and source-only preflight artifacts
  that freeze every algorithm, coefficient, control, gate, cohort, resource,
  native, runtime, isolation, output, and all-false downstream authority term;
  independently review, commit, and push them without execution.
- [x] 6.3 Publish one separately reviewed exact authorization bound to the
  pushed registration, commit and push it, and stop if any registered or
  pre-start identity has drifted.

## 7. Execute One Evidence-Bearing Identity And Close Out

- [x] 7.1 Run repeatable source-only preflight, acquire the lease, validate and
  load native before Torch, then execute at most one evidence-bearing logical
  identity on CPU; monitor process liveness only and follow the registered
  pre-seed retry and same-identity resume rules exactly.
- [x] 7.2 After process exit, run the independent verifier and preserve the
  exact training-collapse, canary-stop, interrupted, resource, invalid, or
  complete-holdout terminal evidence without tuning, seed replacement,
  checkpoint selection, repair, or replacement execution.
- [x] 7.3 Publish a bounded postmortem and project-direction update, sync the
  main spec, archive the change, run final applicable verification, and
  commit/push closeout while formal RL, loading, gameplay, qualification, and
  promotion remain independently gated.
