## 1. Contract And Regression Boundary

- [x] 1.1 Validate the change artifacts strictly and record the frozen input,
  holdout, no-execution, and no-authority boundaries in focused tests.
- [x] 1.2 Add synthetic RED fixtures for manifest/schema rejection, candidate
  probability and entropy summaries, exact saturation boundaries, checkpoint
  continuity, absent initial tensors, and deterministic publication.

## 2. Read-Only Audit Implementation

- [x] 2.1 Implement strict allowlisted source identity, terminal diagnostics,
  training-row, checkpoint, tensor, and final-model validation using only the
  Python standard library.
- [x] 2.2 Implement per-decision and per-chunk candidate/action-family metrics,
  outcome summaries, checkpoint movement, exact first/persistent boundaries,
  and evidence-limit classification.
- [x] 2.3 Implement canonical JSON and JSON-derived Markdown publication with
  source-root output rejection and atomic two-file writes.

## 3. Frozen Evidence Publication

- [x] 3.1 Run the audit once against the tracked consumed terminal bundle,
  reproduce byte-identical outputs, and verify source bytes remain unchanged.
- [x] 3.2 Review the resulting collapse trajectory, document supported and
  unresolved mechanisms, and update project direction without authorizing a
  successor experiment.

## 4. Verification And Closeout

- [x] 4.1 Run focused pytest using a fresh system-temp basetemp, strict OpenSpec
  validation, `git diff --check`, and the repository commit gate.
- [x] 4.2 Sync the new capability to main specs and prepare the completed
  analysis-only change for archive.
