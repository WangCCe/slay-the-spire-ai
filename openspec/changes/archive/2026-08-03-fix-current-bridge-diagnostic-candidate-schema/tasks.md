## 1. Planning And Evidence Freeze

- [x] 1.1 Strict-validate the proposal, design, modified requirement, production candidate boundary, and no-execution authority.
- [x] 1.2 Recheck and record the consumed registration, six canonical output files, closeout, module, verdict, and zero-row result byte identities; stop if any differs from the archived closeout.
- [x] 1.3 Commit and push all planning artifacts before editing the bound runner or focused tests.

## 2. Red Regression And Minimal Fix

- [x] 2.1 Make the shared fake candidate match the exact production validated key set and add red coverage proving candidates omit `action_type` while missing, empty, or non-string Current evaluation `action_type` fails before `step()`.
- [x] 2.2 Remove only the invalid candidate-side `action_type` comparison; preserve unique legal `action_id`, evaluation metadata, hashes, event mapping, transition, fallback, tracker, mutation, support, and verdict checks.
- [x] 2.3 Add a no-native regression that recomputes the committed consumed artifact directory from its frozen registration without changing any registration, artifact, closeout, module, seed, or verdict byte.

## 3. Verification And Direction

- [x] 3.1 Run the focused diagnostic and adjacent bridge/adapter pytest, `py_compile`, `git diff --check`, and strict change validation; do not load the native module or construct an environment.
- [x] 3.2 Run the partitioned repository commit gate without raw unpartitioned full pytest or gameplay validation.
- [x] 3.3 Update `docs/project_direction.md` to record the completed offline source fix while keeping replacement execution, readiness refresh, fresh evidence, gameplay, OPE, model, reward, and training authority false.

## 4. Sync And Archive

- [x] 4.1 Verify consumed evidence byte identity again, sync the modified requirement to the main spec, archive the change, commit and push a tracked-clean `master`, and hand off the exact no-retry boundary.
