## 1. Regression Contract

- [x] 1.1 Add RED regressions for exact API v3 category coverage, separate tensor shapes, state-only changes, candidate permutations, leakage exclusion, determinism, source non-mutation, and malformed input rejection.
- [x] 1.2 Add RED regressions for stable input metadata, integrated state-only ordering reversal, canonical diagnostic rows, and inconsistent diagnostic input rejection.
- [x] 1.3 Add a fresh-process import-isolation regression for native simulator, SpireComm gameplay, Communication Mod, and checkpoint modules.

## 2. Source-Only Implementation

- [x] 2.1 Add the versioned policy-input module and produce finite CPU float32 state and candidate tensors without modifying or calling the r2 combined-feature matrix.
- [x] 2.2 Expose deterministic JSON-compatible identity metadata and construct validated scored-decision rows for the existing diagnostic summarizer.
- [x] 2.3 Keep every empirical, gameplay, training, loading, formal-RL, qualification, and promotion authority false and leave production and experiment imports unchanged.

## 3. Verification

- [x] 3.1 Run focused policy-input, ranker, diagnostics, and adjacent r2 projection pytest with a fresh system-temp basetemp.
- [x] 3.2 Run fresh-process import isolation, Python compilation, `git diff --check`, and strict OpenSpec validation.
- [x] 3.3 Run the unchanged r2 artifact verifier and confirm all registered checks still pass.
- [x] 3.4 Run the repository `commit` test gate once as the routine full test gate; do not launch gameplay because the capability has no production import path.

## 4. Closeout

- [x] 4.1 Record implementation and verification evidence in project direction without changing formal-RL readiness or comparator roles.
- [x] 4.2 Sync the new capability spec, archive the completed change, validate the complete OpenSpec tree, and commit and push the cohesive source-only change.
