## 1. Qualify The Predecessor And Cohort

- [x] 1.1 Re-run the standalone r1 verifier, bind the immutable terminal manifest and controls, and prove the historical artifact directory has no Git diff.
  - Evidence: the standalone verifier accepted `8` artifacts and `173` checks with verdict `experiment_blocked`; manifest SHA-256 is `01fe28cd35c13dcdee305189a27488474edfebfb126ba79aa145ab56d08c8080`, registration is `cf30c2a2e1c10681968ef6f0191e33b272af047c496e811d7ae5aa24205fa452`, authorization is `1c9f336eba2d6baa71e32e1ce882966370a5b6bc86deafa0004e4368304f354a`, and the complete historical path set has no Git diff.
- [x] 1.2 Scan current tracked registration-shaped JSON for every declaration intersecting `50000..51663`; fail closed unless r1 is the sole overlap and is classifiable as `registered_but_unconsumed`.
  - Evidence: a structured scan found `5` tracked registration-schema documents and exactly `2` intersecting physical files: the pushed r1 registration and its byte-identical terminal-output copy, each declaring all `1,664` seeds. They are one logical r1 registration and no second experiment intersects the range.
- [x] 1.3 Confirm r1 recorded zero environments, registered seeds, episodes, optimizer updates, canary outcomes, and holdout outcomes, without loading native code or Torch.
  - Evidence: canonical metrics record zero episodes and zero optimizer updates; the verified eight-artifact inventory contains no training chunk, checkpoint, prefix replay, or evaluation artifact. The bound startup audit locates failure at native import before environment construction or seed access, and all source-only checks completed with Torch and the native module absent from `sys.modules`.
- [x] 1.4 Confirm pushed repair commit `8d123fdf32bd94bc29e53a97f217a2b7ca40c4fe`, working implementation bytes, native module bytes, and physical simulator provenance remain exact.
  - Evidence: `HEAD` and `origin/master` both equal the repair commit; Git and working implementation hashes both equal `9123ff60eb613213149be88a04bee94dece754612eb0a21361992efba3dd851f`, adapter hashes both equal `94a553b6e6757ac96945bdc6ecf45c05bbb333523e4336595a453de070edcb3b`, module SHA-256/size remain `7ac2c750fba6e38d4a023cab72a4d67f158fe7f88414058e5876cef5003fcb88`/`4225024`, and physical simulator hash/count/commit/dirty state plus both submodule commits match r1.

## 2. Publish The All-False Preregistration

- [x] 2.1 Generate a canonical current-source cohort-reuse inventory that binds r1 and records the exact fixed partition, overlap classification, zero-use effects, scan rules, and all-false authority.
  - Evidence: canonical inventory `reports/noncombat_simulator_rl_experiment_20260804_r2_cohort_reuse_inventory.json` is `5,693` bytes with SHA-256 `9664d6961fcb6e713e0f537bc212953b4a0a62236cc3ab015f12036877ea96bd`; it records the unchanged union hash `4cdc47ecf4d3769fdccbb7acd09dd67d2752594db6091a931f707fcf468dc35f`, two physical r1 copies as one allowed logical overlap, zero unclassified overlaps, predecessor bindings, zero-use effects, and all-false authority.
- [x] 2.2 Generate a canonical r2 registration bound to the repaired pushed implementation, unchanged native/simulator and formal-evidence identities, fixed experiment contract, and new reuse inventory.
  - Evidence: canonical registration `reports/noncombat_simulator_rl_experiment_20260804_r2_registration.json` is `31,278` bytes with SHA-256 `8e0576bbf86b2334ccce67ac809410a02dcbfa6419f075211bbe48d0164f8549`; relative to r1 only adapter/source commit, implementation hash, and seed-inventory binding change.
- [x] 2.3 Recompute both artifacts in two independent fresh source-only processes and require byte-identical bytes and digests without native or Torch imports, environment construction, seed access, training, gameplay, CommunicationMod, or checkpoint mutation.
  - Evidence: a fresh generation process and a separate fresh structured recomputation produced the same inventory and registration bytes/digests. Both explicitly finished with Torch and `sts_lightspeed_noncombat_adapter` absent; neither used an execution entrypoint or production checkpoint path.
- [x] 2.4 Prove the r2 authorization and output paths remain absent and that preregistration grants no execution, formal-RL, live, OPE, causal, qualification, loading, or promotion authority.
  - Evidence: both `reports/noncombat_simulator_rl_experiment_20260804_r2_authorization.json` and `reports/noncombat_simulator_rl_experiment_20260804_r2` are absent, and both generated artifacts contain the complete all-false authority map.

## 3. Verify And Publish The Decision

- [x] 3.1 Validate canonical schemas, bindings, source hashes, cohort counts/ranges/hash, predecessor evidence, and the exact proposed r2 identity and output path in fresh processes.
  - Evidence: the independent validator accepted the closed registration schema, recomputed every predecessor binding and overlap row, reproduced all train/canary/holdout summaries, matched current Git and working source hashes, and confirmed proposed identity `noncombat-simulator-rl-20260804-r2` and output `reports/noncombat_simulator_rl_experiment_20260804_r2` only in the non-authorizing reuse decision.
- [x] 3.2 Run strict validation of this change and the complete OpenSpec tree plus `git diff --check`; do not run full pytest or gameplay because no Python, native, simulator, or production entrypoint changes.
  - Evidence: strict validation passed the active change and all `64` active OpenSpec items, and `git diff --check` passed. Full pytest and gameplay were intentionally omitted because the diff contains only canonical reports, planning/specification, and direction documentation.
- [x] 3.3 Update project direction with the pushed preregistration and preserve the separate exact execution-authorization gate.
  - Evidence: project direction records both canonical digests, the single logical overlap, repaired source identity, proposed r2 identity/output, all-false authority, absent controls/output, and the requirement to commit and push a later exact authorization before full source-only preflight.
- [x] 3.4 Sync the accepted delta, archive the completed change, commit and push only scoped files, and preserve r1 plus unrelated untracked artifacts.
  - Evidence: archive applied the single added requirement to the main spec, moved the change to `2026-08-04-preregister-noncombat-simulator-rl-r2`, and post-archive strict validation passed all `63` active OpenSpec items. The scoped commit and push publish only these files while path checks preserve r1 and unrelated artifacts.
