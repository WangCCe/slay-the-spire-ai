## 1. Regression And Arithmetic Contract

- [x] 1.1 Add focused red tests for binomial boundary cases, fixed-budget pass probabilities, and deterministic required-rate thresholds.
- [x] 1.2 Add focused red tests that derive raw and target-supported victories from exact trajectory identities and rational weight numerators, including a raw zero-weight victory.
- [x] 1.3 Add focused red tests for source comparability, provenance failures, duplicate-key rejection, atomic output behavior, deterministic reruns, and all-false downstream authority.

## 2. Offline Feasibility Analyzer

- [x] 2.1 Implement strict canonical input loading and registration/readiness artifact binding in `analysis_scripts/noncombat_study_feasibility.py`.
- [x] 2.2 Implement supported-victory derivation, deterministic binomial operating characteristics, required-rate bisection, sensitivity calculations, and planning-only feasibility classification.
- [x] 2.3 Implement deterministic JSON and Markdown rendering plus a CLI that validates both outputs before atomically publishing either artifact, with explicit LF checkout bindings for exact sources and outputs.

## 3. Frozen Current Audit

- [x] 3.1 Add a canonical source manifest binding the current v2 registration and frozen B3-B7 deterministic-Current readiness artifact.
- [x] 3.2 Generate deterministic JSON and Markdown reports and verify that the current evidence reports `not_demonstrated` with raw and supported victories kept separate.
- [x] 3.3 Update project direction and active study planning artifacts to keep r9, replacement qualification, study start, gameplay collection, OPE reinterpretation, training, and promotion blocked.

## 4. Verification And Closeout

- [x] 4.1 Run focused feasibility tests and relevant existing outcome-evidence/readiness regressions without launching gameplay.
- [x] 4.2 Run the registered commit gate, strict OpenSpec validation, scoped diff review, and read-only target-process and historical-artifact checks.
- [x] 4.3 Sync the accepted delta specs, archive the completed change, commit only scoped files, and push `master`.
