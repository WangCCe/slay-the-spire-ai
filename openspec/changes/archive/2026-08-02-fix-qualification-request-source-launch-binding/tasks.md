## 1. Evidence And Regression

- [x] 1.1 Record the frozen r8 request, published command, bootstrap stage, and offline replay evidence in a read-only diagnosis report without changing historical artifacts.
- [x] 1.2 Add a red regression proving an r8-shaped qualifier suffix that uses the active request path is rejected while the reviewed source path is accepted.

## 2. Canonical Launch Binding

- [x] 2.1 Implement one canonical qualifier suffix builder and exact validator that bind registration, request source, request anchors, and R.
- [x] 2.2 Route the production-Python no-action smoke command construction through the canonical builder without changing its live protocol behavior.

## 3. Verification And Closeout

- [x] 3.1 Run focused qualification regressions with the Windows production Python and a writable fixed basetemp.
- [x] 3.2 Run strict OpenSpec validation, `git diff --check`, and the registered commit gate instead of the unregistered raw full suite.
- [x] 3.3 Confirm no game, Java, live/external qualification identity, study, or training process was started and r8 historical/root hashes remain unchanged; isolated temporary-directory smoke children are permitted test fixtures.
- [x] 3.4 Update the durable project direction, sync the delta specs, archive the completed change, and commit and push one cohesive source-fix change.
