## 1. Freeze The Registration Plan

- [x] 1.1 Validate the complete proposal, design, and delta spec strictly; review the exact identities, authority boundary, rollback rule, and source-only scope.
- [x] 1.2 Commit and push only the OpenSpec planning artifacts so the registration procedure is reviewable before evidence publication.

## 2. Reobserve Exact Inputs

- [x] 2.1 Verify `origin/master` ancestry and tracked source bytes for readiness source `ffd9acc444258483d172529eccfe8ccb05c9bb9b` and publication `0c62aff2fb301dd491017bbf2e775c36a177bf67`.
- [x] 2.2 Revalidate the exact r5 report, candidate, verification receipt, readiness identity, 512-seed schedule, zero-collision result, all-false authority, and proposal eligibility from immutable Git bytes.
- [x] 2.3 Reobserve source inventory, Windows CPU runtime metadata, native module and provenance bytes, CommunicationMod config, complete production-checkpoint tree, and target-output absence without importing Torch/native modules or changing external state; leave delegation and revocation evaluation to the later approval boundary.

## 3. Build And Review Registration

- [x] 3.1 Build one canonical compact v2 registration for `noncombat-cross-fitted-hierarchical-learning-successor-20260808-r2` in an additive staging path using the exact registered output root and all-false authority.
- [x] 3.2 Validate the candidate with the producer and independent standard-library verifier paths; verify canonical byte identity, source/readiness/schedule/native/runtime/isolation/resource bindings, and no newly imported Torch/native module.
- [x] 3.3 Publish only the canonical registration and deterministic review after every check passes; assert the registered output root remains absent and no execution request, approval, authorization, journal, checkpoint, terminal, gameplay, or empirical artifact was created.

## 4. Verify And Close

- [x] 4.1 Run focused compact-registration/control/verifier regressions, canonical artifact checks, isolated import probes, and strict OpenSpec validation with scoped pytest temp storage.
- [x] 4.2 If implementation or test source changed, run the repository commit gate once and record its result; otherwise record full pytest and fresh gameplay validation as not applicable to this evidence-only change and do not rerun the known long gate.
- [x] 4.3 Obtain an independent bounded review of the exact artifact and authority scope, resolving only registration-blocking findings without changing the registered algorithm, cohort, or limits.
- [x] 4.4 Verify the exact staged scope, commit and push registration evidence while the change remains active, confirm the exact registration is on `origin/master`, and preserve request, delegated approval, authorization, native loading, seed access, fitting, training, evaluation, and execution for a later separate change.
- [x] 4.5 After 4.4 is true, sync the delta requirement, update project direction, archive the completed change, and verify the final closeout scope for a separate commit and push.
