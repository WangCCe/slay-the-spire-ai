## 1. Regression Boundary

- [x] 1.1 Add source-only fixtures for a canonical terminal audit, exact scored-row artifacts, multi-family card-reward/shop rows, and single-family event/route rows.
- [x] 1.2 Add RED regressions for source identity, containment, canonical JSON, holdout, row alignment, authority, helper metadata, tie, entropy, and atomic-publication boundaries.

## 2. Read-Only Audit

- [x] 2.1 Implement strict trust-root and two-artifact validation without checkpoint, seed, replay, native, simulator, or holdout-row access.
- [x] 2.2 Implement per-phase and per-category flat versus max-pooled family mass, entropy, fallback, tie, and argmax-transition metrics using the checked-in distribution helper.
- [x] 2.3 Implement canonical JSON and deterministic Markdown rendering with per-file atomic publication, handled-failure rollback, and exact all-false authority.

## 3. Evidence And Documentation

- [x] 3.1 Run the canonical audit over the hash-bound frozen rows and inspect the resulting card-reward, shop, event, and route evidence.
- [x] 3.2 Record the bounded conclusion and next decision gate in project-direction documentation without authorizing training or deterministic selection semantics.

## 4. Verification And Publication

- [x] 4.1 Run focused pytest, byte-identical reproduction, and strict OpenSpec validation.
- [x] 4.2 Obtain an independent code and evidence review, address actionable findings, and rerun focused verification.
- [x] 4.3 Run the repository commit gate, sync the capability spec, archive the completed change, commit the cohesive change, and push `master`.
