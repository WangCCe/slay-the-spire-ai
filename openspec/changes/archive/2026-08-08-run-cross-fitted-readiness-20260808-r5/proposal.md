## Why

Delegated-approval source commit `c977a877fd319ff8bdfe6cb2145c1f9d8cdd3f3d`
changes the successor control plane, independent verifier, and canonical
contract, so the verified r4 readiness `go` is historical evidence rather than
registration eligibility for the new source. One separately source-bound r5
readiness audit is required before any successor registration can be proposed.

Live evidence is the canonical r4 publication and independent verification with
readiness identity
`6723364a5a31c177bad0a4e8980bfbc6ff74914d595a506d434387c9bbcc3b8f`,
plus the pushed delegated-approval commit above. Success is one independently
verified canonical r5 publication or terminal receipt with every empirical and
downstream authority false. A verified `go` permits only a later registration
proposal.

## What Changes

- Preregister exactly one source-only audit named
  `noncombat-cross-fitted-empirical-successor-readiness-20260808-r5` with fixed
  output `reports/noncombat_cross_fitted_empirical_successor_readiness_20260808_r5`
  and scratch `.source_only_readiness_scratch_20260808_r5`.
- Require the final source to be the pushed, tracked-clean planning commit that
  descends from `c977a877fd319ff8bdfe6cb2145c1f9d8cdd3f3d`, contains the
  complete synced r5 plan, and has no source-keyed readiness attempt.
- Preserve the consumed r1 successor evidence and all prior readiness evidence.
  Replay the existing canonical r1-r3 source/evidence bindings, then bind the
  exact r4 canonical publication, readiness identity, started/verified receipts,
  and closeout without treating r4 candidate seeds as empirical accesses.
- Replace a second long copy-pasted authorization tuple with the maintainer's
  recorded solo-repository standing delegation plus the exact pushed r5 plan.
  The plan fixes interpreter, isolated mode, script, cwd, audit id, paths,
  ceilings, all-false maps, at-most-once claim, and no-retry rule. A later
  explicit user revocation in the current Codex task stops plan publication or
  invocation. User messages are ordered by the host task sequence and the
  latest explicit instruction controls. Host, operating-system, network, and
  credential approvals remain mandatory.
- Immediately before invocation, recheck exact source, ancestry, clean tracked
  status, plan diff, fixed evidence, path absence, command, and process absence.
  Any mismatch stops without changing source, id, path, seed, threshold, or
  ceiling.
- Invoke the existing auditor at most once and independently verify only after
  true process exit. Do not modify the auditor, verifier, readiness algorithm,
  or successor algorithm in this change.
- Do not load native, runtime, Torch, model, checkpoint, game, or
  CommunicationMod code; construct an environment; access empirical outcomes;
  fit; train; evaluate; run OPE; register or execute a successor; qualify; or
  promote.

Before a started receipt exists, rollback means abandoning the r5 plan and
removing only its unclaimed paths. After the source-keyed attempt is claimed,
there is no rollback, retry, resume, repair in place, or alternate r5 identity;
only terminal preservation and a later source proposal are valid.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `noncombat-cross-fitted-empirical-successor-readiness`: Add the exact
  delegated-source r5 one-shot identity, immutable r4 evidence boundary,
  standing-delegation orchestration gate, preflight, terminal verification,
  and all-false non-authority contract.

## Impact

The change affects only OpenSpec planning, source-control preflight, the
existing standard-library readiness auditor and verifier, source-keyed attempt
receipts, and later readiness closeout artifacts. It changes no gameplay,
policy, estimator, reward, native adapter, runtime, training, checkpoint,
production configuration, or delegated-approval schema.
