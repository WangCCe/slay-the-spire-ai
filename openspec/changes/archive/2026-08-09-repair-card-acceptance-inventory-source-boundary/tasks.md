## 1. RED Source-Boundary Coverage

- [x] 1.1 Add failing regressions for the exact r1-offending readiness staging path and representative direct staging, scratch, sealed, temporary, tmp, and attempts roots, proving classification occurs before Git blob access.
- [x] 1.2 Add failing or preserving regressions proving ordinary files, nested token-named directories, and names that merely contain generated-root words remain eligible and malformed ordinary evidence still fails closed.

## 2. Minimal Classifier Repair

- [x] 2.1 Implement direct-`reports/` generated-root classification without changing seed extraction, cohort selection, authority, source identity, or publication behavior.
- [x] 2.2 Verify excluded-root kind/path ordering remains deterministic and current candidate-output-specific exclusions remain exact.

## 3. Verification And Closeout

- [x] 3.1 Run the focused classifier nodes and the complete card-acceptance seed-inventory pytest file with the registered Windows pytest temp convention.
- [x] 3.2 Run compile/import-isolation checks, strict OpenSpec validation, diff checks, and an independent code/spec/failure review; resolve every actionable finding.
- [x] 3.3 Publish a bounded repair report that binds the consumed r1 failure, records no retry and no gameplay applicability, and gives a separate future-identity go/no-go without granting execution authority.
- [x] 3.4 Sync and archive the repair change, strict-validate the global OpenSpec set, commit, and push the cohesive source-only closeout without running inventory, native, model, training, evaluation, or gameplay operations.
