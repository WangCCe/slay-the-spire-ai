## 1. Contracts And Source Closure

- [x] 1.1 Add fail-closed registration validation for corpus, lineage,
  implementation/runtime, external source, local projection, limits, signature,
  suitability, verdict, and all-false authority identities.
- [x] 1.2 Add bounded C++ brace/array/table extraction for the exact route and
  card-reward source blocks, map weights, card priorities, and copy limits.
- [x] 1.3 Add the versioned dependency inventory and representation-coverage
  classifier, including hidden map path, policy constants, and adapter action
  mapping.
- [x] 1.4 Add synthetic regressions for missing/mutated source anchors, physical
  identity drift, unregistered cohort/seed/category input, and authority drift.

## 2. Teacher Reconstruction

- [x] 2.1 Implement the source-faithful route dynamic program, cached path
  indexing, strict tie behavior, and action-id mapping.
- [x] 2.2 Implement the source-faithful card-reward offer counting, parsed
  priority/copy tables, upgrade/tie behavior, and skip/bowl action mapping.
- [x] 2.3 Add positive and adversarial synthetic regressions for route ties,
  cached-path derivation, duplicate card priorities, offer-count copy limits,
  upgraded cards, default priorities, and semantic duplicate actions.
- [x] 2.4 Require exact reconstruction of every preserved route/card label and
  publish row-level mismatches without silently adapting source semantics.

## 3. Representation And Teacher Audit

- [x] 3.1 Implement exact teacher-source, adapter-observable,
  legacy-hash-1024, and structured-hash-2048 decision/candidate signatures.
- [x] 3.2 Materialize repeated/conflicting decision groups, candidate aliases,
  indistinguishable targets, pairwise preference contradictions, and semantic
  action equivalence by category and representation.
- [x] 3.3 Implement the fixed critical teacher-suitability checks and ordered
  blocked/adapter-gap/teacher-unsuitable/inconclusive classifier.
- [x] 3.4 Add synthetic regressions covering each metric, projection-only loss,
  actionable raw adapter gaps, every terminal verdict, deterministic ordering,
  finite values, and hard row/candidate/time bounds.

## 4. Hash-Closed Publication

- [x] 4.1 Add atomic canonical publication for configuration, source facts,
  dependency coverage, row evidence, metrics, suitability, report, Markdown,
  manifest, and one optional noncanonical timing journal.
- [x] 4.2 Add a strict standalone validator that recomputes canonical bytes,
  verdict, authority, bounds, hashes, and exact managed inventory.
- [x] 4.3 Run focused tests, Python compilation, strict OpenSpec validation, and
  the registered pytest commit gate; fix only audit contract defects.

## 5. Frozen Registration And Closeout

- [x] 5.1 Commit and push implementation plus OpenSpec artifacts, then check in
  one immutable registration binding that commit and every allowed input.
- [x] 5.2 Verify and push the registration before executing one canonical audit;
  do not alter keys, criteria, source interpretation, or inputs after result.
- [x] 5.3 Execute the registered audit once, strictly recompute it, and perform a
  read-only manual review of source closure, aliases, suitability, authority,
  bounds, and inventory.
- [x] 5.4 Update project direction with the terminal next-step verdict, run
  focused and commit-gate verification without gameplay, sync/archive the
  change, commit only scoped files, and push `master`.

## Recovered Registered Attempt

The v1 registered execution on 2026-08-02 failed closed after the audit body
exceeded its fixed 120-second wall-time bound. No canonical output was
published and that registration remains consumed. A separate runtime-recovery
OpenSpec and fresh v2 registration preserved the contract, published in 34.938
seconds, and strictly recomputed the terminal
`simpleagent_unsuitable_as_policy_quality_gate` result. Task 5.4 remains open
only for bounded verification, spec sync/archive, commit, and push.
