## 1. Freeze And Review The Contract

- [x] 1.1 Strict-validate the complete proposal, design, delta spec, and tasks; independently review the planning-only change, resolve every actionable finding, then commit and push it before source or test edits.

## 2. Add Red Regressions

- [x] 2.1 Add control-plane regressions for the exact `inventory-verification` stage, read-only operation/resource maps, six build-evidence prerequisites, canonical request/authorization/approval/launch validation, and cross-stage or broadened-authority rejection.
- [x] 2.2 Add seed-inventory regressions proving the legacy r4 verification command, direct API build-only/missing-authority calls, and every missing, stale, predecessor, noncanonical, mismatched, or unknown-field verification artifact fail before operation dispatch, receipt, inventory, registered Git blob, seed, or cohort access.
- [x] 2.3 Add receipt regressions for exclusive creation order, canonical write/flush/fsync binding, empty/partial/invalid/complete collision, interrupted publication, post-receipt terminal failure, and duplicate invocation rejection.
- [x] 2.4 Add exact-success and compatibility regressions for one synthetic source-only reconstruction, distinct bounded verification completion, post-receipt stdout failure, unchanged build CLI completion, and no cohort materialization or downstream authority.

## 3. Implement Production Enforcement

- [x] 3.1 Add `inventory-verification` to the shared stage control plane with exact prerequisites, fixed ceilings, standing-delegation-compatible request/authorization/approval/launch validation, and disjoint read-only authority.
- [x] 3.2 Add verification authority normalization and immutable request-bound execution receipts to the seed-inventory module, validating declared source/build/inventory bindings and pre-access identities before receipt creation, then treating materialized inventory drift as post-receipt terminal evidence.
- [x] 3.3 Split CLI argument handling and completion rendering so build remains compatible while verification requires its distinct four-artifact authority and emits a bounded operation-specific completion.
- [x] 3.4 Confirm the standalone verifier needs no update because the execution receipt is control-plane evidence and the compact inventory schema remains unchanged.

## 4. Verify And Close

- [x] 4.1 Run complete owning control, seed-inventory, and standalone-verifier pytest files under a fresh Windows system-temp basetemp; record fresh gameplay validation as not applicable (`265 passed`; source-only control change).
- [x] 4.2 Perform a static-only independent review that is explicitly prohibited from executing inventory commands or tests; resolve every actionable finding and rerun focused tests if source changes.
- [x] 4.3 Run the repository full pytest gate once, strict-validate all OpenSpec items, compile changed Python files, and verify tracked diff/path hygiene without reading r3 inventory content or running r4/r5 verification (`5803 passed, 18 skipped`; `84/84` OpenSpec items valid).
- [x] 4.4 Commit and push one coherent hardening boundary, sync the modified main spec, archive this completed change, strict-validate globally, and leave parent tasks 6.2/6.3 plus every r5/training/downstream authority unchanged.
