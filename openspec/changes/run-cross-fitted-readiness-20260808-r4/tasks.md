## 1. Finalize The Source Identity

- [x] 1.1 Reconfirm repair commit `479f5536ca21e2abd543f33f970bef93103ba0d8` is pushed, r1/r2 and consumed r3 evidence remain immutable, and every registered r4 attempt, output, scratch, staging, and sealed path is absent.
- [x] 1.2 Sync the r4 delta to the canonical readiness spec and strictly validate the change and affected capability.
- [x] 1.3 Run focused source-only planning and preservation checks and obtain independent read-only review of the exact command, paths, repair ancestry, evidence bindings, one-shot rule, exact-approval gate, and all-false authority; do not run the repository commit gate again for planning-only bytes and do not launch gameplay.
- [x] 1.4 Commit and push the exact six-row r4 plan, then require exact `HEAD == origin/master`, clean tracked status, repair ancestry, and the registered no-renames diff before naming the final source identity.

## 2. One-Shot Source-Only Publication

- [ ] 2.1 Immediately before authorization and invocation, recheck exact `HEAD == origin/master`, repair ancestry, clean tracked status, the canonical six-row no-renames diff, every bound input, all preserved r1/r2 and consumed r3 evidence, and absence of the source-keyed r4 attempt, output, scratch, staging, and sealed paths.
- [ ] 2.2 Present one exact human authorization request naming the final source commit; interpreter, `-I`, absolute script, repository root and cwd, and complete command; audit id; absolute output, scratch, source-keyed attempt, staging, and sealed derivations; 7,200-second outer wait; exact internal ceilings and sizes; exact all-false maps; at-most-once claim; and no-retry boundary. Stop without invoking until that complete tuple is explicitly approved.
- [ ] 2.3 Only after exact approval, invoke the registered r4 auditor command at most once, keep every registered limit unchanged, and use a source-specific Windows process query to prove the auditor and every observed descendant absent before inspecting any attempt, output, scratch, staging, sealed, or receipt path.
- [ ] 2.4 Independently verify the canonical publication or terminal receipts, recompute source and receipt identities and all-false authority, and confirm no native, runtime, model, empirical outcome, training, evaluation, game, or CommunicationMod operation occurred.

## 3. Terminal Closeout

- [ ] 3.1 Publish the r4 readiness decision and eligibility boundary in project direction without creating a successor registration or execution request.
- [ ] 3.2 Sync and archive the completed r4 change, run final source-only checks, then commit and push terminal evidence without retrying the consumed source identity.
