## 1. Baseline And Correctness Repair

- [ ] 1.1 Record the 2026-08-27 raw-full and commit-profile counts, durations, failure clusters, slow-test evidence, invalidated qualification, and unchanged authority in a repository report.
- [ ] 1.2 Add a focused Reaper regression fixture repair that supplies the complete current healing state without changing simulator behavior, then run its narrow test node.
- [ ] 1.3 Audit the current-baseline replication source binding against the current bound files; rebind only after proving the intended historical semantic surface is unchanged, then run the complete focused replication file.
- [ ] 1.4 Audit reachable event-semantics Current-policy AST drift against the bound contract; update identities only after proving registered event behavior is unchanged, then run the reachable semantics and bridge-focused files.

## 2. Timing Evidence

- [ ] 2.1 Add RED runner regressions for timing-report path validation, default argv compatibility, telemetry selection equivalence, deterministic per-file aggregation, nonzero pytest result preservation, and no retry.
- [ ] 2.2 Implement opt-in run-scoped legacy JUnit capture and deterministic JSON timing publication in `scripts/run_test_gate.py` without changing default profile argv or return codes.
- [ ] 2.3 Document timing-report usage and update the routine validation table so narrow coherent changes use focused coverage plus `commit`, while full remains an explicit complete boundary.

## 3. Frozen Boundary

- [ ] 3.1 Run focused runner and three repaired-cluster tests with isolated Windows basetemps; stop if any correctness failure remains.
- [ ] 3.2 Run exactly one post-repair `commit` profiling invocation with machine-readable timing evidence and no retry, preserving any failure or slow result.
- [ ] 3.3 Aggregate whole-file timings, select only material measured candidates with explicit source ownership and rationales, and freeze the exact replacement `full_only` set before qualification.
- [ ] 3.4 Add manifest and command-construction regressions for the frozen set, update the profiling report, and prove the inclusive `full` profile command remains unchanged.

## 4. One-Shot Qualification

- [ ] 4.1 Run Python compilation, runner-focused tests, repaired-cluster tests, strict OpenSpec validation, and scoped diff review.
- [ ] 4.2 Run the frozen `commit` profile exactly once on `D:\anaconda\envs\stsai\python.exe`; do not retry, tune exclusions, or reinterpret a failed or over-300-second result.
- [ ] 4.3 Record final counts, pytest and runner durations, exact boundary, result, and authority; qualify correctness and timing only if the one-shot gate passes within 300 seconds.
- [ ] 4.4 Do not rerun raw full pytest solely for selection-equivalent telemetry; retain the 2026-08-27 full result as the complete-boundary baseline and verify the generated full command has no exclusions.
- [ ] 4.5 Do not start Slay the Spire, CommunicationMod, native modules, simulator fitting, or RL training; sync and archive only after all reachable tasks and evidence are complete.
