## 1. Evidence Baseline And Red Regressions

- [x] 1.1 Independently replay and preserve r3 failure self-hash `e495ce302f0ddf9628962e0d4147614a0cf9b9c7c010f256662a98eae76b033d`, file SHA-256 `5a3c47f5b93d7c1f66b5de6c32d3af139188b60735fa36733c6b3c6ee772cfec`, final inventory `2a63cf3b7505ebf6d9e2f605eade7deec3c0afdaaa4da90ca6a99b517c82cb16`, valid attempt/ready binding, absent release, restored isolation, zero surviving processes, and the no-retry/no-start verdict; describe only the independently supported release-side orchestration failure.
- [x] 1.2 Add strict canonical qualification-request fixtures and red tests for duplicate keys, non-canonical bytes, self-hash mismatch, source/registration/implementation/command/config/marker mismatch, stale control artifacts, forbidden study/gameplay artifacts, and a previously consumed root.
- [x] 1.3 Add red runner regressions proving attempt publication precedes the only child start, a valid ready created immediately by the owned process starter is accepted on the first poll, release follows ready validation, and the child exits at the bound success boundary without a retry.
- [x] 1.4 Add red main/runtime regressions proving a valid attempt-hash token exits only after release and before exploration/agent construction, malformed/missing/mismatched bindings fail closed, ordinary startup is unchanged, and `run-next` rejects ambient qualification environment before attempt publication.
- [x] 1.5 Add red independent-verifier tests for canonical completion/failure replay plus request, attempt, ready, release, PID, launch-count, result-branch, self-hash, forbidden-artifact, and authority tampering.

## 2. Canonical Qualification Evidence

- [x] 2.1 Implement strict request building/loading with a versioned exact schema, a bound reviewed-source path and sorted review allowlist, duplicate-key and JSON-constant rejection, canonical LF bytes, self-hash replay, absolute paths, lexical no-follow component checks before any resolution/read/hash, one pinned trusted absolute Git executable for every qualification Git call, a fixed stdlib-only `-I -S` launcher encoded as one CommunicationMod-safe whitespace-free token that no-follow hashes an externally anchored runner and executes only validated in-memory bytes, runner command/anchor rechecks, a pure-stdlib pre-import bootstrap that extracts exactly one external R, proves `HEAD == R`, and hashes tracked executable/importable worktree bytes against R tree blobs without trusting index stat data with one shared fail-closed inert-format predicate plus guarded fixed-`core.autocrlf=true` `git hash-object --path --stdin` canonicalization and only R-tracked raw-byte-identical inert text/eol attributes, implementation snapshot S plus direct-child review commit R, external request/file/size/R anchors, exact S-to-R diff, clean tracked source with only ordinary `H` index rows and no untracked or ignored executable/importable paths, symlinks, Windows reparse points, unsafe Git attributes/filters, or partial-clone/promisor/extension helpers, parent/child bytecode writes and Git lazy fetch disabled, fail-closed Git/no-follow traversal diagnostics, unchanged registration/implementation bytes across S/R/worktree, exact qualification command derived from registered executable/main/arguments with fixed `-I -S`, configuration hash, marker boundary, handshake contract, and forbidden-output bindings.
- [x] 2.2 Implement exclusive canonical completion and failure records that bind the request, canonical self-hashed review binding, source, registration, implementation map, command, configuration, internally consistent launch/PID/attempt/ready/release evidence, cleanup and lstat-based forbidden-artifact observations, and uniformly false study/training/policy authority.
- [x] 2.3 Exclusively publish reviewed source bytes as the active request before attempt or child launch; distinguish reviewed-but-unconsumed source from active/control consumption, reject collisions without child launch, and never overwrite, delete, retry, replace, or reinterpret a root.

## 3. Qualification Child Exit Boundary

- [x] 3.1 Add the dedicated qualification attempt-hash environment contract in `main.py` and validate that it is one lowercase SHA-256 value equal to the loaded handshake attempt after release consumption.
- [x] 3.2 Exit zero through an explicit pre-agent qualification boundary before exploration initialization, agent construction, callback registration, input-state processing, or gameplay; keep the handshake implementation and its 120/10 deadlines unchanged.
- [x] 3.3 Preserve ordinary gameplay and registered study startup, and make `run-next` reject ambient qualification environment before creating an attempt or child.

## 4. Owner-Controlled Qualifier

- [x] 4.1 Add `qualify --registration ... --request ... --request-hash ... --request-file-sha256 ... --request-size ... --review-commit ...` to the existing runner behind the exact externally anchored stdlib launcher, interpret `--request` as the committed reviewed source, and keep CLI result/diagnostics on stderr while preserving CommunicationMod stdin/stdout for the exact isolated child derived from the registered command.
- [x] 4.2 Implement preflight, exclusive attempt publication, one exact inherited-stdio child launch with bytecode writes disabled, immediate-ready acceptance, PID/binding and live pre-release isolation checks under one deadline derived from the ready timestamp and propagated through Git metadata traversal/subprocesses plus chunked root-inventory hashing, rechecked across every boundary including immediately before release without repeating immutable S/R replay, exclusive release publication, bound zero-exit wait, and post-exit isolation checks without reading live logs.
- [x] 4.3 On timeout, early exit, malformed evidence, boundary drift, release failure, nonzero or invalid exit, or cleanup failure, terminate the owned child, publish at most one failure record, leave every study/gameplay output absent, and return a nonzero qualifier status without retry; after release/zero exit, preserve post-exit-validation and completion-publication failures as partial prefixes instead of laundering structurally successful evidence into failure.
- [x] 4.4 Add CLI/direct-script tests proving qualification output cannot contaminate CommunicationMod stdout and no qualifier path can call `start`, initialize a ledger, or claim a registered slot.

## 5. Independent Qualification Replay

- [x] 5.1 Add a qualification replay path to `analysis_scripts/verify_noncombat_outcome_evidence_expansion.py` without importing runner request/result builders or ordinary audit helpers before qualification checks; require exact non-abbreviated qualification options and Python `-I -S`; accept external R/request/file/size anchors plus independently preserved terminal result-hash/file-SHA/size anchors, use one pinned trusted absolute Git executable, lexically reject verifier input reparse components before resolution/read/hash, read S/R Git blobs independently of current HEAD/worktree, reconstruct the canonical review binding, bind terminal anchors into the audit, publish optional audit output only through canonical-parent-aware no-follow exclusive creation outside the qualification root and every request-bound/forbidden path, and validate the direct-parent/exact-diff chain, active request and terminal canonical bytes, hashes, exact JSON types, lifecycle ordering, branch semantics, one-launch evidence, forbidden artifacts, and authority.
- [x] 5.2 Add direct-script and CLI tests for successful, failed, post-exit-validation partial sealing, rejection of unanchored or mismatched terminal evidence and success-looking failed terminals including forged launch counts and missing attempt evidence, reviewed-prepared, consumed-invalid, and contiguous request/attempt/ready/release partial-prefix replay; require full preflight before `reviewed_prepared` and exact active bytes for terminal replay; cover historical replay after current-HEAD drift or removal of the current request parent, external-anchor and review-chain drift, pinned absolute Git invocation, lexical root/source/result junction aliases, missing/non-regular/malformed active paths, real Windows junctions, no-follow dangling forbidden/control entries, non-regular/orphan controls, duplicate-key/non-finite JSON rejection, tampering, gaps, terminal collisions, and explicit refusal to promote any qualifier evidence into study, run-lock, collection, policy, causal, or training authorization.

## 6. Verification And Source Commit

- [x] 6.1 Run focused Windows pytest for runner, main runtime errors, study handshake, independent verifier, and committed registration review using `D:\anaconda\envs\stsai\python.exe`, `-p no:cacheprovider`, and a fresh writable repository basetemp; resolve only failures attributable to this change.
- [x] 6.2 Run the full Windows pytest suite with cache disabled and a fresh writable basetemp. If the failed r3 candidate's current-byte review guard detects deliberate implementation drift, preserve its historical rows and add an explicit historical-versus-active binding distinction rather than rewriting r3 as current or weakening digest validation.
- [x] 6.3 Run compile/import checks for runner, main, verifier, and unchanged handshake; run `openspec validate --all --strict`, `git diff --check`, registration render comparison, and scans proving no handshake deadline/schema, behavior rate, seed, schedule, estimator, threshold, policy, checkpoint, training, Java, or gameplay change entered the diff.
- [x] 6.4 Obtain an independent read-only code and governance review, fix every Critical or Important finding with a red regression, rerun focused and full verification, and require an explicit ready-to-commit verdict.
- [x] 6.5 Commit and push the orchestrator implementation as a separate source-fix commit while the v2 study, r4 identity, run lock, collection, and training remain blocked.

## 7. Binding Refresh And Live Handoff

- [x] 7.1 After the source-fix commit, canonically re-render the v2 registration and compare bytes/hash with `7df8036e111fb55ece15154796d494ea857a74984c9d1a224c2b61f8fc710ace`; preserve the current registration as historical if any byte changes, and in all cases regenerate and independently review source plus implementation bindings.
- [x] 7.2 Prepare the separate tracked study amendment, preserve r1/r2/r3 and the r3 evidence limitation, create r4 with static config only, and generate the source-bound request anchors. Stop before direct-child R or launch when independent review finds that request v1 does not bind broad isolation; preserve the request and r4 as obsolete, unconsumed, and non-authorizing.
- [ ] 7.3 Complete `bind-qualification-isolation-evidence` as a separate source repair, then use a later amendment to bind fresh S, regenerated registration/implementation hashes, request v2, direct-child R, and a previously absent replacement root.
- [ ] 7.4 Run the replacement root exactly once through the tracked qualifier using Windows production Python and real CommunicationMod. Require request-bound and independently recollected CommunicationMod/marker/run/checkpoint/log isolation, attempt/ready/release/zero-exit replay, no study/gameplay artifact, no surviving child, and independent attestation before any later `start` decision.
