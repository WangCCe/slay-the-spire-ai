## Why

The compact 20260808-r2 registration is now exact on `origin/master`, but it
intentionally grants no execution authority. The next irreversible boundary is
to render and review the one exact request digest that a later delegated
approval and tracked authorization may bind.

## What Changes

- Revalidate the pushed 17,595-byte registration at
  `reports/noncombat_cross_fitted_hierarchical_learning_successor_20260808_r2_registration.json`
  with SHA-256
  `9d792cadbece4ea21768386904633ebded2e94525fb186bdcbf4a4d7729dbdf9`
  and its immutable r5 readiness evidence before request rendering.
- Render one canonical execution-request v1 for registration identity
  `noncombat-cross-fitted-hierarchical-learning-successor-20260808-r2`, binding
  the exact native/runtime/source/output/schedule/resource/resume terms and
  explicit requested operations without changing any term.
- Publish the request plus a deterministic source-only review that records its
  canonical digest, exact registration binding, independent structural
  validation, import isolation, and absence of approval, authorization, output,
  or empirical work.
- Treat success as one pushed exact request that is eligible only for a later
  separate standing-delegation resolution and authorization proposal. The
  request SHALL NOT itself authorize native loading, environment construction,
  seed access, fitting, training, evaluation, gameplay, or promotion.
- Make no source, algorithm, model, checkpoint, CommunicationMod, cohort,
  resource, threshold, retry, or gameplay-policy change.
- Before push, rollback removes only additive untracked request artifacts. After
  push, preserve the request as immutable; any defect requires a new request
  identity and review rather than editing the pushed bytes.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `noncombat-cross-fitted-hierarchical-learning-successor`: bind the exact
  20260808-r2 execution request as a separate source-only lifecycle boundary
  after registration and before delegated approval or authorization.

## Impact

- Additive execution-request and review artifacts under `reports/`.
- Existing source-only `render-request` producer path and independent standard-
  library request validation; no implementation edit is expected.
- One delta requirement, project-direction update, and archived OpenSpec change.
