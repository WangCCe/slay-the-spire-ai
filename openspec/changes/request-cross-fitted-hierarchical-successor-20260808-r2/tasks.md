## 1. Freeze The Request Plan

- [x] 1.1 Strictly validate and independently review the proposal, design, delta requirement, exact registration binding, non-authorizing boundary, and rollback rule.
- [ ] 1.2 Commit and push only the OpenSpec plan before rendering request evidence.

## 2. Revalidate Registration Boundary

- [ ] 2.1 Confirm local `HEAD` equals `origin/master`, contains exact registration SHA-256 `9d792cadbece4ea21768386904633ebded2e94525fb186bdcbf4a4d7729dbdf9`, and descends from its readiness source/publication/evidence commits.
- [ ] 2.2 Confirm the registered output root, request target, review target, delegated approval, authorization, execution journal, checkpoint, and terminal artifacts are absent.
- [ ] 2.3 Revalidate the compact registration and r5 readiness evidence through the source-only CLI without importing Torch/native/runtime code or changing external state.

## 3. Render And Review Request

- [ ] 3.1 Capture one successful source-only `render-request` stdout into staging and require canonical byte round-trip equality before publication.
- [ ] 3.2 Independently validate request schema, digest, registration/source/native/runtime/output/schedule/resource/resume bindings, exact operations, all-false base authority, and exact requested-execution authority.
- [ ] 3.3 Publish only the canonical request and deterministic review; verify output-root and approval/authorization/empirical artifact absence and zero blocked-dependency import delta.

## 4. Verify And Close

- [ ] 4.1 Run focused request/control/verifier tests, canonical/self-digest probes, import isolation, and strict OpenSpec with scoped pytest temp storage.
- [ ] 4.2 Record the commit gate and fresh gameplay validation as not applicable unless implementation or test source unexpectedly changes; do not rerun the known long gate for evidence-only bytes.
- [ ] 4.3 Obtain a bounded independent final review and resolve only publication-blocking request defects without changing registration terms.
- [ ] 4.4 Verify exact staged scope, commit and push request evidence while the change remains active, and confirm the exact request blob on `origin/master`.
- [ ] 4.5 After 4.4, sync the delta requirement, update project direction, archive the completed change, and verify final closeout scope for a separate commit and push.
- [ ] 4.6 Stop without publishing standing-delegation resolution, approval, authorization, native loading, environment construction, seed access, fitting, training, evaluation, execution, or gameplay artifacts.
