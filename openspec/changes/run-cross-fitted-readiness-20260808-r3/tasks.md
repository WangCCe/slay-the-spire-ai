## 1. Finalize The Source Identity

- [x] 1.1 Reconfirm compact baseline commit `08d2c74e6e923380f32bc8aa5aa75c8c337f27d7` is pushed, r1/r2 evidence remains immutable, and every registered r3 attempt, output, scratch, staging, and sealed path is absent.
- [x] 1.2 Sync the r3 delta to the canonical readiness main spec and strictly validate the change and both affected readiness and successor capabilities.
- [x] 1.3 Run focused source-only readiness and preservation regressions and obtain independent read-only review of the exact command, paths, compact-baseline ancestry, one-shot rule, exact-approval gate, and all-false authority; do not rerun the compact implementation commit gate for planning-only bytes and do not launch gameplay.
- [x] 1.4 Commit and push the complete r3 plan plus compact-change closeout, then require exact `HEAD == origin/master`, clean tracked status, compact-baseline ancestry, and the exact registered 16-row no-renames diff before naming the final source identity.

## 2. One-Shot Source-Only Publication

- [ ] 2.1 Immediately before authorization and invocation, recheck exact `HEAD == origin/master`, compact-baseline ancestry, clean tracked status, the canonical 16-row no-renames diff, every bound input, all eight exact r1/r2 size/SHA-256 bindings and directory inventories, and absence of the source-keyed attempt, output, scratch, staging, and sealed paths.
- [ ] 2.2 Present one exact human authorization request naming the final source commit; interpreter, `-I`, absolute script, repository root and cwd, and complete command; audit id; absolute output, scratch, source-keyed attempt, staging, and sealed derivations; 7,200-second outer wait; exact stage, verifier, empirical, control-reservation, artifact, schedule, chunk, and consumed-registration ceilings and sizes; exact all-false maps; at-most-once claim; and no-retry boundary. Stop without invoking until that complete tuple is explicitly approved.
- [ ] 2.3 Only after exact approval, invoke the registered r3 auditor command at most once, keep every registered limit unchanged, and use the source-specific Windows process query to prove the auditor and every descendant absent before inspecting any attempt, output, scratch, staging, or sealed path; do not treat outer-wrapper exit or the synthetic-child lease as top-level readiness completion.
- [ ] 2.4 Independently verify the canonical publication or terminal receipts, recompute source and receipt identities and all-false authority, and confirm no native, runtime, model, empirical outcome, training, evaluation, game, or CommunicationMod operation occurred.

## 3. Terminal Closeout

- [ ] 3.1 Publish the r3 readiness decision and eligibility boundary in project direction without creating a compact empirical-successor registration or execution request.
- [ ] 3.2 Sync and archive the completed r3 change, run final source-only checks, then commit and push terminal evidence without retrying the consumed source identity.
