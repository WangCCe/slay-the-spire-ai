## 1. Freeze The Candidate Boundary

- [x] 1.1 Verify from Git that the seven named files are the complete ordinary test-file delta after `52edf92f8`, excluding only the already-full-only conditional-choice file; record the 262.89s, 515.35s, and 528.59s timing evidence.
- [x] 1.2 Strict-validate and independently review the complete planning artifacts, then commit and push them alone as the immutable candidate-set boundary.

## 2. Measure Attribution Once

- [ ] 2.1 Run the exact seven-file suite once on the production Windows interpreter with a fresh registered pytest temp child, `--durations=50`, xUnit1 JUnit XML, and total-phase durations; preserve its complete result without retry, source edits, or candidate changes.
- [ ] 2.2 Verify XML hash, counts, unique file mapping, finite nonnegative times, per-file presence, and at most 30.00s absolute XML-to-pytest-wall residual; only then apply the preregistered 5.00-second per-file and 265.70-second aggregate rules. On failure, independently review a terminal report, mark selection/final-gate tasks inapplicable, and proceed only to failure closeout without manifest edits.

## 3. Publish The Frozen Test Boundary

- [ ] 3.1 Add RED exact-membership/manifest regressions for `17 + selected_count` `full_only` files, nonblank measured rationales, inclusive ordinary collection, and unchanged `full` construction.
- [ ] 3.2 Add exactly the deterministically selected entries to `tests/test_gate_manifest.json` and update direct-ownership documentation without changing the runner, pytest configuration, assertions, or complete-suite membership.
- [ ] 3.3 Draft the dated r3 report with fixed baseline, invalidation, attribution, candidate identities, ownership rules, success metric, rollback, and open final-gate fields.

## 4. Verify Before Final Gates

- [ ] 4.1 Reuse the preserved attribution result; run complete runner/manifest tests, command-construction dry runs, strict OpenSpec, diff checks, and independent review without rerunning the seven-file suite.
- [ ] 4.2 Record base HEAD, canonical diff hash, individual gate-affecting manifest/runner-test/docs hashes, and a separate pre-gate report hash for the reviewed uncommitted selection; do not commit or push it yet.

## 5. Qualify Once

- [ ] 5.1 Preflight the registered temp root and required sandbox permission, verify the gate-affecting hashes, then run finalized `commit` exactly once with a 720-second outer observer; require exit zero and at most 300 seconds. Reverify the hashes afterward. On failure or slowness, preserve the result, restore only the uncommitted manifest/runner-test/docs selection draft, skip `full`, and enter terminal closeout without retry or tuning.
- [ ] 5.2 Only after an in-budget commit pass and another hash verification, run unchanged `full` exactly once with a 3,600-second outer observer. Reverify the hashes afterward. On failure, preserve the result, restore only the uncommitted manifest/runner-test/docs selection draft, and enter terminal closeout without retry.
- [ ] 5.3 Finalize and independently review the success or terminal report using only preserved outcomes; record fresh gameplay validation as not applicable.

## 6. Closeout

- [ ] 6.1 On success, reverify the gate-affecting hashes, commit/push the frozen selection with content matching the hash ledger, then sync/archive in an independent closeout commit/push. On failure, publish only independently reviewed terminal evidence and archived planning. Strict-validate the completed closeout without changing policy/RL authority.
