## 1. RED Contracts And Source Boundaries

- [x] 1.1 Add exact-schema, all-false-authority, and typed-decision regression tests.
- [x] 1.2 Add pushed Git identity, clean tracked-tree, immutable evidence hash, and drift-precedence regression tests.
- [x] 1.3 Add canonical historical inventory, exact 512-seed selection, and full consumed-cohort disjointness regression tests.
- [x] 1.4 Add actual-scale scratch rehearsal tests for one 64-position chunk, constant validation counts, watchdogs, complete process-tree cleanup, blocked imports, and cleanup isolation.
- [x] 1.5 Add exact-decimal budget, fixed failure precedence, deterministic-gzip artifact limits, and JSON/Markdown agreement regression tests.

## 2. Auditor And Independent Verifier

- [x] 2.1 Implement the source-only readiness auditor with canonical schemas and pushed source/evidence bindings.
- [x] 2.2 Implement complete seed inventory reconstruction, fixed candidate schedule publication, and consumed-cohort disjointness checks.
- [x] 2.3 Implement the isolated actual-scale child rehearsal without native, Torch, runtime fitting, environment construction, or empirical seed access.
- [x] 2.4 Implement fixed budget arithmetic, source-keyed attempt receipts, typed decision precedence, verify-before-install staging, bounded deterministic-gzip/JSON/Markdown publication, and all-false downstream authority.
- [x] 2.5 Implement a separate standard-library verifier that reconstructs every decision input with streamed Git/artifact handling, a fixed deadline, and no auditor or empirical dependency imports.

## 3. Verification And Publication

- [x] 3.1 Run focused source-only tests and import-isolation checks without loading native code or consuming a seed.
- [x] 3.2 Obtain an independent source review and resolve accepted findings with RED regressions.
- [x] 3.3 Strictly validate OpenSpec, run the repository commit gate once, then commit and push the implementation source.
- [ ] 3.4 Run exactly one bounded source-only readiness publication against the pushed implementation commit without retrying, tuning, or widening a ceiling.
- [ ] 3.5 Independently verify the canonical report, publish the project-direction decision, and keep every empirical authority false.
- [ ] 3.6 Sync and archive the completed OpenSpec change, run required final checks, then commit and push the evidence without creating an empirical registration.
