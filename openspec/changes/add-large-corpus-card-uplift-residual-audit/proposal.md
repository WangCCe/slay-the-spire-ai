## Why

The 79-parameter card-uplift residual passed every fixed train cross-fit and
development gate, including 11 corrected versus one worsened development
action. The pre-reserved `80320..80383` cohort is now the necessary independent
test before considering any fresh gameplay evaluation.

## What Changes

- Bind the frozen residual model, selected configuration, r7 entry checkpoint,
  expanded corpus evidence, source, native identity, and reserved audit seeds.
- Collect at most two complete card states per audit seed and 512 action
  branches, with at most four registered Courier censors.
- Score the frozen r7 entry and frozen residual on at least 110 complete audit
  states without refitting or changing configuration.
- Apply the same regret, ranking, correction, and safety gates used on
  development and publish exact audit artifacts.

## Capabilities

### New Capabilities

- `noncombat-large-corpus-card-uplift-residual-audit`: Defines fixed-model
  reserved-audit collection, evaluation, isolation, gates, and no-promotion
  result authority.

### Modified Capabilities

None.

## Impact

- Adds one bounded native audit runner and focused tests.
- Consumes the registered `80320..80383` audit cohort with at most 512 branches,
  expected to take 20–30 minutes.
- Does not train, tune, start gameplay/CommunicationMod, modify production
  checkpoints, or promote a policy. Rollback remains the r7/native policy.
