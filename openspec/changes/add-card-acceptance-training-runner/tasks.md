## 1. Freeze Runner Boundary

- [x] 1.1 Reconfirm pushed r6 registration/request/review identities, parent tasks 6.4/6.5 incomplete, absent authorization/output, registered control/runtime zero drift, additive registration-support drift, and the no-runner readiness evidence; strict-validate and tool-prohibited review the complete proposal, design, specs, and tasks.
- [x] 1.2 Resolve every planning finding, then commit and push the planning/readiness boundary before adding runner source, tests, launch manifests, authorization, or output artifacts.

## 2. Add RED Runner Regressions

- [x] 2.1 Add RED tests for exact canonical launch-manifest, request-subordinate runner composites, standing-delegation/exact-human resolutions, and command-specific run/terminalization envelopes; bind runner/source/interpreter/request/registration/review/seed-inventory producer/independent registration verifier/source inventory/native module/recursive PE import/dependent DLL identity/output/resource/rollback authority, stage authorization/approval/fresh observation, dead owner/lease/prefix where applicable, closed CLI identity, all-false authority, duplicate/unknown/noncanonical input rejection, and immutable self-digests.
- [x] 2.2 Add RED source-only preflight tests proving absent authorization is readiness-only, the output root must be absent, an existing root is rejected without child inspection, and runtime/native/model import, registration seed decoding, environment construction, lease acquisition, checkpoint access, directory discovery, and output creation are not attempted.
- [x] 2.3 Add RED execution-composition tests with injected fake registration/runtime/environment/process observers for immutable r6 registration plus manifest-bound rollback view/run-envelope launch marker, exact 512-seed order, candidate/control write-ahead debits, resource charging, zero-progress initial checkpoint and per-chunk predecessor/initial/final canonical state chain with independently derived component hashes, family-saturation rollback/terminal, partial-chunk preservation, setup reopen, and sole complete-boundary continuation.
- [x] 2.4 Add RED dead-owner terminalization tests for a fresh owner/lease/prefix-bound terminalization envelope, sibling guard and closure marker, final prefix validation under an exact stale-owner lease lock without owner mutation, exact registered rollback/failure closure, idempotent same-envelope closure-only resume after terminalizer death, prior-run-envelope/conflicting-prefix/staging rejection, permitted byte-level control/production identity observation, and zero runtime/native/model/environment/seed/replay access.
- [x] 2.5 Add RED standalone-verifier tests for manifest/command/source drift, authorization/launch-observation drift, checkpoint/journal/resource mismatch, terminal ordering, unknown artifacts, true downstream authority, and prohibited canary/holdout evidence.

## 3. Implement Minimal Runner

- [x] 3.1 Implement the standard-library launch-manifest, runner-composite authority resolvers, and command-specific run/terminalization-envelope builders/parsers/validators plus exact closed `preflight`/`run-training`/`terminalize-dead-owner` CLI without importing execution runtime or native/model modules at module load.
- [x] 3.2 Implement source-only preflight with pushed/tracked source checks, exact request/registration/review and runner/verifier bindings, source-inventory path/hash binding without opening it, canonical output-root absence, bounded all-false completion, and no output child inspection, writes, checkpoint access, or seed decoding.
- [x] 3.3 Implement the private one-process training lifecycle: complete authority plus exact pushed run-envelope validation before source-inventory access and bounded lifecycle-state inspection, source-bound seed-inventory producer plus independent r6 registration validation, immutable r6 registration plus manifest-bound rollback execution view/run-envelope launch marker, process-owned lease, exact cohort extraction after authorization, source-inventory reobservation plus manifest-bound adapter/native/runtime lazy loading under native and dependent-DLL write/delete-denying handles, recursive PE import closure with delay-import rejection and unique manifest-path resolution, and registered public-API boundary, environment construction, per-chunk paired collection, write-ahead hooks, resource accounting, zero-progress initial checkpoint and predecessor-linked independently derived checkpoint bindings/publication, terminal/rollback closure, and existing setup-reopen/continuation rules.
- [x] 3.4 Implement dead-owner terminalization with fresh command-specific envelope authority, proven process death, sibling guard, exact stale-owner lease locking without owner mutation, final owner/lease/prefix validation, closure marker, idempotent same-envelope closure-only resume, registered candidate-disabled rollback/failure closure, and no runtime restoration, seed access, empirical operation, continuation, or replay.
- [x] 3.5 Implement an independent standard-library runner verifier that reconstructs manifest, command, lifecycle identity, journals, ledgers, checkpoints, terminal/rollback evidence, process closure, and denied downstream operations without importing runner, control, runtime, native, or model modules.

## 4. Qualify Source Only

- [x] 4.1 Run focused runner/control/runtime/verifier pytest under the Windows system-temp convention, compile checks, and direct import probes proving runner/verifier imports do not load Torch/native/model/runtime execution modules.
- [x] 4.2 Run deterministic source-only preflight, run-envelope, and dead-owner terminalization-envelope fixtures plus mutation/failure-order probes in fresh processes; require preflight zero native/model/environment/seed/checkpoint/output access, terminalizer zero empirical/replay access, and exact bounded completion.
- [x] 4.3 Run the configured commit gate and configured full gate once each at the reviewed boundary, record duration and infrastructure-only failures without retrying for cleanup/timing, strict-validate OpenSpec globally, and record live gameplay validation as not applicable.
- [ ] 4.4 Give a tool-prohibited reviewer exact source/diff/test evidence only, resolve every actionable finding, then commit and push the tracked-clean runner source boundary without authorization or training.

## 5. Publish Runner Readiness

- [ ] 5.1 From pushed tracked-clean source, render one canonical r6 launch manifest and source-only preflight binding the exact runner/verifier source, interpreter, r6 registration/request/review/source inventory, native module/recursive PE import/dependent DLL/API/provenance identity, output root, resources, denied operations, rollback authority/control/production-isolation identities, and closed CLI set while authorization/output remain absent.
- [ ] 5.2 Independently text-review, validate, commit, and push the launch manifest/preflight; keep parent tasks 6.4/6.5 incomplete and do not create approval, authorization, launch observation, execution envelope, lease, runtime import, or training output.
- [ ] 5.3 Publish a bounded closeout, sync applicable main specs, archive this change, strict-validate globally, verify tracked path hygiene, and commit/push without native/model/environment/gameplay/training/evaluation/OPE/qualification/promotion operations.
