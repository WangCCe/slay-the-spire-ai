## 1. RED Regression Boundaries

- [ ] 1.1 Add context-factory tests for one complete raw registration validation, cached canonical identity/output values, caller-mapping mutation isolation, and corrupt raw boundary rejection.
- [ ] 1.2 Add a synthetic 64-access regression proving complete registration-validation counts remain constant through debit, journal replay, resource reconciliation, and terminal status while durable corruption checks still fail closed.
- [ ] 1.3 Add exact-clock regressions for first-chunk deadline failure, non-infrastructure failure, prior resume charge, ceiling clamping, idempotent final charge, and verifier rejection of a missing or mismatched terminal charge revision.
- [ ] 1.4 Add same-process terminal-intent reuse and interrupted-publication recovery regressions proving canonical bytes and inventories remain exact without nested complete registration validation.
- [ ] 1.5 Preserve import isolation, live/unreadable lease rejection, dead-owner recovery, true-child liveness documentation, and all-false empirical authority in focused source tests.

## 2. Control-Plane Repair

- [ ] 2.1 Implement the private validated execution context and raw-versus-context boundary routing without a global, object-id, or cross-process cache.
- [ ] 2.2 Propagate the context through authorized execution, access journals, resources, checkpoints, failure handling, isolation, and terminal helpers while retaining schedule, lease, hash-chain, and byte validation.
- [ ] 2.3 Generalize attempt elapsed charging to every post-start terminal path and update the independent verifier for the exact final resource revision.
- [ ] 2.4 Carry the just-published terminal intent through same-process terminal and manifest construction while keeping full raw-file validation for recovery.
- [ ] 2.5 Update execution guidance and project direction so wrapper completion cannot substitute for true Python child exit and an unlocked lease.

## 3. Verification And Publication

- [ ] 3.1 Run focused control, verifier, runtime-boundary, and import-isolation suites with a fresh system-temp pytest child; run no native or empirical test.
- [ ] 3.2 Publish a deterministic source-only repair report with structural validation counts, synthetic terminal/resource evidence, scope, and all downstream authority false.
- [ ] 3.3 Run an independent code/spec/authority review, add RED regressions for accepted findings, and make only narrow control-plane fixes.
- [ ] 3.4 Run strict change/global OpenSpec validation, `git diff --check`, and the repository commit gate exactly once; record its result without rerunning solely for duration.
- [ ] 3.5 Sync and archive the completed change, update project direction, commit, and push source-only artifacts without registration, native loading, seed access, fitting, training, evaluation, gameplay, or CommunicationMod.
