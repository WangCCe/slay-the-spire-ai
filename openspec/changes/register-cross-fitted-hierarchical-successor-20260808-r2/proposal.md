## Why

The independently verified r5 readiness publication is a `go` with proposal
eligibility true, 512 fresh seeds, zero collision, and all downstream authority
false. It is now possible to create the separately reviewed compact
registration that the successor contract requires, without yet requesting or
authorizing empirical execution.

## What Changes

- Build one compact v2 all-false registration for successor identity
  `noncombat-cross-fitted-hierarchical-learning-successor-20260808-r2`, bound to
  readiness source `ffd9acc444258483d172529eccfe8ccb05c9bb9b`, publication
  commit `0c62aff2fb301dd491017bbf2e775c36a177bf67`, and readiness identity
  `cdff8937e6c15b27df582bf8d9d606c2185d94cdcfbe4e4f907d6a0244e48d38`.
- Bind the exact pushed source inventory, r5 report/candidate/verification
  receipt, complete 8x64 schedule, native build identity, CPU runtime,
  CommunicationMod and production-checkpoint isolation, immutable resource
  ceilings, output root, and retry/resume contract.
- Publish the canonical registration plus a deterministic source-only review
  that independently checks source ancestry, readiness evidence, schedule,
  isolation, authority, and absence of dependency loading or empirical work.
- Treat success as exact source-only validation of one tracked registration,
  with all authority and empirical-operation maps false and no new native,
  model, environment, seed, fitting, training, evaluation, OPE, gameplay,
  qualification, promotion, execution-request, approval, or authorization
  artifact.
- Make no algorithm, control-plane, verifier, gameplay-policy, production
  checkpoint, CommunicationMod, or threshold change.
- Before the registration commit, rollback removes only this additive change
  and untracked candidate artifacts. After the registration is pushed, preserve
  it as immutable evidence; any defect requires a new registration identity
  rather than editing or reusing r2.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `noncombat-cross-fitted-hierarchical-learning-successor`: bind the exact
  20260808-r2 compact all-false registration and its source-only review boundary
  to the independently verified r5 readiness publication.

## Impact

- Additive registration and review artifacts under `reports/`.
- Source-only orchestration through
  `analysis_scripts/noncombat_cross_fitted_hierarchical_learning_experiment.py`
  and its independent verifier; no production or runtime source edit is
  expected.
- One delta requirement and project-direction update recording the immutable
  registration boundary.
