## 1. Freeze The R7 Contract And Stop Rules

- [x] 1.1 Reconfirm the proposal-approved scope, archived pre-request observability and qualifier contracts, current canonical registration, r1-r6 dispositions, still-absent registered study root, and all-false `start`/collection/OPE/policy/training authority.
- [x] 1.2 Define the exact previously absent r7 root and fixed direct-child claim/stage/request/handoff/handshake/terminal/failure paths; prove every path and the external attestation path absent through lexical no-follow inspection before creating any candidate byte.
- [x] 1.3 Freeze the no-source-edit rule. A defect found before publication SHALL preserve the preparation, leave the live root absent, open a separate TDD source-fix change, and stop. A defect found after publication SHALL stop any planned invocation, preserve and retire the published root under the supported evidence branch, open the separate source-fix change, and never continue or retry r7.
- [x] 1.4 Add an amendment-local deterministic checklist/report schema for offline go/no-go and final closeout with exact source, request, root, configuration, inventory, process, result, attestation, disposition, limitation, and authority fields; do not add runtime code or a new live command.

## 2. Render The Offline Candidate

- [x] 2.1 Select and record one clean implementation snapshot S after proposal approval; prove the tracked worktree, registration, implementation map, runner, launcher, verifier, handshake, and ordinary gameplay startup bytes match S and that no executable/importable drift, unsafe Git metadata, or lazy-fetch dependency exists.
- [x] 2.2 Capture the exact current CommunicationMod configuration bytes and semantics, marker/run/checkpoint/global-log inventories, registered study-root absence, target-process inventory, and every request-bound protected path without launching Java or the game.
- [x] 2.3 Deterministically render the canonical r7 request-v3/bootstrap-v1 candidate, launch token, fixed launcher vector, runner hash, expected static-root image, baseline restoration package, direct-child inert allowlist, and all-false authority into a guarded repository-local review area while the external r7 root remains absent.
- [x] 2.4 Render the complete candidate twice in fresh processes and require byte-identical canonical ASCII/LF outputs, exact sizes and SHA-256 values, no placeholder or host-secret leakage, and no mutation of CommunicationMod, game, study, model, policy, checkpoint, marker, run, or global-log state.
- [x] 2.5 Prepare the exact proposed S-to-R inert review tree and external anchor construction using the established source-only qualification workflow, but do not create the final R, publish the external root, or launch r7 until precommit review findings are resolved.

## 3. Prove The Offline Go/No-Go Gate

- [x] 3.1 Run focused Windows pytest for qualifier runner, pre-request stages, source-only loader, independent verifier, handshake, runtime qualification exit, and path/isolation regressions with cache disabled and a fresh writable repository basetemp; clean or prove absent every generated executable/importable path before source replay.
- [x] 3.2 Run the complete Windows pytest suite serially with cache disabled and a fresh writable repository basetemp unless an existing full transcript binds the exact S inputs; preserve post-R transcripts outside the guarded repository worktree and record external paths, hashes, exit codes, counts, and input stability.
- [x] 3.3 Run `openspec validate --all --strict`, `git diff --check`, canonical candidate replay, exact S-to-R diff and source inventory checks, CommunicationMod whitespace-vector reconstruction, root/path absence checks, ASCII/stale-marker scans, and protected-state no-mutation comparison.
- [ ] 3.4 Obtain an independent precommit code and governance review of the proposed inert tree, candidate request, launcher/runner anchors, static-root image, rollback package, process/configuration assumptions, one-shot boundary, and authority fields; resolve documentation-only findings, then create and push one final direct-child R and perform an exact post-commit source-only re-review. Preserve post-R transcripts outside the guarded repository worktree with external anchors and route every source finding through task 1.3.
- [ ] 3.5 Publish one externally anchored canonical offline go/no-go record without changing R or its worktree. A no-go SHALL preserve obsolete preparation, leave the external r7 root and CommunicationMod bytes unchanged, enter the absence-only branches of section 5, and skip every task in section 4; only an exact all-green `go` MAY proceed.

## 4. Invoke R7 At Most Once

- [ ] 4.1 From an exact current `go`, recheck `HEAD == R`, every source/request/review/runner hash, external root absence, baseline configuration and protected inventories, registered study-root absence, zero target processes, and unchanged rollback material immediately before live publication.
- [ ] 4.2 Exclusively publish the exact reviewed static r7 root and exact request-bound CommunicationMod launch configuration, then independently compare every byte, semantic field, fixed path, inventory entry, and launcher token with the go record before starting ModTheSpire.
- [ ] 4.3 If any post-publication comparison fails before the planned operator invocation, do not issue it and preserve the exact published root. When zero target processes and absent control/result paths remain provable, restore and independently compare the CommunicationMod baseline and protected inventories, bind evidence that the invocation command was not issued, and enter the post-publication/no-invocation branches of section 5. If a target process, control path, launch observation, or uncertainty appears, perform controlled cleanup and enter the live-boundary branches instead. In either case skip tasks 4.4-4.5 and never delete or reuse r7.
- [ ] 4.4 Only after every post-publication comparison passes, invoke r7 exactly once through real CommunicationMod and `D:\anaconda\envs\stsai\python.exe`; preserve the externally observed launch boundary and immutable root while the trusted launcher creates the claim, the qualifier owns the no-action child handshake, and no external release monitor, retry, second game launch, study `start`, or gameplay action is permitted.
- [ ] 4.5 Observe only bounded qualification/process state needed to determine termination. If the qualifier has not assumed request-owned restoration or any process survives unexpectedly, stop the owned/target processes, restore the exact request-bound baseline, and recollect isolation without deleting, repairing, completing, or retrying r7.

## 5. Replay, Attest, And Classify R7

- [ ] 5.1 Select the evidence branch without inference. For offline no-go, bind external-root/control/result/attestation absence plus unchanged CommunicationMod, protected inventories, and process state. For post-publication/no-invocation, preserve the exact static-root inventory, prove control/result paths absent, bind restored CommunicationMod and protected-state equality, and bind evidence that the operator invocation command was not issued and target-process inventory stayed zero. For an observed or uncertain live boundary, preserve the exact post-publication root inventory and declared absences, result anchors when present, before/after bytes, inventory comparison, process or launch observations, and controlled-cleanup record without synthesizing any field.
- [ ] 5.2 For an observed or uncertain live boundary, run the standalone verifier only when its required anchors exist and require exact classification of the supported complete terminal, partial, invalid, abrupt, failed, or externally observed no-claim boundary without manufacturing a producer result; otherwise record verifier non-applicability and bind only the independently supported external boundary. For offline no-go, do not invoke terminal replay; independently replay only the frozen candidate and prove every external live/root/result path remains absent. For post-publication/no-invocation, do not invoke terminal replay; independently verify the immutable static root against the reviewed candidate and prove control/result absence, exact baseline restoration, unchanged protected inventories, and zero operator-invocation/target-process evidence.
- [ ] 5.3 Produce one branch-specific external attestation: an offline absence attestation for no-go, a publication/no-invocation attestation binding the preserved root and zero-invocation restoration evidence, or a live-boundary attestation binding every available verifier or external observation, request-bound restoration, protected inventory and registered-root comparisons, final zero target processes, and root inventory. Neither non-live branch SHALL claim a terminal or live verifier result, and any live uncertainty SHALL retire r7.
- [ ] 5.4 Classify exactly one governance disposition: `obsolete_before_publication`, `retired_after_publication_without_invocation`, `retired_after_live_boundary`, or `qualified_for_start_review`. Even the qualified disposition SHALL NOT invoke `start`, create a run lock, collect a trajectory, inspect outcomes, compute OPE, change policy/rewards, train, or promote.
- [ ] 5.5 Determine the evidence-backed deferred-task updates for `bind-qualification-isolation-evidence`, `fix-cold-start-study-handshake-timeout`, and `run-v2-known-propensity-outcome-evidence-study`. Apply tracked checkbox edits immediately only for obsolete or retired branches; for a qualified branch preserve the proposed updates in the external handoff and make no tracked write before the later frozen-study boundary releases it.

## 6. Close Or Preserve The Amendment

- [ ] 6.1 Render a deterministic external closeout binding the exact proposal/source range, preparation/go-no-go bytes, S/R/request/review/root/result/verifier/attestation hashes or absences, configuration and protected-state comparison, process evidence, disposition, limitations, rollback result, and all-false authority. Import it into the repository immediately only for obsolete or retired branches.
- [ ] 6.2 Re-run strict OpenSpec validation, read-only diff/whitespace/hash/stale-marker checks, closeout replay, and independent final review without changing tracked R; run focused or full pytest again only if any bound source or test input changed after the offline gate.
- [ ] 6.3 Apply the branch-specific freeze. For obsolete or retired branches, commit and push the reviewed repository closeout, sync delta specs, and archive this amendment. For a qualified branch, keep HEAD/worktree exactly at R, preserve the handoff and provisional closeout externally, leave this change active, and do not update tasks, commit, sync, archive, checkout another commit, prepare r8, or advance to `start` inside this amendment.
- [ ] 6.4 Only after a later reviewed decision declines `start`, or after the complete run-lock tracked-write prohibition ends following study finalization and independent verification, import the exact externally anchored qualified closeout and deferred task updates, review them, commit/push, sync, and archive this amendment.
