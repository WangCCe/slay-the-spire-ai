## Why

The pre-request observability and tracked qualifier repairs are now implemented, reviewed, synced, and archived, while r1-r6 remain failed, obsolete, or retired and no replacement identity exists. A separately reviewed amendment is required before preparing one previously absent r7 identity, and that amendment must prove either one independently attested no-action qualification or one immutable fail-closed retirement without authorizing study `start`.

## What Changes

- Add an offline r7 preparation phase that proves the identity and root were previously absent, freezes a clean source snapshot S, preserves the exact CommunicationMod baseline and protected live inventories, renders one canonical request-v3/bootstrap-v1 package, and permits only inert direct-child review paths.
- Require independent source-only review of the exact S-to-R diff, runner bytes, launch vector, request/review anchors, static root, rollback material, and all-false authority before any live invocation. If preparation exposes a source or protocol defect, stop and route that defect through a separate regression-backed source-fix change; do not modify implementation and continue under the same identity.
- After every offline gate passes, permit at most one real CommunicationMod invocation through the production Windows Python and the reviewed stdlib launcher. The child remains no-action, the qualifier owns the bounded handshake, and every bootstrap/request/handoff/terminal prefix is preserved without retry, repair, deletion, or identity reuse.
- Independently attest the resulting r7 root, external terminal anchors, request-bound configuration restoration, protected marker/run/checkpoint/log inventories, and zero surviving target processes. A complete valid chain marks r7 qualified only for a later `start` review; any invalid, partial, missing, or cleanup-uncertain chain retires r7 permanently.
- Record exactly one deterministic disposition: obsolete before publication, retired after publication without an issued invocation, retired after an observed or uncertain live boundary, or qualified for later `start` review. A published r7 root SHALL be preserved and retired if the final prelaunch comparison fails; it SHALL NOT be deleted, reused, or mislabeled as an absence-only no-go. Obsolete or retired branches may commit and archive immediately; a qualified branch SHALL keep its handoff and closeout externally anchored, preserve tracked-clean `HEAD == R`, and defer every tracked write, task update, sync, and archive until the later `start` decision declines launch or the existing run-lock no-write window closes. Success is a fully replayable one-shot result with exact restoration and closed authority, not a favorable study result or permission to collect evidence.
- Keep study `start`, run-lock creation, slot execution, outcome inspection, OPE interpretation, reward or gameplay-policy changes, training, causal claims, and promotion out of scope.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pre-request-qualification-observability`: bind one previously absent r7 preparation, independent review gate, at-most-once live qualification, immutable terminal or partial evidence, restoration proof, and independent attestation to the existing v3 observability contract.
- `noncombat-outcome-evidence-expansion`: define the r7 result handoff so even a valid qualification remains only input to a later reviewed `start` decision, while every failed or partial result remains consumed and non-authorizing.

## Impact

- Planning and evidence surfaces: a new immutable r7 preparation/root, canonical request/review/terminal anchors, an independent attestation, and a deterministic branch-specific closeout; a qualified closeout remains external until the frozen-study tracked-write boundary permits import. R1-r6 and every recorded absence remain unchanged.
- Runtime surfaces under review: `scripts/run_noncombat_outcome_evidence_expansion.py`, `analysis_scripts/verify_noncombat_outcome_evidence_expansion.py`, the reviewed qualification launcher, and existing handshake/runtime boundaries. No source edit is planned in this change.
- Live boundary: one bounded Windows-only CommunicationMod qualification through `D:\anaconda\envs\stsai\python.exe` after a separately recorded offline go/no-go review. The exact pre-launch CommunicationMod bytes and protected inventories are the rollback baseline.
- Rollback and failure: before publication, keep rejected review preparation as obsolete, non-authorizing evidence while leaving the external r7 root absent. After publication but before an operator invocation, preserve the published root, restore and attest the exact baseline, and retire r7 without retry; classify no-invocation only when zero target processes and absent control paths remain provable, otherwise use the uncertain-live-boundary branch. After invocation, preserve the consumed root, stop owned/target processes, restore the exact request-bound baseline, independently recollect isolation, and retire r7 on any uncertainty; never delete evidence or retry the identity.
- Non-goals: no Java/game launch during proposal or offline preparation, no registered study root or run lock, no gameplay, no collection, no OPE, no model/checkpoint mutation, no training, and no policy decision.
