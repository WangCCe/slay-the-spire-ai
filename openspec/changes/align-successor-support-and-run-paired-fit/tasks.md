## 1. Contracts And Regression Fixtures

- [x] 1.1 Add failing fixtures for source/parent/config binding, development-run
  exclusion, fixed four-batch inventory, no-takeover authority, and practical
  five-run batch recovery.
- [x] 1.2 Add failing fixtures for exact opportunity membership, unique
  trace-to-state joins, run assignment, context-row identity, malformed logs,
  and target sufficiency.
- [x] 1.3 Add failing fixtures for direct context weighting, unchanged support
  gates, stop-before-optimizer behavior, and conditional delegation to the
  existing paired fit.

## 2. Live Context Target

- [x] 2.1 Implement source-only registration and preflight for the four
  parent-only five-run batches and target publication.
- [x] 2.2 Implement candidate-independent guard-replacement membership, exact
  raw-state joining, completed-run assignment, exclusions, and context rows.
- [x] 2.3 Implement target sufficiency, development/fresh seed isolation,
  per-batch summaries, deterministic identity, round-trip validation, atomic
  publication, and failure receipts.

## 3. Aligned Support And Conditional Fit

- [x] 3.1 Extend context weighting to consume the immutable live target rows
  without constructing a fake replay transition batch.
- [x] 3.2 Implement a source-bound wrapper that validates the target and merged
  corpora, evaluates every unchanged support gate, and constructs no optimizer
  on failure.
- [x] 3.3 On support pass, delegate once to the unchanged current-state control
  and successor-delta fit, calibration, deferred fresh evaluation, and policy
  decision while preserving all row, weight, arm, seed, and parent identities.
- [x] 3.4 Publish aligned support, model artifacts, sampling identities,
  thresholds, metrics, manifests, and development-only authority atomically.

## 4. Focused Verification And Registration

- [x] 4.1 Run focused target, support, fit-wrapper, and predecessor regression
  tests with a fresh system-temp pytest child.
- [x] 4.2 Run strict OpenSpec validation and source-only input, run-exclusion,
  collision, configuration, and no-takeover inventories.
- [x] 4.3 Run exactly one full commit gate for the cohesive source boundary with
  a timing report, then commit and push source plus the four batch and target
  registrations before gameplay.
- [x] 4.4 Retain the terminal r1 publication failure, add regression coverage
  for bounded missing joins versus ambiguous or reused joins, and commit new
  source-bound r2 registrations before collecting a fresh cohort. Do not run a
  second full gate.

## 5. Fresh Parent-Only Holdout

- [x] 5.1 Collect each registered five-run batch with Windows CommunicationMod,
  production r16 action authority, and no candidate takeover; inspect fresh
  `.run`, AI debug, CommunicationMod error, shadow, and decision-state evidence.
- [x] 5.2 Seal exactly 20 unique fresh runs, build the target once, and verify at
  least 300 total and 20 late-floor joined opportunities plus all parent,
  authority, seed, trace, join, identity, and manifest conditions.

## 6. Conditional Training Evidence

- [ ] 6.1 Bind and commit the sealed target in the exact fit registration,
  evaluate aligned support once, and stop without fitting or more gameplay if
  any unchanged gate fails.
- [ ] 6.2 If support passes, execute the registered fixed paired fit once and
  verify parent immutability, equal arm samples, frozen thresholds, deferred
  fresh access, model round trips, and hard/descriptive decisions.
- [ ] 6.3 Commit and push the target and terminal decision artifacts without a
  second full gate, then sync and archive the completed OpenSpec change.
