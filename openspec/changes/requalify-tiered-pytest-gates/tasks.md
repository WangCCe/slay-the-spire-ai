## 1. Freeze The Requalification

- [x] 1.1 Record the over-ceiling 2026-08-09 commit gate, the verifier,
  lifecycle-candidate, runtime, and remaining-set duration measurements, the
  fixed 13-file candidate set, rollback boundary, and unchanged five-minute
  success metric.
- [x] 1.2 Strictly validate, commit, and push the archived original capability
  plus this complete requalification plan before changing the manifest,
  executable tests, or qualification report.

## 2. Reclassify Measured Boundaries

- [ ] 2.1 Add RED manifest regressions for exact repository `full_only`
  membership, nonblank measured rationales, inclusive ordinary-test behavior,
  and unchanged `full` construction.
- [ ] 2.2 Add the 13 measured whole-file entries without changing the runner,
  pytest configuration, any test body, or any domain profile target.
- [ ] 2.3 Update testing documentation so an excluded file or its owned source
  requires direct focused validation, while ordinary code retains focused plus
  `commit` and the complete boundary remains `full`.

## 3. Qualify The Final Boundary

- [ ] 3.1 Run runner/manifest-focused regressions, command-construction probes,
  strict OpenSpec validation, and `git diff --check`.
- [ ] 3.2 Run the final `commit` profile once, require a green result at or below
  300 seconds, and record test count, skips, exclusions, and wall time without
  retrying merely for duration.
- [ ] 3.3 Because the manifest and `full_only` boundary changed, run the
  unchanged `full` profile once; preserve its exact result and duration without
  retries or test edits.

## 4. Publish And Close

- [ ] 4.1 Publish the dated qualification report with all attribution and final
  gate evidence, and update project direction; fresh gameplay validation is
  not applicable because no live behavior changes.
- [ ] 4.2 Sync the modified capability, archive the change, run final strict
  OpenSpec validation, commit the cohesive gate requalification, and push
  `master` without staging unrelated artifacts.
